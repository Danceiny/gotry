# dsh 上游若干包的 peer 范围(如 dsh-map-tools@0.4.4 声明 ^0.1.0-rc.5)
# 在 semver 预发布规则下永远无法被 0.1.1-rc.x 满足——npm 10+ 严格模式会
# ERESOLVE(node 22 内置 npm 即如此)。产品依赖树以 lockfile 为权威,
# 这里放开 peer 严格校验,本地贡献者与 CI(npm ci)行为一致。
legacy-peer-deps=true