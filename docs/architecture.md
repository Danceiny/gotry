[English](architecture.md) | [简体中文](architecture.zh-CN.md)

# GoTry Technical Documentation (Single Technical Authority)

> Positioning: this repo's **complete technical documentation** — what the system is, how it is composed, where each module lives, how to run it, and where it evolves.
> Audience: engineering agents and humans who take over or maintain this repo. For "what form is it in now" see §1; for "why was it decided this way" see §8; for "what work to pick up" see §10.
> Discipline: a single file carries a single concern; version history belongs to git; no vN file suffixes. Upstream: the master outline (`gotry-master-outline.md`) and product design (`gotry-product-design.md`).
> Downstream: loopx todos are derived from this document's §9 evolution stages and §10 debt list; engine/solver detail work happens only when flagged in the debt list.

**At a glance**

- GoTry is the "from departure to the next departure" AI travel Agent: the LLM handles understanding and explanation, deterministic components handle verdicts and arithmetic, and write operations are always gated.
- Current form: M3 minimal-viable, no known blockers in the distribution chain; M3 Exit lacks real cohort evidence (D-18); the M4 memory domain advances in parallel under founder authorization.
- The dsh outer lifecycle is owned by the GoTry launcher: the child runs in its own POSIX process group; exit/error (including delayed close caused by inherited stdio), abnormal close, and parent-only SIGINT/SIGTERM go through bounded TERM→KILL with confirmation that the direct child/group is drained; this does not cover the deeper owner contracts of the dsh SDK transport or its internal supervisor.
- M4 scoring and explicit-consent collection, tenant CLI boundaries, Z3 lifecycle, and full-stack stability fixes have all landed in main; #258/#267 web onboarding is only the M4 UX engineering surface, and its POSIX onboarding/installer process groups plus accepted-install signal/timeout cleanup do not advance #20 Exit. The M4→M6 living task graph and M5 WriteGate still only schedule dependencies and acceptance. Real M3/M4 cohorts, supply agreements/internal authorization, P6 approval, and real pilots remain TODO; each milestone's Entry/Exit is unchanged.
- Five layers: L1 interaction / L2 orchestration (dsh plugin) / L3 unified itinerary model + Z3 / L4 data capabilities / L5 loopx governance.
- State foundation: single-file SQLite ledger (ADR-15), one ledger semantics for local + Web (ADR-16); `tenant_id` runs through append/read/fold/rebuild; old local history does not guess tenants; all external dependencies go through the effect interpreter (ADR-18).
- External benchmark: Round 1–10 frozen treatments and the Round 11 wire slice produce no attributable official score/uplift; the Round 12 structural half (config v4 closed body schema) has merged but the frozen rerun has not been run (D-28); per-round facts in §9, engineering contract in `evaluation/benchmark-environment-bridge.md`.
- **#290 compatibility note (2026-09-09)**: the target `dsh-system-prompt` Config exposes `personaPrefix` / `personaSuffix`; legacy `persona:` is not projected. The bridge handler classifies errors structurally: `timed_out` is a deadline abort, `spawn_failed` is a synchronous spawn failure or a rejected public `done`, `runner_failed` is a resolved non-zero exit or a failure reading collected output after resolution. See the §9 #290 entry. No claim of release/publication, M5/M6 entry, Windows execution, or real supplier/HotelByte admission.
- **#327 historical duplicate-argument revision (2026-09-10; superseded by #3580)**: #327 temporarily admitted a narrow identical-duplicate recovery path. The current planner accepts exactly one canonical JSON object through one `JSON.parse`; repeated objects, nested/stringified envelopes, prefixes, trailing garbage, truncation, non-object sequences, and scalar/object coercions all fail closed. Schema-shape correction must happen as a typed `INVALID_ARGS` rejection followed by a later canonical call in the same DSH run, never in an adapter recovery scanner. Evidence is limited to offline and local-subprocess engineering proofs; it does not constitute real provider reliability, HotelByte UAT, M3/M4 cohort, or M5/M6 admission.
- **#341 ground-transfer first slice (#364 direction binding)**: `gotry_feasibility_check` accepts only explicit origin/destination latitude-longitude and `mode=driving`, generating route facts with `asOf`, freshness/cache, provenance, and evidence class from the registered public `map_driving_route` result; the arrival direction (A→B) and the return direction (B→A) are requested, cached, and degraded separately, so a miss/error/stale/mismatch on one side only falls back that side to the static estimate and never borrows the other side's dynamic value; it only overrides the static `taxi` transfer minutes of a named destination (per-direction `minutesOut`/`minutesRet` overrides consumed by `evaluateChoice`), while `bus`/`bus_plus_taxi` keep static values with an explicit fallback returned, and static prices are always labeled `[static pack: estimate]`. `asOf` is the host observation/query time, not the provider publish time; cache hits and expired re-queries are visible; provider failure, invalid coordinates, unsupported modes, and binding mismatches all explicitly keep the static values; it does not cover live traffic, transit/rail/fare, nor change the existing #20/M5/M6 admission boundaries. The 2026-09-11 boundary-freeze suite pins this offline boundary closed (vocabulary closure, complete fallback taxonomy including self-contradictory route facts, the 900-second switch contract, and evidence-annotation completeness); the wider remainder is gated real-data work, not engineering residue.
- **Public delivery ledger (#270)**: every execution lane starts with an issue and is filed via Draft PR, exact-head review, and merge/destination receipt; founder-authorized in-repo Claude Code/worktree lanes are exempt from external-bot T0/T1 veto, but still pass normal review and the evidence gate. Public contract in `ops/external-pr-workflow.md` §0; local/fixture proofs do not change the real gates of #20/#22/#136/#137.
- For work go to §10.1; the timeline belongs to `roadmap.md`; documentation organization rules belong to `README.md`.

**Table of contents**

| | | | |
|---|---|---|---|
| [1 What the system is](#1-what-the-system-is) | [2 Overall architecture](#2-overall-architecture-five-layers-and-current-state) | [3 Code map](#3-code-map-what-each-module-is) | [4 Unified itinerary model](#4-unified-itinerary-model-domain-core-only-solve-entry) |
| [5 Conversation loop](#5-conversation-loop-l2) | [6 Data and runtime](#6-data-and-runtime) | [7 Testing and verification](#7-testing-and-verification-strategy) | [8 ADR](#8-adr) |
| [9 Evolution](#9-evolution-the-single-source-of-timeline--roadmapmds-m0-m6-only-principles-and-current-state-are-kept-here) | [10 Debt list](#10-debt-register-engine-detail-work-may-only-originate-here) | [11 Freshness mechanism](#11-freshness-mechanism-sync-discipline-between-docs-and-reality) | [12 Document map](#12-document-map) |

---

## 1. What the system is

> This section only answers "what the system **currently** is". "What happened when and why it changed" all belongs to §9 evolution and
> `release-notes.md` — any date narrative appearing in this section is misplaced (§11 freshness discipline item 4).

**GoTry is the "from departure to the next departure" AI travel Agent**: motivation interview in, verified itinerary plans and multiple-choice gates out; the LLM handles understanding and explanation, deterministic components handle verdicts and arithmetic, and write operations are always gated.

M3 minimal viable product; no known blockers in the distribution chain.

**Timezone surface (Issue #343)**: `ts/src/tz-resolver.ts` resolves v2's explicit IANA zone and local date into a UTC instant, rejecting unknown zones and DST gaps/overlaps at the parse boundary; `doorToDoorFromMove` in `ts/src/model.ts` uniformly uses UTC instants to compute durations. The dsh/mock adapter keeps the v2 pack's `homeZone`; profile provides only schedule; explicit vacation removes the trip's work-window restriction; numeric v1 stays compatible. This deterministic contract does not represent live schedules/prices/availability/inventory; data-source boundaries in [`docs/data-sources.md`](data-sources.md).

### 1.1 Delivery form and entry

| | |
|---|---|
| Entry | `./gotry` (in-repo) or `npx @danceiny/gotry@latest web` (npm) |
| Runtime | dsh production form, DeepSeek native |
| Version | see `package.json` and `release-notes.md`; **the `rc` dist-tag already points at the latest rc (completed 2026-09-02, #50②) and `@rc` is usable; user guidance still prefers `@latest`** |
| License | MIT |
| Persona | 13 behavioral-contract clauses (slimmed 2026-09-11: narrow-domain detail relocated into tool descriptions / routing card; count regression anchor = persona-surface-guard-tests); anchor card, memory brief and channel routing card injected into persona. **Single source of runtime composition = repo-root `cordis.gotry-patch.yml`** |

### 1.2 Capability surface

The tool surface is registered in `ts/src/index.ts` (**the inventory and counts defer to code; no numbers are hardcoded here** — §11 freshness checklist item 4):

- **Verdict class**: feasibility, skeleton validation, flight verification, **artifact fact gate `gotry_fact_gate`** (mandatory before delivery; when blocked you must not claim a "verified plan")
- **Feasibility date boundary (Issue #2)**: `gotry_feasibility_check` applies a host-clock future lower bound by default to any dated candidate; an explicit `planning.intent=future` request year additionally gets a year-end upper bound; expired years are not rolled forward; expected rejections return a structured validation result rather than an incident. Fully dateless input keeps the old feasibility computation; historical computation requires explicit `planning.intent=historical`.
- **Retrieval class**: hotels (`gotry_hotel_search` hbcli bridge + **session surface ctrip-hotel**: real prices under the user's login state, passive sniffing), weather, Anything generic search, web pages, video subtitles, GitHub, Fliggy (飞猪) official retrieval (flight/train/hotel), session retrieval (flight/hotel/train; train = 12306 public query surface; **dida supplier portal**: hotel-be portal integration migration line, 2026-09-09)
- **Hotel date input gate (issue #283, D-36)**: `parseAbsoluteDate` shared from `time-anchor.ts` uses `isRealIsoDate` to reject nonexistent calendar days; the hotel consumption boundary further rejects missing dates, arithmetic overflow, and checkout not later than checkin; only valid dates enter `HBCLI_HOTEL_SEARCH`. Failures uniformly return `verdict=input_required` without dispatching hbcli; valid dates and the existing explicit static fallback after supplier failure stay compatible. Isolated fixtures only prove the engineering boundary; they do not constitute real supplier admission.
- **Ctrip (携程) flight malformed-response gate (issue #279)**: the structured batchSearch parse judges a legitimate empty list as `miss`, a list containing valid segments as `hit`, and malformed unknown shapes as `error`; the compatible `parseBatchSearch` still never throws and returns `[]` on error. Both the extension and CDP lanes judge the challenge first, then map parser errors to a structured `error` without exposing options.
- **FlyAI malformed-response gate (issue #352)**: `gotry_flyai_search` keeps a business `miss` for an exactly empty `data.itemList: []`; every item of a non-empty flight/train/hotel list must pass typed entry parsing — any malformed sibling returns a whole structured `error` with `via=flyai-error` and discards valid siblings, so it never enters the fact-side negative-inventory branch; only a fully valid list can `hit`. `flyai-tests.ts` uses an isolated fake CLI to cover production effect, fact sidecar, empty list, full hit, three-way mixes, typed fields, exit failure, and 429; this is only local offline engineering evidence and does not constitute real provider/UAT or issue closure.
- **Offline sf evidence summary (issue #335, Tracks #272)**: `sf-summary.ts --evidence-root <temp root>` first inventories batches by producer canonical evidence filename, then selects the latest candidate by batch collection time; it preserves each record's requested/effective/fallback, per-record capture/start, and manifest/batch provenance; legacy batch-less records are marked unknown/ineligible; a missing or corrupt latest batch fails closed. Any selected challenge/guard evidence (#411), including a complete eight-record batch or legacy top-level `sessionVerdict`, likewise stays fail-closed and is annotated with `challenge_stop_detected`. This deterministic fixture evidence does not represent live login state, inventory, current interface shape, or packaged connected/degraded calibration. Issue #420 (2026-09-12): the standalone CLI's main guard now canonicalizes both `process.argv[1]` and `import.meta.url` via `realpathSync`, so file- and directory-symlink invocations (and macOS `/var`↔`/private/var` aliases) reach main() and yield identical summaries to the canonical path; import-inertness is preserved (no default HOME evidenceRoot write).
- **Memory class**: motivation, wish pool (admission/recall), travel timeline, companions
- **Artifact class**: artifact list/read (ledger work-order deliverables + session working-directory md/html, read-only) — **#285 slice (2026-09-10)**: the Host persists `presentationMeta`; the public `./client` adapter registers two custom wire-name `tool.call.toolview` keyed cards via `window.__ModuleLoader__.load`, rendering clickable paths, line-number previews, source identity, and content version from the runtime `block`; workspace/sidebar remain additional file-preview surfaces. Read-scope whitelist = stateRoot root + session dsh working directory (excluding node_modules/.git); extension whitelist = text types (md/txt/json/jsonl/csv/log/yaml/yml/html/htm); cross-root / symlink escape / missing file / oversized file (>2MB) uniformly return ok:false + error + hint. This layer is read-only; the WriteGate red line is not involved. Acceptance evidence = `scripts/artifacts-capability-tests.ts` 25 isolated fixture proofs + `scripts/dsh-artifact-web-e2e.ts` fresh-profile Web list→select/open→read→edit→updated-read proof + smoke §15/§15b. **#441 slice (2026-09-12, parent #438)**: top-level working-directory `.html`/`.htm` artifacts are discovered case-insensitively alongside `.md` with the same non-recursive / dotfile-excluded / mtime-desc semantics, and read back as source text (`lang: html`, original line numbers, full-content fingerprint, paging) — the read path does not parse markup, runs no script or inline event handler and fetches nothing, and the read card labels HTML as source (`HTML 源码预览（只读文本）`). Opening a listed HTML entry is a separate, explicitly labelled client action that hands the file to the host's native HTML preview: the client marks it with an `Open HTML preview` accessible label, a visible HTML-preview badge and a note that the page's scripts may run; that rendering (and any script execution) belongs to the host renderer, not to this read layer; the registered Lavish browser feedback chain is covered by #443 CLOSED + PR #456 merged acceptance. Native HTML preview proof is accepted on #448 and its persistent regression lives in `ts/scripts/dsh-artifact-web-e2e.ts` (re-runnable: `GOTRY_ARTIFACT_WEB_E2E_OUT=<dir> npx tsx ts/scripts/dsh-artifact-web-e2e.ts`); the IAB-side blob-iframe blank and the `srcdoc` comparison normal are old-environment observations and neither implies that IAB is repaired. **#458 slice (2026-09-12, on top of #25/#285/#441)**: `listArtifacts` paging + literal metadata search — `offset` (zero-based nonnegative integer) and `search` (literal case-insensitive substring over `id`/`title`/filename; trim then empty = no filter; no regex/glob semantics, body content never searched) are first collected → canonical-path dedupe → search filter → deterministic global sort (`updated` DESC + `(source, id, canonical path)` codepoint tie-break) → page slice. Default page size 20, cap 50; pre-#458 integer `limit` zero/negative clamps to 1 (compatible). `total` reflects the post-filter set; `nextOffset` only when `offset + returned < total`; an `offset` past the end returns an empty page with the known total + `truncated:false` + no `nextOffset`. Validation refuses non-finite / non-integer / out-of-range / non-string `limit`/`offset`/`search` predictably (no silent coercion to "no filter"); invalid input echo keeps the requested `offset` so a caller can detect a beyond-end request, and the summary text names "empty page and known total" instead of "no artifacts" when `total > 0`. Acceptance evidence = `scripts/artifacts-capability-tests.ts` 25 isolated fixture proofs (12 base + 13–18 #441 + 19–25 #458: >50 cross-page enumeration, equal-timestamp tie-break, mixed-source dedupe, literal search cases, invalid-type rejection, search+paging across >50, read boundaries under paging) + `scripts/dsh-artifact-e2e.ts` registered-plugin list→pagination/search→read→Host-presentation proof (>50 mixed-source fixtures, stable ties / dedupe / filtered totals / safe args).
- **Itinerary HTML document generation (internal slice of #442, parent #438; 2026-09-12)**: the pure bounded renderer `ts/src/itinerary-html.ts` (plan plane vs evidence plane, no IO/`Date.now`/Markdown parsing/date·night·budget arithmetic, explicit `ok:false` rejection set) is reachable from a real product entry — the registered tool `gotry_itinerary_render` (`ts/capabilities/itinerary-artifact.ts`) takes `title`, an explicit itinerary object and `fact_ids`, loads facts **only** from the current `config.stateRoot` fact registry (caller-supplied fact objects are never accepted; unknown/duplicate/over-limit ids and malformed registered rows are rejected), renders, and creates exactly one **new** file at the session working directory top level (`gotry-itinerary-<ASCII token>.html`, `O_CREAT|O_EXCL` — no overwrite, no symlink-target escape, `.git`/`node_modules` refused, crypto-random name when no basename is given); the session working directory must be an explicit absolute path from the host session — a missing, empty, whitespace-only or relative cwd fails closed in the tool boundary and again in the capability, with deliberately no process-cwd fallback — and the result returns the final real path; invalid input writes zero bytes. An empty `fact_ids` list is a legitimate explicitly-unverified plan and no overall "verified" badge exists. Evidence = run-all §6c (`itinerary-html-tests.ts` + `itinerary-artifact-tests.ts`, isolated `mkdtemp` stateRoot/cwd fixtures). Native HTML preview proof is accepted on #448; persistent regression lives in `ts/scripts/dsh-artifact-web-e2e.ts` (re-runnable command above). The registered Lavish browser feedback chain is covered by #443 CLOSED + PR #456 merged acceptance. The generated document's browser coverage (real browser navigation, native details and Return-keyboard, narrow-screen readability, escaping) is closed by #442 CLOSED + PR #449 merged acceptance. This slice is bounded to the registered renderer + generation + browser coverage above; the generated document is not supplier evidence and not M3 / M4 / M5 admission.
- **Lavish local review tools (#443, parent #438)**: the host explicitly enables five default-off tools through `lavishAxiPackageRoot`. They bind to the exact host session and workspace, serializing bounded untrusted feedback. Terminal records reject active commands; stop preserves owned cleanup and its failure result, and plugin unload closes registration. Existing itinerary generation, discovery and source reading feed the review. See the [Lavish contract](design/lavish-local.md#9-registration-into-the-product-tool-surface); the registered Lavish browser feedback chain is covered by #443 CLOSED + PR #456 merged acceptance, and native HTML preview proof is accepted on #448 with persistent regression in `ts/scripts/dsh-artifact-web-e2e.ts` (re-runnable: `GOTRY_ARTIFACT_WEB_E2E_OUT=<dir> npx tsx ts/scripts/dsh-artifact-web-e2e.ts`). This engineering capability does not establish M4/M5/M6 admission.
- **Account class**: session login `gotry_session_login` (pops the login entry in the user's Chrome; ticket cookie names pass through name-only, zero values)
- **Turn class**: `gotry_turn_handoff_list` revisit query for background deep-planning work orders (read-only; open = planning in background/ETA, settled = deliverable excerpt, failed = honest failure note; collection settlement is driven by `scripts/turn-handoff-collect.ts`, ADR-24 v2)
- **Booking planner read-only constraint (#282; authority tightened by #3580)**: the embedded planner never deletes a non-empty occupancy room or child-age condition. A room missing `adults` remains schema-invalid; the plugin returns typed `INVALID_ARGS`, and only a later canonical call in the same DSH run may supersede it. The adapter neither coerces the action nor retries an invalid event trace in another provider run. Prose-only/no-tool responses retain a bounded three-run nudge, while provider failures are surfaced without being swallowed. Evidence is limited to offline engineering proofs; it does not replace real supplier inventory/UAT or the M5 gate.
- **Booking Copilot planner shared-source derivation (#263, 2026-09-09; authority tightened by #3580)**: the planner prompt and persona derive from the canonical `booking.surface` schema rather than hand-maintained duplicates. `ts/src/time-anchor.ts` supplies the shared host-local relative-date anchor, and `SearchCriteriaPatch` names plus the shape-only search example derive from the canonical contract. The earlier numeric-keyed-array, nested-envelope, missing-version and scalar/object coercions have been removed: model quirks must be corrected through a typed plugin rejection and a later canonical tool call, never by rewriting a successful action in the adapter. The planner follows the selected provider route/model and preserves the reasoning-model token budget. Real inventory/UAT remains D-29.
- **Booking Copilot provider failure, typed authority and route-tuple boundary (#3580, 2026-09-12)**: an executable planner decision must come from exactly one canonical, schema-valid registered `tool/call` paired by `callId` with its successful `tool/result`; missing, duplicate, mismatched, out-of-order, rejected or competing successful calls fail closed. A malformed or unauthorized event trace is never retried in a later provider run. Plugin schema failures use DSH `ToolArgsError` (`INVALID_ARGS`), so only an earlier rejected call followed by one later canonical success inside the same run can recover. `finalResponse`, fenced JSON, repeated JSON, stringified/nested decisions and adapter-repaired actions are non-executable text or invalid data. The adapter reads provider failures only from authoritative DSH events (`turn/end.reason.error`, with the packed `assistant/attempt` finish record as a compatibility fallback only when `turn/end` is absent). Authentication and quota exhaustion are non-retryable; rate limits, empty responses and transport/server failures are retryable; invalid requests, invalid responses, configuration failures and unknown failures fail closed under a finite typed error registry with provider-authored messages removed. The child environment selects one complete credential/base/model/token-budget namespace and never cross-mixes `DEEPSEEK_*` with `LLM_*`; invalid or incomplete route tuples fail before model use. Tenant and customer journeys consume the shared 11-action full-journey allowlist; `hotel.select` remains payment-link-only. Focused injected-event and real local DSH subprocess proofs cover this boundary; provider qualification and live surface UAT remain separate evidence.
- **gotry-backend release build script (#367, 2026-09-10)**: `scripts/build-booking-copilot-release.mjs` (and the matching `scripts/build-gotry-backend-release.mjs`) wrap the kernel + booking-copilot + session-search modules into the published artifact that `bin/gotry-backend.js` consumes. The release script runs only after the typed contract proofs (`ts/scripts/kernel-tests.ts` 8/8) and the booking-copilot server proof (`ts/scripts/booking-copilot-runtime-proof-tests.ts`) are green; the kernel's module-exact routing table is the single source of dispatch. Not a code-behavior change — the same HTTP surface continues under `GOTRY_BACKEND_PORT > GOTRY_BOOKING_COPILOT_PORT > 3082`; this entry exists so the artifact path is traceable to a release-time script rather than an ad-hoc assembly step.
- **Execution environment pinned to the browser client (#380, 2026-09-11, founder dictum "server-side zero Chrome")**: `ts/capabilities/session/extension-bridge.ts` extracts a transport-agnostic `createBridgeJobQueue`; `handleMountedRequest` mounts the gotry-backend `/v1/session/bridge/{health,status,jobs,results}` auth surface (results go through the query adapter to the kernel's exact routing table). `ts/capabilities/session-search.ts` introduces `multiCollect` for the dida recommended-flow lane — `hotels` + `recommendPrices` both arriving is the settlement condition (extension-side bucketing + Node-side `mergeDidaRatesBodies`). `ts/src/backend/modules/session-search.ts` drops the CDP/transport dependency and exposes only the bridge endpoints; `login/open` lets the extension bring the dida login page to the front. The founder directive "hide all config from employees; portal dispatches join ticket" moves the hotelbyte product's `join ticket` dispatch out of the CLI surface — employees see no configuration, only a join flow. `extension/background.js` and `extension/content-bridge.js` wire the remote bridge (chrome.storage + optional_host_permissions + options page); dida `multiCollect` is the canonical recommended-flow wiring. §38 extension-contract tests 36/36. Version bump: `package.json` `0.0.1-rc.22 → 0.0.1-rc.24`, `extension/manifest.json` follows (release notes for this version live in `docs/release-notes.md` v0.0.1-rc.23 / v0.0.1-rc.24).
- **Booking planner factRef authority (#212, tightened by #3580)**: action ids and fact refs must already satisfy the runtime-safe charset and length boundary. Unsafe refs and the reserved `modelref:` namespace are rejected; the adapter no longer hashes or aliases model-authored refs. Every canonical tool-call action is validated before the safe-ref gate. Assistant text remains non-executable even when it contains valid-looking JSON.
- **Booking Copilot embedded planner persona target and host-driven workspace validation (#467 + #468, 2026-09-13, in §9)**: `buildDshEmbeddedBookingPatch` now targets the registered public `system-prompt` row, so the embedded personaPrefix and corrected decision envelope example actually reach the first outgoing DSH model request. For `search.run` and `search.patch`, the runtime validates the host's post-action workspace: a higher revision requires cleared hotel focus, loaded offers, shortlist, selection, and verification; an unchanged revision permits no content mutation. Missing optional fields compare safely. Real Booking UAT remains D-29.
- **Booking planner schema-rejection loop (#278, tightened by #3580)**: plugin validation emits typed `INVALID_ARGS`, letting the DSH loop show the exact rejection to the model and accept one later canonical call in that same run. The adapter never drops rooms, invents adults, coerces arrays or repairs action envelopes. A run that ends with only an invalid call fails closed; only a no-tool/prose response receives the separate bounded tool-call nudge.
- **Booking-executor observation surface (#377, 2026-09-10, M1 slice 1)**: `ts/src/backend/modules/booking-executor.ts` exposes only `POST /v1/booking/observe` — opens the supplier page via CDP, reads the booking-entry button text, and captures a screenshot for evidence. The module deliberately contains no fill/submit primitives; those belong to a future controlled-executor behind the M5 WriteGate red line. Auth is fail-closed (`GOTRY_BACKEND_BOOKING_API_KEY`); off-domain `entryUrl` is rejected. The observer is fully injectable — 5 isolated offline contract tests in `ts/scripts/booking-executor-tests.ts` cover the surface without touching real supplier pages. This is M1 read-only observation only, no product behavior change for `gotry web` users; it lays the audit substrate that M5 WriteGate / `approval_claims` / `write_effect_intents` will build on.
- **Dida supplier-portal adapter v2 (#372, 2026-09-10)**: triggered by the D-13 first real-session calibration — `ts/capabilities/session/adapters/dida-portal.ts` now recognizes both `HotelRecommendAPI/SearchHomepageRecommendHotels` (60 hotels) and `SearchHomepageRecommendPrices` (top-6 starting prices) in addition to `PopularDestinationAPI/SearchHotels` (24) + `SearchHotelPrices` (8 prices), since UAT probes found the load-state recommended flow (not the July-caliber `HotelPriceAPI/SearchRealTime`) is what the page spontaneously emits. `parseDidaRecommendHotels`/`parseDidaPrices` accept both `HotelId` and `HotelID` keys; `SessionDidaRateOption` carries `priceDate`. The CDP lane uses windowed multi-response collection (bodies batched by endpoint, merged at window close); the extension lane keeps single-response discipline (multi-sniffing still requires a fresh store submission). `extension/content-main.js` sniffers extended in lockstep. extension-tests 34/34 + session-tests 200 pass (2 fail = FlyAI 429 environmental). Real inventory/UAT remains D-29.
- **Self-check class**: `gotry_doctor` is a read-only doctor by default; with explicit `action=repair` it selects auto-repairable items by stable item id, requests in-session scope approval, reuses the bootstrap idempotent installer, and actually re-checks; the CLI-side same-source command is `npx @danceiny/gotry doctor`, with the report written to `gotry-state/doctor-report.md`
  - Coverage: extension / agent-reach `.venv` / hbcli / FlyAI key (including latest anonymous-trial rate-limit time) / dsh-calendar mount state / sidebar / bundled MIT `dsh-map-tools` (`SettingsProvider.prototype.installSection` wiring; both 0.1.2-alpha.3 and 0.1.5-alpha.1 already published) / `dsh-tool-ask-user`, each graded ok/degraded/missing + precise install instructions
  - Installation is triggered only via three explicitly authorized entries: `npx @danceiny/gotry doctor --fix`, the per-launch onboarding prompt before interactive `gotry web` startup (#258/#267), or the native approval card of `gotry_doctor action=repair` (#284); all three reuse the same idempotent installer. Browser-store installation, credentials/API keys, profile configuration, package reinstallation, and Node upgrades remain user actions; no-approval-channel/denial/cancellation all execute zero. LLM keys belong to the dsh host, deliberately unmanaged
  - Tool-layer not-installed/needs-setup errors uniformly point to doctor (2026-09-02 Dubai session postmortem: blocks blind retries against broken channels)
- **Ops surface**: quality-metrics read-only aggregation `ts/scripts/build-metrics-report.ts` (2026-09-05, #138 first slice) — fact gate verdict distribution and blocked rate / channel-health down·cooldown (30-day window) / incident surface (7-day window; fatal observed via `uncaughtExceptionMonitor` while preserving host exit semantics) / bridge latency p50·p95·max (**>500ms over-limit count = the visible face of the §11 review-cadence trigger anchor**) / ledger and doctor report existence, aggregated into a single markdown; fully read-only with zero SQLite opens (`openDb` may write kv on connect); defaults to stdout, writes only with `--out`; an engineering-surface deliverable, does not constitute M3 Exit evidence (belongs to #22)
- **External-drive surface**: channel probe tick `ts/scripts/channel-probe.ts` (2026-09-07, external-event seam segment 1, #82 local producer) — read-only probing of headlessly probeable channels (hbcli whoami / open-meteo / opensky; session-family explicitly skipped as unprobeable; flyai not probed by default to save quota), writing `down` events on anomaly and `'ok'` events on recovery (latest-wins supersedes down) into `channel-health.jsonl`; loopx/cron drives one round per run, non-resident. The persisted health surface is consumed by wish-pool recall (`conditions.channels` optional condition + down-refutation at recall, `wish-channel-gate-tests` §53), by existing doctor readers, and — since #436 — by the six non-hit tool results: the tool-result boundary reads `readLatestChannelEvents` with the strict-timestamp option and projects the pure `persistedDownChannels` set into `routingAdvice`, so a persisted `down` excludes the channel with no verdict call; missing/unparseable/future/retention-expired rows are dropped before latest-wins so they cannot hide an earlier valid down; only `down` adds a persisted exclusion — `'ok'` recovery, expiry, or a valid latest `cooldown` merely ceases it under the existing routing rules — and the persisted set never clears the session state. The #432 W2A slice adds only an inert, default-off, pure adapter contract (`ts/capabilities/external-event.ts`, run-all §53b): serialized `w2a/0.1` core envelopes are bounded to 64 KiB, admitted only by an explicit exact `{sensorId, package, sensorVersion, sourceType}` tuple, and projected to untrusted typed metadata while `summary` / `user_identity` / `source_event` / `attachments` / `_meta` are stripped. It registers no listener or consumer, makes no authentication claim, writes no health/wish/ledger state, and does not change runtime behavior; real sensor auth, transport ownership, and consumer activation remain #82.
- **Outreach**: AgentReach wrapper (upstream installed in `.venv`; reflection bridge `agent-reach-bridge.py` calls the upstream registry directly, zero channel knowledge)

**The tool surface has no preset static routing priority** (persona (19) stays flat); channel ordering is generated by the **channel registry** and filtered by health state (D-8):

- `ts/capabilities/channel-registry.ts` is the single data source (intent × channel × quota class × evidence level); the persona routing card (`{{channel_routing_card}}`) and the in-result `routing` advice (when verdict≠hit) are generated from the same source.
- Advice is not dispatch; the interpreter performs no hidden rerouting; the session health surface (`channel-health.ts`) records down/cooldown, cleared on `hit`; the six non-hit tool-result `routing` fields additionally read the persisted latest health surface at the tool-result boundary and exclude the union of the session state and that persisted down set (session down wins).
- Per-source evidence-chain annotation is unchanged.

Data intake: three-layer flights (skeleton 168 pairs + verification bridge + anchors), hotel hbcli bridge (realtime/static fallback + evidence annotation), OpenSky realtime ADS-B, Open-Meteo weather, Fliggy FlyAI official read-only channel.

### 1.3 Account sessions and the authorization gate

- **Authorization gate**: the `tools/pre-execute` listener (`session-consent.ts`) requests authorization via the dsh native ApprovalService on **first call per session per site**. allowed-once is recorded in the session granted set; subsequent calls in the session skip the prompt; **user denial = revocation for this session**, no more prompts and no execution. No approval channel / headless always **fails closed**.
- **Sub-agent and jobs id boundary (#194 track A)**: before `job_output`/`job_kill` enter the dsh jobs registry, GoTry reads the current caller's direct-child listing via typed `tools/pre-execute`; on hitting a continuable durable id it returns a recoverable error in the host's same shape, pointing to the completion notice, `list_agents`, and `send_message` (use `interrupt_agent` to stop the current turn). Misses, one-shots, or non-owners still fall back to native dsh jobs ownership/errors; the benchmark minimal kernel does not install this product guard. The upstream dsh unknown-id root contract is still tracked by [#194](https://github.com/Danceiny/gotry/issues/194).
- **Site binding (2026-09-10, #308 forward fix)**: multiple kinds of the same tool (`gotry_session_search` → flight / hotel / dida / train) share `interpretArgs`' query-first selection with execute; a missing kind keeps the flight default; authorization and denial are bucketed per site; unknown/malformed kinds fail closed (deny, no privilege expansion). 12306 remains a public query surface, but train still goes through `sessionAccess: off|ask|allow` and per-site denial/cancellation semantics, never bypassing the authorization gate. Empirical regression in session-tests §I + smoke §13.
- **Switch**: plugin config `sessionAccess: ask|allow|off` (disable anytime / explicit pre-authorization / master switch off) — RFC pillar ④ "explicit user authorization + site whitelist + disable anytime" in code.
- **Login**: `capabilities/session-login.ts` pops the login entry in the user's Chrome; login **always completes on the external site**; the login guide page not mounting ReadGuard is the only exemption surface (the retrieval-surface invariant is unchanged).
- **Read-only invariant**: ReadGuard method×URL two-factor write interception + audit + fail-closed; pacing gate; evidence chain `[session:*]`.

### 1.4 Factuality and state foundation

- **#299/#355 flight/train claim split and 12306 typed rail facts (2026-09-10, D-26/#273 residual)**: `extractClaims` collects complete G/D/C/Z + 3–4 digit train numbers into a separate `trains`; complete-token judgment does not misdelete legitimate two-letter airlines (including `CZ8582`/`9C8781`); `flightClaimVerdict` only looks at `kind:'flight'`; `railClaimVerdict` only looks at `kind:'train'` exact-date/route/code/bookable facts, outputting `rail_claim_unverified` when evidence is missing. Both FlyAI and the 12306 session follow real registered tool → typed fact log → canonical anchor/`gotry_fact_gate`; the 12306 parser distinguishes recognized nonempty/recognized empty/malformed/transport-runtime error; positive facts defer to host-captured invocation route/date and exact response URL binding; the inline startTrainDate is only validated and preserved, with `captured_at_non_future_and_max_age_15m`, `canWebBuy=Y`, and a closed set of available-seat tokens. Lists carry no fares; no new RailFact renderer or static-schedule fallback is added. Evidence remains limited to deterministic adapters/fixtures; no claim of live 12306 or M4/M5/M6 gate.

- **Single source of bookable facts** (ADR-19): `ts/src/bookable-facts.ts` (`gotry_bookable_fact.v1`, pure functions) — flyai/session exact-date tool results are recorded item by item, hit = positive fact / miss = negative fact, query_id replayable, IATA normalized; the four evidence tiers **never merge**. **exact-date miss = "unconfirmed/currently not bookable, re-check at D-xx"**; backfilling from historical schedules/adjacent dates/route pages is forbidden.
- **Artifact fact gate `gotry_artifact_gate.v1`** (`ts/src/artifact-gate.ts`): the three render primitives (`renderFlightFact`/`renderHotelFact`/`renderPolicyFact`) embed `<!-- fact:<fact_id> -->` anchors at line ends — artifacts generated via render primitives carry provenance anchors by construction, and the gate side deterministically back-traces anchors (heuristics yield); hand-edited anchors (unknown fact_id) or hand-edited anchored-line content (policy line as_of inconsistent with the fact) = `fact_anchor_unknown` direct violation. Handwritten/historical artifacts go through heuristic fallback: five claim classes (flight number / carrier nonstop / hotel bookability / policy keywords / airline-airport mapping) + the `policy_without_as_of` time boundary. When the gate blocks, you must not claim a "verified plan" (`presentation=verified_label_forbidden`).
- **Policy anchor full-field content fingerprint (issue [#359](https://github.com/Danceiny/gotry/issues/359), parent #273, D-26 residual closure)**: rendering and the gate share a single authoritative constructor; **when a policy line contains a `<!-- fact:` anchor**, it must strictly equal the canonical full line + a single `<!-- fact:<id> -->` (subject/statement/source/fetched_at/query_id/as_of); any change → `fact_anchor_unknown`, same source as unknown anchors; multiple anchors, malformed anchors, non-canonical non-empty prefix/suffix text, or the borrowing form "borrowing a legitimate anchor to write the opposite policy" all fail closed. Handwritten/historical policy lines **without** `<!-- fact:` still go through the existing unanchored/free-text heuristic fallback, outside this slice (handed to parent #273 / D-26). `opts.itinerary.trip_start` bridges to `opts.tripStart`, and like `review_by` is a legitimate review-reminder form. **Compatibility fallback (legacy public callers)**: when the caller passes neither `opts.tripStart` nor an itinerary with `trip_start`, the gate accepts the renderer's two fixed canonical forms — (a) the no-reminder line strictly equal to `renderPolicyFact(f)`; (b) a line carrying the renderer's fixed phrase `; far-future policies must be re-verified — check once more at YYYY-MM-DD`, where the date must be a real ISO calendar day; the gate extracts the date, rebuilds `renderPolicyFact({...f, review_by: date})`, and compares byte-for-byte with the original line, no longer manually splitting body/provenance; if the fact carries `review_by`, the renderer's only legitimate reminder date is `f.review_by`, and a mismatch with the extracted date fails closed. Even when trip context is known, the `renderPolicyFact(f)` no-reminder canonical form remains legitimate. body/provenance/anchor structure/position are compared after renderer reconstruction, binding subject/statement/as_of/source/fetched_at/query_id/reminder position phrase/exact anchor. Residual: reverse extraction remains a regex heuristic; price tolerance, FX/multi-currency, and M5 WriteGate are reviewed per existing debt; the exchange-rate/multi-currency residual is tracked under [#344](https://github.com/Danceiny/gotry/issues/344) (CNY-only settlement today; the first real non-CNY supplier quote, user budget, or destination demand triggers implementation per [`docs/data-sources.md`](data-sources.md) §2).
- **Flight/hotel anchor field fingerprints (issue [#363](https://github.com/Danceiny/gotry/issues/363), parent #273, D-26 residual closure)**: the render primitives (`renderFlightFact`/`renderHotelFact`) and the gate share the same authoritative facts; the heuristic **re-extracts the flight_no token from the rendered text of a flight line** requiring strict == `fact.flight_no` (uppercase normalized), and **a hotel line must strictly contain** `fact.destination` + `fact.check_in` + `fact.check_out` (exact substrings); any inequality → `fact_anchor_unknown` fail-closed, same source as unknown anchors. **Field fingerprints do not replace full-line strict comparison** (root contract item 2): the comparability of flight_no/carrier/times/route/price, train canonical, policy canonical, §4b price-compatible forms (total budget / baggage fee / grand total / explicit fare / thousands separators / price-on-request / ¥7xx / approximate price), and the classification and ordering of `price_contradicted` vs `unverified_price_claim` are not swallowed by the new fingerprints and keep the existing judgment primitives. **Borrowed/unknown/duplicate anchors** remain handled by the existing `fact_anchor_unknown` path; field fingerprints only close the root counterexample of "keeping anchor/registry/fact_id while rewriting the visible flight_no/destination/date range" fail-closed. The `gotry_fact_gate` registered gate execution surface adds no new paths, keeping `loadFactRegistry` + `gateArtifact`. Residual: reverse extraction remains a regex heuristic; price tolerance, FX/multi-currency, and M5 WriteGate are reviewed per existing debt; parent #273 residual debt retained; the exchange-rate/multi-currency residual is tracked under [#344](https://github.com/Danceiny/gotry/issues/344) (CNY-only settlement today; the first real non-CNY supplier quote, user budget, or destination demand triggers implementation per [`docs/data-sources.md`](data-sources.md) §2).
- **Fact gate price boundary (issue [#300](https://github.com/Danceiny/gotry/issues/300), parent [#273](https://github.com/Danceiny/gotry/issues/273))**: a flight/train exact-date bookable line is compared only within canonical fare fields or explicit fare-label fields against the complete comparable CNY hard price of that flight/train's own fact; amounts separated off with non-fare text labels do not participate; bare/unattributed or malformed fare money yields `unverified_price_claim` blocked. Numeric mismatch = `price_contradicted` blocked; source fact missing price, unsupported hard currency, or no anchor with price outside the heuristic 120-character window = `unverified_price_claim` blocked; ambiguous/non-numeric values are not compared, no conversion guessing, hotel `priceRaw` does not participate. Price tolerance, FX, and multi-currency policy still await owner/founder decision — the exchange-rate/multi-currency residual is tracked under [#344](https://github.com/Danceiny/gotry/issues/344) (CNY-only settlement today; the first real non-CNY supplier quote, user budget, or destination demand triggers implementation per [`docs/data-sources.md`](data-sources.md) §2).
- **Fact gate read-side finite word sets (issues [#301](https://github.com/Danceiny/gotry/issues/301)/[#302](https://github.com/Danceiny/gotry/issues/302), parent #273)**: the handwritten/historical artifact heuristic also promotes bookability assertions under a lodging heading (`## Accommodation` etc.) without lodging tokens to hotel claims, merged with the five non-keyword lodging categories (resort/hostel/youth hostel/villa/apartment; boutique hotels keep using the existing hotel token) triggering `unverifiable_hotel_claim`; a policy line hitting the 13-word finite union (EVUS/ETA/eVisa/vaccine/vaccination/health declaration/quarantine/work visa/residence/return visa/passport validity/yellow fever certificate/insurance) while missing as_of → `policy_without_as_of`. Latin tokens are case-insensitive but not embedded in longer Latin words. Deterministic fixture coverage; no claim of live policy correctness, supplier readiness, or M5/M6 admission.
- **State authority = single-file SQLite ledger** (ADR-15, `ts/src/state-ledger.ts`): events append-only + projection fold rebuildable + red lines in transactions + work-order durable recovery (exactly-once) + pending_writes saga. Old JSON/JSONL degrade to one-way export views. The `state-cli` operation surface centrally and strictly parses cmd/positional/`--state-root`/`--tenant`/`--limit`; unknown/duplicate/missing-value/illegal numerics (including `.5`/`+.5`) fail closed before touching the ledger; `--tenant` is only a scope, not authentication or authorization.
- **Dual-form freeze** (ADR-16): local + Web share one ledger semantics; `tenant_id` is a first-class field; `insertEvent` writes to the current ledger owner; `readEvents`/projection fold/rebuild all filter by tenant first; cross-tenant same id/idem_key never overwrite; sync = event replication, not state translation. Historical v1/JSON can only be safely folded into the default `local`; non-local events already miswritten as `local` have no reliable tenant back-inference and must not be guess-fixed by migration scripts.
- **Booking saga vocabulary layer** (ADR-17, `ts/src/booking-saga.ts`): the state alphabet matches the pending_writes CHECK verbatim.
- **WriteGate mechanism layer** (write_gate.v1, `ts/src/write-gate.ts`, issue [#231](https://github.com/Danceiny/gotry/issues/231), 2026-09-11): persisted trusted approvals + atomic outbox landed as an **offline mechanism/contract layer — non-runtime activation**: no real supplier/transaction call exists, and the product runtime does not instantiate `WriteGate` before M5 Entry (tables are created lazily by `ensureWriteGate`; the ledger SCHEMA, tool surface, and default paths are untouched). Shape: `PreparedChallenge` (nonce/fingerprint/disclosure frozen server-side before presentation; only digests enter the ledger; the raw nonce goes only to the trusted host presentation channel), `ApprovalReceipt`/`ApprovalClaim` (issued only through the trusted host confirmation callback; one-time consumption via a conditional `consumed_at IS NULL` UPDATE), `WriteEffectIntent` durable outbox (transaction 1 = claim consumption + pending_writes pending→confirmed + outbox append in one SQLite transaction; transaction 2 = atomic claim queued→dispatching with immutable `attempt_id`/`fencing_token` persisted before any external call; the model can only request presentation — a model-initiated confirmation is rejected with zero outbox). Storage-level non-bypassable constraints (D-22 anchor, outbox side): composite FK `(tenant_id, receipt_id) → approval_claims`, non-empty-receipt CHECK, claim-field CHECKs on dispatching/dispatched, forward-only `dispatch_status` trigger (dispatching/dispatched/rejected can never return to queued), immutable `attempt_id` trigger. L4 revocation blocks issuance/consumption/dispatch; pre-dispatch revalidation failures settle `rejected` (zero supplier writes, never re-queued). Falsification suite: run-all §61 (crash injection at three transaction-1 points, two-process claim race, replay/expiry/cross-tenant negatives, raw-SQL physical red lines).
- **Async terminal contract**: `gotry_async_terminal.v1` maps 4/4 to `succeeded`/ledger `settled`/exit 0, non-4/4 to `failed`/ledger `failed`/exit 2; terminal re-invocation returns the same structured result and exit code with zero recomputation.
- **Booking Copilot lifecycle projection** (single `booking.surface` contract; 2026-09-05 #133 convergence: the former v2 form promoted to canonical, v1 retired, the dual-protocol era ended): `planning → submitted → working → waiting_receipt → input_required → terminal outcome`; the public types keep the seven phase literals (terminal states split `terminal`/`error`). The availability reducer additionally has `need_offers → waiting_offers → need_check → waiting_check → terminal` typed sub-states, which do not change the outer six-state projection. Identity binding and approval details in §8.23.

### 1.5 Memory domain and time awareness

- **Six memory layers** (design in `design/memory-design.md`): motivation brief read back into persona, utility sidecar (attribution only on owner confirmation), wish pool 0..1 recall (`gotry_wish_pool_list`), travel timeline (`gotry_trip_log`), companion profiles (`gotry_companion_save`), time-window decay (downgrade-only, never delete / floor 0.1 / zero motivation decay). Metrics and reach: `scripts/memory-metrics.ts` read-only projection + `scripts/nudge-digest.ts` proactive revisit (`GOTRY_NUDGE_ENABLED=false` globally disables). M4 planning lifecycle observation additionally has an explicit opt-in CLI `ts/scripts/memory-lifecycle.ts` + pure logic `ts/src/memory-lifecycle.ts`: writes only to an isolated `stateRoot`, consent/HMAC required, exports candidate/synthetic scorer input, never touches real sessions.
- **Persistent home city / default origin (Issue #338)**: the write patch accepts `homeCity` and optional `homeCityEvidence` (a single non-empty evidence may be omitted; multiple require explicit exact binding); the projection stores `homeCityPreference { value, evidence, updated_at }`. `{{motivation_brief}}` reads back only a complete typed preference as a soft default; the current turn's explicit origin wins; on typed missing/malformed or evidence not in pool it only asks for clarification. By default it does not hard-filter or change deterministic candidates/recommendation; `resolveDefaultOrigin` is only a pure precedence contract.
- **Time awareness**: deterministic anchor layer `ts/src/time-anchor.ts` + slot extraction `travel-slots.ts` + slot→date resolution `slot-spec.ts`. **Arithmetic lives in code; the LLM reads cards and never computes.** Future planning with a named year — a four-digit year, or a relative year (今年/明年/后年/年内) resolved against this round's anchor — is filtered at the solve entry in `loop.ts`: current year = reference date to year-end, future years = the whole year; expired years are explicitly rejected; historical/backtest intent bypasses; the reference date is not persisted into `TripState`.

### 1.6 Engineering invariants

- Tool execute uniformly goes through `guardToolExecute` exception isolation + flat observation envelope (ADR-13).
- **Turn budget = routing + wall-clock dual exits** (ADR-24 v2): every turn ends in one of three states — "answered in person / handed off to background with a promise / converged answer" — stream death is not allowed. A deterministic classifier (`ts/src/turn-policy.ts`, zero LLM) splits quick/sync/deep; past the hard threshold, tool schemas are synchronously suppressed (`ts/src/turn-deadline.ts`); the deep exit files a `gotry_turn_handoff.v1` work order with ETA announced; the collection loop is fulfilled by `scripts/turn-handoff-collect.ts`. Design and verification details in §8.24.
- External dependencies go through **effect description + interpreter** (ADR-18): the tool layer only produces pure-data effect values `{effect, params}`; channel access/backoff-retry/circuit breaker/compile-time mocks converge into `ts/capabilities/effect.ts` and `resilience.ts`.
- **HotelByte embedded Booking Copilot is a read-only collaboration surface, not an M5 write-gate unsealing**: single `booking.surface` contract (2026-09-05 #133 convergence, v1 retired); production standalone defaults to `bff-bound-turn-only`; the browser `user.turn.ingress` opens only under full authenticated principal/scope + BFF trusted binding; must blockers can only be released by a one-time approval bound to task/context/receipt/presentation key. Off-page automatic write/payment already has an M5 WriteGate proposal (`design/write-gate-production-design.md`), but zero transaction implementation before M4 Exit + supply-chain agreements are met; no product acceptance may be claimed before the four-surface real-inventory acceptance passes (D-29). Full text in §8.23.
- **Booking dispatch diagnostics (#329)**: synchronous HTTP 409 turn-dispatch rejections only record the existing typed `code` and exact closed `reason`; suffixed or unknown errors uniformly become `UNCLASSIFIED`. Proof is offline HTTP/stderr byte tests in an isolated subprocess; it does not constitute provider reliability, HotelByte UAT, or M3/M4/M5/M6 admission.
- The py tree keeps only the `gotry_feasibility` oracle; the product runtime has zero Python dependency (D-7 discharged).
- **External benchmark bridge is default-off throughout** (Phase 1 seam): owner-local config, fixed argv/allowlist, recursive no-oracle key rejection, cold-start + headless one-shot, native definition-only agent, startup composition isolation, structured terminal diagnostics — any contract drift fails closed. The single flat `action=tools|call|errors` protocol generates each tool's exact call schema from the descriptor at the same source and reuses the same validator before spawn; adapter output only accepts the exact result/domain/failure envelope; stale terminal states must not mask newer bridge facts. Per-round frozen treatment facts in §9; full contract in `evaluation/benchmark-environment-bridge.md`.
- **Z3 lifecycle boundary (#227)**: `ts/src/z3-shared.ts` is the only runtime entry: cold-init Promise cached first (concurrency creates only one Context), session-level mutex `withZ3`, low-level native cleanup local queue (only wrapping the current `z3-solver@5.2.0`'s `dec_ref`/`*_dec_ref` and async native calls; it does not modify the global `FinalizationRegistry` and does not free still-reachable objects early). If GC/explicit-release cleanup collides with an active native check, it is deferred until actual native idle and then drained; fatal WASM/heap errors poison first, and subsequent solves fail closed. Regression anchors: run-all §30/§30b/§30c.
- **Node build support surface (#265)**: the floor stays `>=22.15.0`; dist is transpiled to ESM only by TypeScript 5.9.3 exactly pinned in root `devDependencies`, with no reliance on global packages, `ts/node_modules`, hoisting, or Python; the published runtime carries no TypeScript. CI runs typecheck + full-stack regression on Node 22/24, and exact-file-set/ESM/dynamic-import focused proofs on Node 22/24/26. This item belongs only to the M4 open-source/release-quality line; it does not count toward #20's real repeat-cohort, nor satisfy #136 supply agreement/internal authorization or #137 P6 approval and real pilot; the three gates stay open.

### 1.7 Milestone criteria (Issue #19)

M3's engineering and distribution surfaces are ready, **but real seed-user evidence has not converged, and M3 Exit remains open**. M4 advances in parallel under founder authorization; the Issue #20/#223/#238 scorer has landed in main and been hardened, and the #228/#248 explicit opt-in planning lifecycle collector is also in main, but the collector only produces isolated candidate/synthetic exports and must not replace a real `observed_private` N≥5 repeat cohort + source-review attestation; issue #225 splits M4→M6 into a living task graph and records that the ledger/state-cli/Z3/map base fixes are already in main (#229/#237/#243/#244/#245). The first M5 supply chain selected `hotelbyte-cli` (read-only investigation and contract preparation only; it does not mean a supply agreement is signed). **These plans/designs/candidate exports do not constitute M3/M4/M5/M6 Exit proof**. Before admission, only #136/#137-authorized design, read-only investigation, fixtures, and failing-before may advance; transaction runtime, supplier writes, and the real B2B path start only after their respective Entry gates are met.

Evidence surface status: `ts/scripts/product-metrics.ts` (M3 cohort) and `ts/scripts/memory-value-report.ts` (M4 value) freeze the sample window/inclusion-exclusion/denominator/attribution and thresholds; inputs accept only HMAC-SHA256 pseudonymous keys and unknown fields fail closed; synthetic fixtures **never produce a business pass**. `ts/scripts/memory-lifecycle.ts` only exports first-visit/revisit lifecycle events under explicit consent/stateRoot/HMAC as #223 scorer input, keeping candidate/synthetic source_review, never manufacturing manual attestation. M4 observed-private additionally requires the `memory_value_source_review.v1` manual source-review attestation contract, bound to this scoring payload via `reviewed_summary_digest_sha256`; when absent or digest-mismatched it can only serve as candidate, and `evidence_kind` or HMAC string shape must not be treated as provenance. Real evidence only enters the git-ignored `ts/gotry-state/evidence/`.

Evaluation boundary: Phase 0 has only contracts/registry/validators and a deterministic cadence policy (no scheduling, no spend, no scoring, no uplift claims); Phase 1 bridge per-round frozen treatments are all diagnostic-only with official scores null (see §9 and D-28). Full contract in `evaluation/evaluation-foundation.md`.

## 2. Overall architecture: five layers and current state

```
L1 interaction: conversation is the interface (gates presented as in-message multiple choice; standalone UI is post-Stage-1)
L2 orchestration: conversation loop ts/src/loop.ts — LlmPort (mock✅/real✅ provider-neutral) + deterministic interview + solve mount
    └ dsh plugin gotry-tools: ✅ end-to-end on the real dsh headless runtime (empirically proven since 68ea364 rc.1; previously used vendored `0.1.2-alpha.1` on 2026-08-29; from Round 5 source/package share root-pinned `0.1.2-alpha.3`; 2026-09-09 #268 migrated to the 230-package exact `0.1.5-alpha.1`; 2026-09-11 upgraded to the 232-package exact `0.1.5-rc.1`) — the model actively calls gotry_feasibility_check via pi-ai (MiniMax-M2) and cites engine numbers; composition in cordis.gotry-patch.yml (generated at runtime by bin/gotry-inner.js; the old copy under ts/ retired 2026-08-28)
L3 domain: unified itinerary model ts|py unified.* — Segment/Option/anchors/work window/timezone
    └ feasibility engine: Z3 selection + named constraints + unsat-core attribution + Optimize optimum
L4 data: static pack data/*.json (real schedules + estimated prices, evidence-annotated) + golden cases
L5 governance: loopx (objective/gate/evidence/quota; spend only after verification)
```

| Layer | Responsibility | Contract | Invariants (no evolution may break) |
|---|---|---|---|
| L1 | Presentation and capture | transparent card schema (D1 §6) | why/cost must reach the user; gates can only be multiple choice |
| L2 | Understanding and orchestration | `ts/src/contracts.ts` (TripState + five tools) | LLM does no arithmetic verdicts; writes must pass WriteGate |
| L3 | Verdict and accounting | unified itinerary model (§4) + TrueCost | arithmetic and solving are layered; arithmetic is pure functions, independently testable |
| L4 | Data and capability | CLI/JSON bridge (hotelbyte-cli style) | evidence-chain annotation ([live API]/[shared experience]/[estimate]); estimates must be explicitly marked |
| L5 | Governance | loopx state | spend only after verification; blockers recorded, not spun on |

Data flow (Erhai golden case, one call end to end):

```
materials (photos + one sentence) → motivation interview (driven by deterministic missing fields) → JourneySpec extraction
  → solve_unified (Z3 picks departures; anchors/work window/budget as named constraints)
  → {verdicts, exclusions (with reasons), red_flags, optimal budget} → render (cards + gates)
  → infeasible candidates → wish pool (recall conditions: days/budget/season)
```

## 3. Code map (what each module is)

| File | Role | Status |
|---|---|---|
| `ts/src/contracts.ts` | top-level data and tool contracts (TripState / five-tool IO / wire schema) | draft, awaiting founder walkthrough |
| `ts/src/loop.ts` | conversation loop: runTurn/interviewNext/async deep planning (request/collect + no-disappointment four) | ✅ replay-verified |
| `ts/src/mock-llm.ts` | scripted LLM (ADR-8): deterministic replay of the intelligence side of real conversations | ✅ (kept as regression fixture after S4) |
| `ts/src/unified.ts` | **unified itinerary model TS edition (only solve entry)**: Segment/Option/timezone/work window + Z3 solving (flight chains) + enumeration solving (candidate form) | ✅ 4/4 + candidate reconciliation |
| `ts/src/model.ts` | door-to-door full-cost arithmetic (pure functions, single-candidate form) | ✅ |
| `ts/src/engine.ts` `journey.ts` | the old two solve surfaces (pure oracles, golden comparison) | **deprecated** |
| `ts/src/index.ts` `bridge.ts` | dsh plugin (pure TS unified solve + hbcli bridge + process guardrails, latency metering) | ✅ smoke |
| `ts/src/turn-policy.ts` `turn-deadline.ts` | Agent per-turn "routing + wall-clock dual exits": deterministic classification (quick/sync/deep, zero LLM) → TurnPolicy; schema synchronously suppressed past the hard threshold; converge=EXHAUSTED / handoff=file `gotry_turn_handoff.v1` work order + ETA announcement; benchmark pins a fixed policy | ✅ run-all §45 |
| `py/gotry_feasibility/unified.py` | Python oracle (**historical comparison only after v0.0.1-rc.2**, no longer referenced by the product runtime) | retained |
| `py/gotry_demo/` | **deleted 2026-08-22** (D-7 tail debt: the demo plan generator once called the deprecated journey.solve_journey; artifact docs/milestones/demo-plan-2026-07-17.md remains in git history) | — |
| `ts/scripts/replay.ts` `replay-async.ts` | **acceptance fixtures**: real conversation replay (13 turns → 3 turns) and async form | ✅ |
| `ts/scripts/{engine,journey,unified,diff}-tests.ts` | suites (8/5/4 assertions + TS-vs-TS same-spec stability) | ✅ (occasional diff-test ordering is a known issue; no Python dependency after v0.0.1-rc.2) |
| `ts/src/time-anchor.ts` | **time anchor layer** (ADR-12, pure functions): anchor-card rendering (today / relative week / month segments / quarter / festivals) + absolute month-day parsing; the single source of "today" for persona and the extraction chain | ✅ time-eval §1 |
| `ts/src/travel-slots.ts` | **slot extraction layer** (travel_slot_extraction.v1): schema + extraction prompt + expiry validation + language detection + scorer; verbatim preserved, verdicts belong to code | ✅ time-eval §2-4 |
| `ts/src/slot-spec.ts` | **slot→date resolution layer** (D-10 slice A): anchor-card vocabulary + absolute expressions + "+N" suffix → YYYY-MM-DD; out-of-vocabulary unresolved kept verbatim (ADR-12 boundary: no open-ended parsing); spec date-consistency gate | ✅ time-eval §5 |
| `ts/src/tool-packet.ts` | **tool observation envelope** (RFC S1/ADR-13): GotryObservation flat-sealed shape + ToolFailure + interpretArgs, the single entry normalizing the three parameter forms | ✅ smoke §9 |
| `ts/src/memory-utility.ts` | **memory utility sidecar** (RFC S2/ADR-14): recalled/applied/verified_outcome events + idempotent append + read-only projection; attribution only on owner confirmation | ✅ smoke §10 |
| `ts/src/memory-lifecycle.ts` `ts/scripts/memory-lifecycle.ts` | **M4 planning lifecycle collector** (#228): writes only with explicit stateRoot/consent/HMAC; dataset key verifier + source/wait freeze; first-return flow/wait/reflux/preference HMAC-pseudonymized; JSONL+manifest write-all/atomic publish/path isolation; exports to #223 scorer candidate/synthetic, manufactures no manual attestation | ✅ run-all §55 |
| `ts/src/memory-decay.ts` | **time-window decay primitives** (memory-design P3): 30/90/180/365d tiered factors (floor 0.1) + kind weights + freshness confidence; zero decay on the motivation layer is a construction guarantee | ✅ run-all §23 |
| `ts/src/companions.ts` | **companion profiles** (memory-design P2): upsert merge + negative-list guard (zero ID/phone ingestion); constraints only enter ranking | ✅ run-all §21 |
| `ts/src/travel-timeline.ts` | **travel timeline** (memory-design P1): trips.jsonl append-only + idempotent / overlap-conflict halts + verified↔timeline cross-consistency | ✅ run-all §20 |
| `ts/src/wish-pool.ts` | **wish pool matching pure functions**: condition scoring + 0..1 selection (muted excluded / deterministic tie-break); shared by wish_pool_list and nudge | ✅ run-all §21 |
| `ts/capabilities/channel-registry.ts` `ts/capabilities/channel-health.ts` | **channel registry + channel health surface** (design/tool-orchestration-design.md, issue #106/#107/#108/D-7/D-8/D-9): single data source of channel×intent×quota class×evidence level; persona routing card and `routing` advice generated from the same source; session transient state (down/cooldown, cleared on hit) + `channel-health.jsonl` persistent event surface; ordering = lexicographic (availability, evidence level, efficiency); advice is not dispatch | ✅ run-all §50 |
| `ts/capabilities/flyai.ts` | **FlyAI official channel**: pipeline layer of Fliggy's 8 read-only tools (search-flight/train wired first), evidence chain `[live API: flyai@ts]` | ✅ run-all §24-F |
| `ts/capabilities/session-search.ts` + `session/` + `extension/` | **session retrieval surface** (RFC P1-P3.5): transport = extension bridge PRIMARY (`extension/` MV3 + `extension-bridge.ts` loopback bridge, zero new dependencies; **capability routing**: a site's jobs are dispatched only to pollers declaring that site) / cdp explicit fallback (ReadGuard physically intercepts write requests + audit, fail-closed) / persistent for tests; Ctrip flight adapter (batchSearch sniffing) / dida supplier-portal adapter (SearchRealTime envelope flattening, 2026-09-09) / action-cache self-healing layer (variable keys + fingerprint passive invalidation + miss write-back); pacing gate; `[session:*]` evidence chain | ✅ run-all §25/§38 |
| `ts/capabilities/session/extension-distribution.ts` + `ts/scripts/extension-distribution-cli.ts` | **extension distribution channel** (ADR-21 distribution A): GitHub Releases download chain (stable asset names / dist-manifest fail-closed parsing / SHA256 / platform tar extraction / key pinning / version comparison / atomic swap), explicit degradation to bundled on failure; CLI single-line JSON for bootstrap spawn | ✅ run-all §43 |
| `ts/capabilities/artifacts.ts` | **artifact surface** (issue #25 minimal slice): artifact discovery (ledger workflow_runs authoritative + ledgerless fallback async directory view + dsh working-directory top-level md) + line-window read (dsh read card); read-only, path/extension whitelist | ✅ smoke §13 |
| `ts/capabilities/effect.ts` `ts/capabilities/resilience.ts` | **effect interpreter + resilience primitives** (effect_interpreter.v1, issue #16 adopted / ADR-18): effect-value registry (channel handlers + policy table) + production/mock interpreters (channel observations passed through verbatim + trace cross-cutting evidence) + exponential backoff (withRetry) / circuit-breaker tri-state (CircuitBreaker); all 23 tools' external-dependency surface converged (issue #115) | ✅ run-all §37 |
| `ts/src/state-ledger.ts` | **transactional state ledger** (ADR-15): SQLite single file as sole authority (events append-only + semantic idempotency keys / projection tables fold-rebuildable / workflow_steps durable work orders / pending_writes saga); `gotry_async_terminal.v1` freezes the 4/4 / non-4/4 terminal states, exit codes, and zero-recompute re-invocation; gatekeeping pure functions reused as write-path and fold processors; read path falls back to legacy files; first write auto one-shot migration + snapshot | ✅ run-all §28 |
| `ts/src/booking-saga.ts` `ts/scripts/booking-saga-tests.ts` | **booking saga state-machine vocabulary layer** (booking_saga_fsm.v1, issue #17 adopted / ADR-17): state alphabet + four edges as full-function edge table + structured rejection closed set + audit-chain validation; §36 physically reconciled cell-by-cell against the ledger saga base | ✅ run-all §36 (pure functions, zero write-path wiring) |
| `ts/src/booking-surface/recovery-chain.ts` `ts/scripts/booking-recovery-chain-tests.ts` | **unavailable/changed recovery-chain contract** (issue #142 non-gated slice): pure-function disposition chain over supplier changed/unavailable verdicts — detection classifies receipts into the existing `ActionReceipt.status` closed set (confirmed/changed/unavailable/inconclusive/unrelated, no new channels; incoherent receipts fail closed with the policy's own `availability_receipt_incoherent`/`availability_observation_required` vocabulary); controlled recovery records requery/recheck-version/alternative-candidate steps inside the planner three-call budget semantics (a 4th counted turn is refused — degrade explicitly instead), with the availability-policy per-hotel budgets frozen-referenced (`RECOVERY_REUSED_BUDGETS`); user-communication red lines: resolving onto a different hotel/offer without prior explicit disclosure is structurally refused (`recovery_silent_swap_forbidden`), a version shift requires an explicit old→new explanation, and exhausted recovery may only degrade with a mandatory non-empty disclosure; audit events (`booking.copilot.recovery.*` namespace) carry receipt digests and validate saga-style (detected → attempt* → disclosed? → resolved/degraded absorbing); detection/legality stay owned by the availability reducer (ADR-23) — this layer is its user-communication and audit projection, non-runtime-activated | ✅ run-all §65 (offline fixtures; zero real supplier calls) |
| `ts/capabilities/hotelbyte-transaction.ts` `ts/scripts/hotelbyte-reconcile-tests.ts` `ts/scripts/hotelbyte-spawn-e2e-tests.ts` | **HotelByte transaction bridge + unknown query-orders reconciliation contract** (issue #232, M5 pre-entry): pure-function reconciliation state machine per `design/write-gate-production-design.md` §7 — book-execution classification (CLI exit0 is not success proof: timeout/kill/non-JSON/unbound success/partial confirmation classify unknown; explicit business failure classifies supplier_failed); fail-closed seed bindings (attemptId/customerReferenceNo/idem key/fingerprint required; fact_id/approval-receipt-digest/outbox-ref traceability on every record, probeDigest on every event); miss stays unknown inside the 180s+600s recovery window with rebooking structurally forbidden; window expiry alone is never no-order proof and only escalates to manual; authoritative terminal negative evidence bound to the attempt is the only new-intent gate; multi-order/reference-mismatch/post-terminal counter-evidence surface explicit conflict, never silent success or silent drop; the pure contract layer itself has no spawn/network/credentials, and the spawn-level full-chain E2E (§5) executes only a local fake CLI fixture — non-runtime-activation | ✅ run-all §60 (offline fixtures; zero real supplier calls); spawn-level E2E landed: run-all §66 (PR pending link) |
| `ts/capabilities/sponsor-plugin.ts` `ts/scripts/sponsor-reuse-tests.ts` | **sponsor plugin + same-kernel end-to-end reuse proof face** (issue #235, M6 pre-entry): traveler principal/sponsor/BFF principal three-subject separation (missing or conflated ids fail closed at construction); authorization route closed set + mandatory session/order binding (no credential field anywhere on the chain — copying Buyer/credentials is structurally excluded); runtime activation default-off (`SPONSOR_RUNTIME_ACTIVATION_DEFAULT=false`, explicit opt-in only, real wiring behind #137 M6 Entry); same-kernel reuse = the module only imports kernel functions (`booking_saga_fsm.v1` state machine + wish-pool condition scoring, zero copy/no re-declaration, kernel drift turns the suite red), and one runner drives both B2C and B2B forms with cell-identical saga edge sequences; red lines structurally encoded: kernel full-condition-hit enforced before ranking (commission cannot redeem unmet traveler conditions; tie-break only by sponsor declared order with mandatory bias disclosure), disclosure digest enters the confirmation fingerprint, empty receipt rejected in both forms, cancel/refund walks the same kernel edges; isolation: no `ts/src/**` file references the sponsor module (C-side default path untouched) and the B2C trace passes the leak checker with zero findings | ✅ run-all §64 (offline fixtures; non-runtime-activation, zero real calls) |
| `ts/src/booking-surface/cancel-refund-commission.ts` `ts/scripts/issue-233-cancel-refund-commission-tests.ts` | **cancel/refund independent outcomes + commission disclosure contract layer** (issue #233, M5-4 pre-entry, pure functions): cancellation and refund are two outcome objects with disjoint status closed sets — no merged success state, asymmetric terminals (cancel confirmed + refund failed/pending/unknown) are first-class; `supplierReferenceNo` constructible only from supplier return values; `refunded` binds supplier/wallet authoritative evidence; `cancel.serviceFee` can never become the customer refund amount and is disclosed separately; commission/sponsorship disclosure (who pays / to whom / amount basis; unknown must not default to none; explicit none requires a recorded contract rule) with canonical-JSON SHA-256 digest bound into the request-fingerprint field `commission_disclosure` — a disclosure change invalidates the old receipt; a mechanical user-copy token gate keeps ledger tokens and wording consistent (480-combo matrix) | ✅ run-all §62 (contract only — zero supplier/payment call surface, not wired into any runtime path) |
| `ts/src/bookable-facts.ts` | **bookable fact model** (gotry_bookable_fact.v1, issue #46/ADR-19, pure functions): fact schema (route / exact local date / flight number / marketing+operating carrier / airports / price / source / query_id / confidence tier / bookability four states) + flyai/session result converters (hit = positive fact / miss = negative fact / error not recorded) + IATA normalization + judgment primitives (claim expressibility fail-closed / same-day connection hard constraint / night-count·O&D·budget invariants / detour detection) + render primitives (codeshare dual-carrier / DMK landing note / unbookable wording / connections only protected_connection) | ✅ run-all §39 |
| `ts/src/artifact-gate.ts` `ts/capabilities/fact-log.ts` | **artifact fact gate + fact sidecar** (issue #46/ADR-19): markdown bookable-claim reverse extraction (flight number / carrier nonstop / Chinese carrier names / airport mapping / policy "as-of" context / ✓ / connection assertions, subsection context inheritance) → item-by-item back-trace to the registry (not_in_source / route_unqueried / time contradiction / price contradiction / connection violation, etc.); the 21st tool `gotry_fact_gate`; when blocked, no "verified" claim; fact-log appends to the `<stateRoot>/gotry-state/bookable-facts.jsonl` sidecar (never blocks the retrieval main path) | ✅ run-all §39 + smoke §16 |
| `data/airline-airports.json` `ts/data/golden-trip-2027-facts.json` | airline→airport mapping + city→IATA vocabulary (gotry_airline_airports.v1, as_of snapshot + review_by): FD=DMK/VZ=BKK conflict detection surface, gate fails closed on missing load; golden fixture = the 2027 trip fact set locked to issue #46 audit values (11 queries) + good/bad itinerary invariant pair | ✅ run-all §39 |
| `ts/scripts/state-cli.ts` | **ledger operation surface** (ADR-15): migrate/log/stats/rebuild/rewind/forget/pw-* go through the centralized parser then pass to StateLedger tenant-scoped; unknown/duplicate/missing-value/illegal numerics (including `.5`/`+.5`) fail closed. export/tick/whatif are explicitly local-only: they would respectively write a shared legacy filename, invoke local async settlement, and generate a whole-database admin snapshot (not a tenant export); non-local is rejected before mkdir/openDb/solve/write | ✅ run-all §29; #241/#243 landed in main and closed |
| `ts/scripts/product-metrics.ts` `ts/data/product-metrics-fixture.json` + `ts/scripts/nightly-evidence.ts` `ts/data/m3-nightly-prompts.json` `ts/data/llm-price-table.json` | **M3 cohort evidence scoring surface + nightly evidence producer (Issue #22)**: threshold-frozen manifest + de-identified cohort/nightly schema + finalization-rate/NPS/POI-hallucination-rate scorers; fixtures and real evidence are segregated, unknown fields fail closed; the nightly producer freezes the prompt set and price table (peak conservative upper bound, unknown models fail closed), no-credentials waiting/backoff/no-spend, exit 3 on over-budget, records must pass the consumer's parseNightlyRun before being written | ✅ run-all §33/§35; real cohort still to be collected, nightly real-run records await a credentialed environment |
| `ts/scripts/time-eval-tests.ts` `data/time-slot-eval.json` | time-awareness evaluation (25 questions): deterministic part in CI, `--real` real-model inspection (read-only report) | ✅ real model 25/25 |
| `data/golden_erhai.json` `flights_2026.json` `hotels_2026.json` `golden_trip_2026.json` `行程细化计划.docx` | golden case / schedules / accommodation / full task / Kimi conversation originals | — |

## 4. Unified itinerary model (domain core, only solve entry)

```
JourneySpec = { segments, budget?, workWindow?, default wake-up line, … }
Segment     = { id, role: choice|fixed, anchors{arrive_by/depart_after/…}, options[] }
Option      = { id, move(services×transfers×buffer×red-eye×tz), stay?(nights/price/work_window), score, min_days }
```

- Two forms, one model: candidate choice (Erhai = 1 segment, 3 destination Options) and segment chain (demo = 5 segments); the old engine/journey are its degenerate cases on single-segment/fixed-chain, now deprecated.
- **Solve layering**: arithmetic pure functions (`evaluate_*`: wake-up/arrival/energy/effective duration/money) strictly separated from Z3 selection (named constraints, unsat-core attribution, Optimize optimum).
- **Timezone semantics (D-5 discharged)**: real flight = (arrival − departure) − time difference; door-to-door = pre-flight + real flight + transfer (EK329 full chain 11h20m, flight 7h35m, minute-for-minute identical to the official site).
- **Work window (M-1 landed)**: home timezone → origin-local conversion; Options departing inside the workday window are deterministically excluded before solving, with reasons recorded — gate q3 is deterministically answered by one rule (all Friday-evening departures excluded, only Saturday morning left, matching the real choice).
- **Red-eye sleep model**: energy = 30+8×(flight−1h), clamp[30,75]; EK329 lands at 75% (awaiting reconciliation, Q10 calibration).

## 5. Conversation loop (L2)

`runTurn(state, msg, llm, solve)`: extract facts (calendar asserted once, conflicts pointed out explicitly) → incremental interview (driven by missing fields; workWindow/bookedResources are solve prerequisites; budgetTier demoted to a gate, non-blocking) → when constraints are complete, extractSpec→solve→render (plan + exclusion reasons + red flags + gate multiple-choice). Complex trips switch to **async deep planning** ("come back in an hour"; revisits deliver with the no-disappointment four self-check). Detailed design in `design/stage1-top-down-design.md`.

**Replay acceptance** (`ts/scripts/replay.ts`): Kimi's 13-turn failure = GoTry's 3 turns; zero calendar back-and-forth; the work window and booked hotel are asked out in the first turn; the final turn is already a verified plan.

## 6. Data and runtime

> **Single authority for data sources = `data-sources.md`** (established 2026-08-22): domain matrix × four-layer architecture (static pack / free realtime / hbcli bridge / OSM ecosystem) × Google Place chain (hbcli→search OpenAPI→geography) × evidence-chain contract × TREK reference adoption. This section keeps only the runtime summary.

- Runtime: two empirically proven paths — ① TS in-process (self-built loop, ~6ms/solve); ② real dsh headless + cordis composition (pi-ai→MiniMax, `cordis.gotry-patch.yml`, 68ea364). Since v0.0.1-rc.2 the Python CLI bridge is offline, pure TS. Environment trio `LLM_API_KEY/LLM_BASE_URL/LLM_MODEL` (backward compatible with old DEEPSEEK_*).
- Reuse landed: dsh (import, rc aligned) / loopx (import, 0.5.1 running) / Z3 (import, dual bindings) / hotelbyte-cli (import+extend, place chain in data-sources.md §4) / T system·ai-agent-book·TREK (reference, zero code — TREK data-surface pattern adoption table in data-sources.md §5). Kernel manifest frozen (issue #234, PR link pending): `ts/data/kernel-manifest.json` pins the kernel module surface (engine `unified.ts`/`model.ts`, ledger `state-ledger.ts`, gate `bookable-facts.ts`/`artifact-gate.ts`; SHA256 + basis per entry, bound to the freeze baseSHA) and the mechanical gate (run-all §63) proves zero hash drift + real runtime import-trace loading + same engine/ledger/gate functional-path coverage + evidence-snapshot hash binding.

## 7. Testing and verification strategy

**Three evaluation layers (ADR-11)**:
- **Regression layer (anti-regression)**: TS-vs-TS dual-path stability (same spec, different module instances) + golden assertions (Erhai 8+5, Phuket chain 4, unified model 20/20) + **replay fixture** (mock replay is behavior-level regression; the Kimi conversation is the failure baseline). **Since v0.0.1-rc.2:** no more reliance on the Python oracle diff; run-all-tests' 9 suites pass in one go with no Python runtime. Full-stack entry: `scripts/run-all-tests.sh`.
- **Quality layer (anti-drift)**: evaluation set + metrics panel — POI hallucination rate, finalization rate, no-disappointment four, NPS; goes live at M3 (see `tech-strategy.md` §4); before that, replay terminal-state assertions are the backstop.
- **Inspection layer (anti "mock green while real intelligence rots")**: the nightly form of real-LLM replay (`replay-real.ts`) has landed — `nightly-evidence.ts` (frozen prompt set + frozen price table, budget gate `GOTRY_NIGHTLY_BUDGET_USD`, no-credential waiting/backoff/no-spend zero writes, run-all §35; real runs that cost money stay out of CI, executed manually by heartbeat/founder). Output `gotry_m3_nightly_run_v1` records append to the **private evidence ledger** `ts/gotry-state/evidence/m3/cohort.jsonl` (git-ignored); `cost_usd` comes only from dsh-llm's usage accumulator × the frozen price table. ADR-10 was born exactly from mock-green-while-real-LLM-rotted; the lesson is institutionalized.

## 8. ADR

**Lifecycle**: proposal → adopted → (discharged | superseded | retired); "never re-review" is an explicit terminal class (ADR-7/9/10 rest on invariant-level judgments). **Three birth channels**: ① born of failure — a real run exposes a problem mock/derivation cannot see, ADR filed the same day (the ADR-10 pattern); ② born of reconciliation — `milestones/demo-reconciliation.md` §3: model gaps → file an ADR and evaluate whether it enters the engine; ③ born of milestone review — the M-exit pass walks every retirement/re-review condition of the whole table (§11), and whatever triggers is filed on the spot. **Anchor**: every ADR must have a code/test execution anchor, or be explicitly marked "process-level" — an ADR without an anchor silently rots away as the system evolves, unnoticed.

| # | Decision | Alternatives and trade-offs | Retirement/re-review condition | Anchor |
|---|---|---|---|---|
| 1 | Z3 as the verdict layer | rule engine / OR-Tools / pure LLM (4.4%) | evaluate OR-Tools if solve >500ms or variables >10³; unsat core non-negotiable | `unified.ts`/`unified.py` solve layer; dual-side suites |
| 2 | Dual implementation: TS production + Python oracle | single implementation (no reconciliation) | **2026-08-22 v0.0.1-rc.2:** Python oracle path offline (diff-test switched to TS-vs-TS; run-all-tests no longer needs a Python runtime); py/ retained as historical comparison (not deleted), **no longer referenced by the product runtime** — prerequisite for one-command npm distribution |
| 3 | Bridge convergence: in-process first | all-TS / all-Python | **2026-08-22 v0.0.1-rc.2:** Python bridge offline, only the hbcli bridge remains (vs hbcli & hbcli fallback); invariant ≤2 bridges | `ts/capabilities/hbcli.ts`; `bridge.latency.jsonl` |
| 4 | loopx as control plane | self-built state machine | when concepts clash beyond adaptation | process-level (`.loopx/` governance state) |
| 5 | Unified itinerary model | keep dual engines | discharged (engine/journey retirement day = migration completion day, tracked by D-7) | `unified.ts`/`unified.py` |
| 6 | Static pack (demo phase) | wire APIs directly | retired into fixtures at M2 | `data/*.json`; golden cases |
| 7 | Arithmetic/solve layering | mixed | never re-review | `model.ts`/`model.py` pure-function layer (layered test structure) |
| 8 | mock-LLM first | wait for API key (pseudo-blocker) | after S4 the mock stays as regression fixture (fulfilled) | `ts/src/mock-llm.ts`; `ts/scripts/replay.ts` |
| 9 | Deterministic interview (missing-field driven) | LLM improvisation (the Kimi disease) | never re-review | `loop.ts interviewNext`; replay fixture (work window asked out in turn one) |
| 10 | Translation ≠ fabrication: the LLM only produces skeleton and anchors; schedule data always comes from the capability layer (static pack → live API); spec validation gate as backstop | let the LLM produce the full spec directly (measured: MiniMax-M2 cannot make up timetables — it either fabricates or deadlocks) | never re-review | `loop.ts validateSpec`; `dsh-llm.ts SKELETON_SYSTEM`; `replay-real.ts` |
| 11 | Evaluation layering enters the architecture: regression layer (unit/diff/replay) against regression, quality layer (evaluation set + metrics panel) against drift, inspection layer (nightly real-LLM replay with budget gate) against "mock green while real intelligence rots"; M-exit must pass the corresponding layer | replay fixture only (quality drift unfelt) / evaluation tooling added after the fact (metrics not in the architecture = nonexistent) | re-review once after the M3-exit metrics panel goes live | `run-all-tests.sh`; replay trio; `tech-strategy.md` §4 |
| 12 | Time-awareness layering: anchor card (arithmetic in code) + slots preserved verbatim (LLM neither converts nor translates) + expiry/language verdicts in the code layer | see §8.12 | **2026-08-27 review (triggered by D-10 slice A): design holds**; supplementary boundary in §8.12 | `time-anchor.ts`; `travel-slots.ts`; `slot-spec.ts`; `time-eval-tests.ts` |
| 13 | Tool observation envelope (RFC S1, effect-interpreter mapping): 12 tools' success path flat `ok:true` + payload, failure `{ok:false,summary,evidence}` (guard fallback same shape, `ToolFailure` compile-time aligned); single entry `interpretArgs` normalizing the three parameter forms (former unwrapQuery moved to `tool-packet.ts`) | per-tool free-form returns (shape drift, every new tool re-guesses) / nested envelope `{ok,value}` (renderers/callers all forced to unwrap, high intrusion) | re-review when a second real caller (neither dsh nor smoke) needs a different observation shape | `tool-packet.ts`; `incident-log.ts guardToolExecute`; smoke §9 |
| 14 | Memory utility sidecar (RFC S2/S3): three event classes append-only, **attribution only on owner confirmation**; wish stable id + dormancy; recall 0..1/turn | see §8.14 | re-review on multi-user AaaS ledgerization (RFC §6.5) or a second utility consumer | `memory-utility.ts`; `index.ts gotry_wish_pool_list`; smoke §10 |
| 15 | Transactional state foundation (RFC `rfc/transactional-state-rfc.md`, converging the industry's durable-execution five-piece set) | see §8.15 | re-review on multi-user AaaS-ification (RFC §6.5 claim/CAS implementation) or when multi-writer/multi-end replication is needed (cr-sqlite/Litestream, trigger = D-15) | `state-ledger.ts`; run-all §28/§29 |
| 16 | Dual-form architecture freeze (local + Web): **one ledger semantics, two host bindings**; `tenant_id` first-class field; append/read/fold/rebuild all scoped to the current ledger owner; sync = event replication, not state translation | see §8.16 | never re-review (dual-form is the product-form foundation); sync protocol and claim/CAS implementation deferred to triggers; historical local events must not be tenant-back-inferred without evidence | `state-ledger.ts` schema v2; run-all §28 dual-form assertions |
| 17 | Booking saga state-machine naming (issue #17 adopted, 2026-08-29) | see §8.17 | re-review when M5 decides WriteGate (the unsealing delta's schema CHECK / seam vocabulary / L4 automation classes); if a booking flow needing parallel multi-writers appears, re-review the keyed single-writer form | `ts/src/booking-saga.ts`; `docs/design/booking-saga-fsm.md`; run-all §36 |
| 18 | Effect interpreter effect_interpreter.v1 (issue #16 adopted, 2026-08-29) | see §8.18 | re-review the "flat" boundary when a product verdict needing cross-channel price aggregation appears; write effects (booking/payment) entering the registry must follow the booking_saga_fsm.v1 edge table (M5 Entry) | `ts/capabilities/effect.ts` `resilience.ts`; `docs/design/effect-interpreter.md`; run-all §37 |
| 19 | Single source of bookable facts + artifact fact gate (issue #46, 2026-08-30) | see §8.19 | re-review coverage when a second gated artifact class appears (e.g. hotel direct booking); re-review the policy-fact production end after a live policy source lands; the root-fix direction = artifacts generated only by render primitives (structured → markdown one-way), reverse extraction demoted to fallback | `ts/src/bookable-facts.ts` `ts/src/artifact-gate.ts`; `data/airline-airports.json`; run-all §39; smoke §16 |
| 20 | Provider-aware price table v2 + long-term price-drift mechanism (issue #49, 2026-08-30): frozen price table `gotry_llm_price_table_v2` (DeepSeek tiered + MiniMax flat, unknown models fail closed, no price guessing) + four-provider drift monitoring, **never auto-apply prices** (adjustments via PR + human review) | manual price check at release (lags) / auto-apply (rejected: an official 5% drop was once mistaken for our bug) | re-review when monitoring false positives become systemic, or when the price-source form changes | `ts/data/llm-price-table.json`; `ts/scripts/price-drift-watch.ts`; run-all §41/§42 |
| 21 | Extension distribution three channels (issue #21 distribution channel, 2026-08-30; store track listed 2026-09-02) | see §8.21 | ~~re-review wizard steps after store approval~~ (triggered: wizard degraded to offline health-watch waiting; install = the browser's business, rendering = dsh UI's business, §3.3 responsibilities handed back); re-review mirror defaults if GitHub-unreachable regions normalize; re-review the channel abstraction when a second distribution artifact appears | `ts/capabilities/session/extension-distribution.ts`; `scripts/package-extension.mjs`; run-all §43; `docs/ops/extension-webstore-submission.md` |
| 22 | static golden is an **auditable benchmark comparator**, not a live flight source (issue #67) | see §8.22 | re-review as a new provider when an official flight API appears that needs no private credentials, has a clear license, and is stable, or when hbcli publishes a flight contract; static remains only a deterministic regression fixture | `ts/capabilities/session/static-flight-golden.ts`; `ts/data/sf-static-routes.json`; run-all §44 |
| 23 | embedded Booking Copilot security boundary and BFF request identity binding (single booking.surface contract) | see §8.23 | off-page automatic write/payment must enter the M5 WriteGate proposal/ADR follow-up implementation; multi-writer/cross-host triggers ADR-15/16 re-review; ~~retire v1 after all consumers migrate to v2~~ (**triggered 2026-09-05**: #133 converged to a single contract, v1 retired, files promoted to suffix-free canonical names) | `schemas/booking.surface.schema.json`; `ts/src/booking-surface/` (contracts/runtime/server/startup etc.); run-all proof surface |
| 24 | Turn budget = routing + wall-clock dual exits: deterministic classification → converge/handoff; handoff files an independent work order awaiting a loopx tick to collect | see §8.24 | when routing misclassification becomes systemic (user feedback "what should have been answered in person got handed off" observable), first expand the Tier 0 signal vocabulary, then consider Tier 2 (structured state); when handoff work orders back up and need a real collector, start the loopx tick design; if the evaluation-side 60s is too tight, adjust the env pin first | `ts/src/turn-policy.ts`; `ts/src/turn-deadline.ts`; `ts/src/index.ts` wiring; `ts/scripts/turn-policy-tests.ts`; `ts/scripts/agent-planning-turn-deadline-{tests,e2e}.ts`; `scripts/run-all-tests.sh` §45 |
| 25 | Channel health surface and dynamic routing advice (issue #106/#107/#108, D-7/D-8/D-9 adopted 2026-09-03): the tool surface stays flat (ADR-18 judgment untouched), the interpreter does no hidden rerouting; the channel registry single data source generates the persona card/tool descriptions/doctor lines; on retrieval verdict≠hit an ordered `routing` advice is injected into the result (availability > evidence level > efficiency lexicographic, health-state filtered), the contract teaches at the failure site; quota five-class taxonomy (user-session/user-key/anonymous-trial/free-public/static) freezes attribution semantics; calendar not mounted by default (D-9) | interpreter auto-rerouting (rejected: the model thinks it called A but actually went through B, breaking call auditability) / static priority inversion (rejected: every new user pays the extension-install cost first) / prose doctrine only (rejected: prose rots, ordinary models can't read it) | when routing advice misfires systemically, fix registry data first; re-review together with ADR-18 when a cross-channel price-aggregation product verdict appears; the official key pool (product-wide application) awaits re-review at M3 real-cohort scale | `ts/capabilities/channel-registry.ts` `channel-health.ts`; `docs/design/tool-orchestration-design.md`; run-all §50; smoke (flyai needs-setup→routing) |

### ADR expansions (the bodies referenced as "see §8.x" in the table)

#### 8.12 Time-awareness layering
- The time evaluation set entered the repo (`data/time-slot-eval.json`, append-only, semantics unchanged) — the quality layer's first piece landed.
- Alternatives and trade-offs: all-LLM perception — measured anchor absence (the legacy path injects no "today", expiry unjudgeable); full code parsing of Chinese relative dates — open-ended expressions, a maintenance black hole.
- **Boundary added by re-review**: the resolution layer recognizes only the anchor-card vocabulary + absolute expressions + the "+N" suffix; out-of-vocabulary unresolved is preserved verbatim, **no open-ended parsing** (anchor `slot-spec.ts`; time-eval §5).

#### 8.14 Memory utility sidecar
- Three event classes `recalled`/`applied`/`verified_outcome` append-only (`gotry-state/memory-utility.jsonl`).
- **Attribution discipline**: attribution can only be recorded at confirm-outcome, explicitly stated by the user; **the model is not allowed to self-rate "useful"**.
- Wish stable `wish_id` + muted (dormant, not deleted); recall 0..1/turn (`gotry_wish_pool_list` condition scoring; muted never recalled; no hit, no hard push) — the measurement base of the M4 north star "next-departure rate".
- Alternatives and trade-offs: recording "useful" on recall — self-claimed usage ≠ improving outcomes; wish deletion — aspirations should not be rejected.

#### 8.15 Transactional state foundation
Single-file SQLite ledger (better-sqlite3, WAL) = sole authority, converging the industry's durable-execution five-piece set:
1. **events append-only**: semantic idempotency keys physicalized as UNIQUE, `wish_id` semantically derived.
2. **projection tables fold-rebuildable**: gatekeeping pure functions reused as-is, zero semantic-layer rework.
3. **red lines in transactions**: evidence/conditions rejection rolls back.
4. **durable work orders**: `workflow_steps` intent-before-execute, crash recovery exactly-once.
5. **pending_writes saga**: WriteGate L2/L3 foundation (idempotency key/receipt/compensation) + what-if fork (VACUUM INTO).

Old JSON/JSONL degrade to one-way export views (red line 6); one-shot migration (automatic on first write + snapshot `pre-ledger-backup/`).

Alternatives and trade-offs: Postgres/DBOS/Temporal/Restate platforms — a single-user local product needs no server side (the SQLite durable school, "one file is the control plane"); pure file hardening tmp+rename — cannot fix cross-file forks and concurrency; `node:sqlite` — zero-dependency but newer, the D1 runner-up.

#### 8.16 Dual-form architecture freeze
- **One ledger semantics, two host bindings**: local = better-sqlite3 reading the file directly; Web = the same schema on a per-user SQLite file (or Postgres, isomorphic schema).
- **`tenant_id` is first-class from day one**: events/projections/work orders/pending_writes all carry the tenant column, constant `'local'` in the single-user phase, primary keys namespaced against cross-user collisions.
- **Execution boundary (issue #224 fix)**: `insertEvent` must write the current ledger tenant; `readEvents(kind?)`, projection fold, and `rebuildProjections(toSeq?)` must carry the tenant condition; the `wish.updated` fold looks up existing items of the current tenant — cross-tenant same `wish_id` never cross-reads. legacy JSON/JSONL and v1 DBs migrate only into `local`; non-local events already miswritten as `local` by the old bug lack an auditable owner, cannot be guessed back by schema migration, and can only go through a manual data-repair issue/PR with external evidence.
- **Sync = replication of ledger events, not translation of state**: event rows carry `tenant_id` + idempotency keys, dual-end merge naturally idempotent. Writes must go through the ledger; reads must carry tenant context into the invariant tables.
- Alternatives and trade-offs: local and Web each growing their own logic — the multi-user-phase merge could only be rebuilt from scratch; cloud-authoritative + local cache — violates red line 6 local-first; syncing projections instead of events — projections are derived state, merging would fork.

#### 8.17 Booking saga state-machine naming
The booking/payment/refund saga **introduces no orchestration framework (LangGraph etc.)**; the FSM lands as the vocabulary layer of the ledger's `pending_writes` (`ts/src/booking-saga.ts`, `booking_saga_fsm.v1` pure functions): the state alphabet matches the CHECK constraint verbatim, edges fully functionalized.

Alternatives and trade-offs: LangGraph/Temporal-style frameworks — a second runtime, violating the harness baseline and the reuse matrix; states scattered across SQL strings — edge semantics drift with no vocabulary; multi-agent prompt coordination — implicit dependencies.

#### 8.18 Effect interpreter `effect_interpreter.v1`
"Effect description + interpreter" sinks to the L4 channel boundary — the tool/orchestration layer only produces pure-data effect values `{effect, params}`; channel access, backoff-retry, circuit breaker, and compile-time mocks all converge into the interpretation layer.

Alternatives and trade-offs: per-tool free calls into the capability layer — cross-cutting logic duplicated, no backoff/circuit-breaking/mock surface; Python browser-use — violates zero-Python-dependency; interpreter with built-in multi-channel routing order — violates OTA flatness.

#### 8.19 Single source of bookable facts + artifact fact gate
Every bookable fact (flight number/times/airports/price/policy) **is allowed to exist only in the structured fact layer** (`gotry_bookable_fact.v1`); artifacts pass the gate before rendering.

This slice completes the inline hard-price boundary of the shared flight/train primitives: when an exact-date fact's price is CNY, only the `¥NNN`/`CNY NNN` within the flight/train's own window are bound for reliable consistency checking; inconsistency produces `price_contradicted`; source fact missing price, unsupported hard currency, or no anchor with price outside the heuristic 120-character window produces `unverified_price_claim`; from-prices/approximate prices and non-numerics are still not compared, no conversion or tolerance inference, hotel `priceRaw` keeps redacted semantics. Does not close [#273](https://github.com/Danceiny/gotry/issues/273) or all D-26 residuals.

Alternatives and trade-offs: LLM self-annotation — unenforceable (issue #46 empirical failure); lenient release at render time — unverified facts out the door; policy surface wired to a live visa API — v1 policy facts are render-side + gate-side only, the production end is recorded as D-26.

**Root-fix direction**: artifacts generated only by render primitives (structured → markdown one-way); reverse extraction demoted to a transitional state.

#### 8.21 Extension distribution three channels
The Chrome platform forbids non-store CRX direct install — GitHub Releases only does "download" (stable asset names tar.gz/store-zip/dist-manifest); the Web Store is the "one-click install + auto-update". Three channels, same extension: **Chrome Web Store (listed 2026-09-02, recommended)** one-click install/auto-update; the GitHub channel is explicit opt-in (review-free, faster version updates); bundled guarantees the offline deterministic fallback.

**Listing field notes**: the store re-signs with its own generated signing key and does not honor the manifest-fixed key — the store edition's extension ID (`oeajpiccmonococjcegddlooeeohlbgd`) differs from the unpacked fixed ID (`olpgkofjhhiiiahdkkbcninhjmegghfe`); the bridge Origin whitelist trusts both channels equally (`EXTENSION_ORIGINS`, run-all §38); the port pool/host whitelist do not drift with the channel.

Alternatives and trade-offs: install CRX from arbitrary URLs — forbidden by the platform; npm package as the only channel — extension updates forced onto the rc release train; independent pinning — conflicts with the decoupling goal; self-hosted update server — violates the zero-infrastructure surface.

#### 8.22 static golden = auditable comparator
route/carrier come only from the OpenFlights pinned revision; times/prices come from the manual band with every field marked estimated; `requested`/`effective` source, revision/license, and fallback reason are recorded in the same evidence entry. **On snapshot or route failure, warn on stderr then fall back to manual; silently switching source or masquerading as live availability is forbidden.**

Alternatives and trade-offs: `hbcli search-flight` — neither local nor upstream has this capability (N/A); Ctrip credential-free open API — not found, and the public page returns 432 (N/A); simply renaming manual to static — provenance fraud; keeping manual only — continued vendor lock.

#### 8.23 ADR-23: embedded Booking Copilot security boundary and BFF request identity binding

> **Status update (2026-09-05)**: the re-review condition "retire v1 after all consumers migrate to v2" **has triggered** — #133 (stacked merge #134) converged the dual protocols into a single `booking.surface` contract: the v1-only runtime was deleted, the v2 stack promoted to suffix-free canonical files (`contracts.ts`/`runtime.ts`/`server.ts`/`startup.ts`), `SCHEMA_VERSION='booking.surface'`, a single HTTP path with a single handshake, version vocabulary removed from the ledger; JSON wire field names unchanged. Below is the decision text from the dual-protocol era (the boundary design still holds); the current form defers to this entry.

Booking Copilot is a BFF-only embedded read-action surface inside the existing workbench:

- **Protocol form (historical)**: v1 was kept compatible, v2 ran in parallel as a closed typed contract; the same listener, task ownership, and ledger dispatched by request version+schema hash. **Converged to a single contract since 2026-09-05, v1 retired**.
- **identity binding**: production standalone's `bff-bound-turn-only` accepts only internal `user.turn`/receipt continuations already bound by the BFF; the v2 browser submits only a safe opaque `requestKey`/optional `taskHandle`, and only a full authenticated principal/scope + BFF trusted binding seam can atomically map `user.turn.ingress` to a server-generated, durably bound `taskId + turnId + contextRef + surface + allowedActions`. Under bound-turn-only, ingress returns a typed 503 before any ledger/planner side effect; a taskHandle can bind only one task/context within an actor scope.
- **one-time approval**: a must blocker can only be released via an option persisted by the runtime and actually presented; the approval binds field-by-field the task, context, source turn, source action, source receipt digest, canonical presentation key, random delivery nonce, and option digest, and can be consumed only once.
- **availability reducer (typed recovery sub-state-machine)**: one recovery freezes at most 5 candidate hotels; each generation holds at most 3 current OfferRefs per hotel; each hotel lifecycle allows at most 2 CheckAvail and 2 offers/HotelRates generations. `unavailable`, material `changed`, or an unconfirmable gap invalidates the whole generation and requires a fresh query; partial evidence yields only inconclusive exhaustion — it cannot claim the market has no rooms. generation/attempt/candidate/receipt digest/workspace revision fold with the ledger; restarts and identical replays do not add budget; terminal is an absorbing state. The recovery-chain contract layer (`ts/src/booking-surface/recovery-chain.ts`, run-all §65) projects this reducer into the user-communication and audit surface: explicit disclosure of supplier changed/unavailable verdicts, no silent hotel/offer substitution, and saga-shaped `booking.copilot.recovery.*` audit events — the #142 merge-gate real-inventory evidence remains gated no-spend.
- **same-revision replay guard (ordinary TURN branch)**: applied AFTER the confirmed-availability `confirmed_workspace_drift` rejection so the historical specific diagnostic stays stable; `BookingCopilotTaskRuntime.resumeTask` then compares the durable workspace semantic digest (contextRef/revision stripped) of every ordinary `booking.copilot.user.turn.observed` row against the prior state at the same revision; a silent drift fails closed as `ledger_corrupt:<task>:same_revision_turn_workspace_drift` instead of letting a row written by an old public `startTask` (no live-time CAS) smuggle a verifiedOffer/loadedOffer/selection drift into replay and turn the replayed workspace into checkout authority. The explicit `replayUpgradeRequired` reanchor and the confirmed-availability `confirmed_workspace_drift` rejection stay intact; the live-time `startTask` guard (`workspace_mismatch` at same revision without an active reanchor) remains the primary defense.

Explicitly rejected alternatives: a standalone chat booking page; replacing v1 with a breaking v2; free-text/JSON execution; treating server outbox intent as already displayed; exposing `Book` on the embedded surface. Current capability excludes portal tokens, PII, or supplier-cost egress; off-page automatic write/payment is deferred to the M5 WriteGate proposal (`design/write-gate-production-design.md`); multi-writer or cross-host triggers ADR-15/16 re-review.

#### 8.24 ADR-24: turn budget = routing + wall-clock dual exits (turn-policy / turn-deadline)

**Problem (shaped by the 2026-09-02 trajectory)**: every conversation turn must end in one of three states — ① answered in person ② handed to async with a revisit promise ③ converged answer; a fourth state "the stream died and nothing was delivered" is never allowed. The old step-count gate (16 soft / 18 hard) and the v1 fixed wall-clock gate (60s/120s) share the same wrong shape: at the deadline "refuse tools + force a final" — the model can still die mid-stream and the trajectory user gets nothing.

**v2 design (complexity decides the exit structure, not a mapping onto bigger constants)**:

- **Routing layer** (`ts/src/turn-policy.ts`): every user/message (source.kind=user; plugin/skill injections do not participate) is deterministically classified quick / sync-planning / deep-planning. Pure function, zero LLM, zero IO — control-plane verdicts must be deterministic (the accountability iron rule; ADR-9 is the same-shape precedent); the router cannot spend the resource it allocates; LLM routing would break evaluation reproducibility (§45 offline E2E has zero real LLM). v1 rule inputs are only Tier 0 (date-span extractor + constraint vocabulary + message length); `loop.ts`'s `isComplex` is the S5 loop architecture's post-interview review, is not on the product path (its only caller is the replay script), and judges the trajectory's first message false, **so it does not enter this layer**. Misclassification is bounded: the worst outcome is "what could have been answered in person got handed off" (recoverable next turn), so the router only needs to be good enough, not perfect.
- **Allocation layer**: classification → `TurnPolicy={softMs,hardMs,exit}`. The threshold table is data, not code (evaluation/deployment swap tables, not executors): quick 120s/180s converge; sync-planning 300s/600s converge; deep-planning 120s/240s **handoff** (the sync window only suffices for scoping + filing the work order).
- **Execution layer** (`ts/src/turn-deadline.ts`): on hard-threshold refusal, inherited tool schemas are **synchronously** suppressed (trajectory lesson: deferring to `step/end` makes the model loop on the refusal result within the same step until token truncation), then return by exit: converge=`TURN_DEADLINE_EXHAUSTED` (answer with the evidence at hand); handoff=files a `gotry_turn_handoff.v1` work order (`<stateRoot>/gotry-state/turn-handoffs/`, containing the user's original text / ETA≈1 hour / status=open) and instructs the model to state in one sentence "handed to background + ETA + how to revisit". The work order is an **independent format in an independent directory**: async-collect would immediately settle a spec-less state as failed (a false failure); the handoff collector is the future loopx agent tick — filing the work order already fulfills the three-state promise (documented, revisit-able), it does not need the collector to exist first.
- **Wiring**: the product path `apply()` installs by default (routing+handoff is product behavior); benchmark opt-in pins the fixed `{60s,120s,converge}` policy for reproducibility. `GOTRY_TURN_DEADLINE_SOFT_MS`/`_HARD_MS` only pin numbers, never change exits; `GOTRY_TURN_HANDOFF_ROOT` pins the work-order isolation root (in source mode the dsh cwd is the founder's real data directory `ts/dsh-runtime`; tests must pin it — inspection state discipline). Programmatic calls without an agent do not count; turn/session terminal states release state and limits. CI first builds a tarball of the current SHA, resolves the dsh peer closure in an isolated pnpm consumer, then hands that install entry to the same E2E — it does not pass the root dev tree off as the published form.
- **Verification** (`run-all §45`): `turn-policy-tests.ts` table tests (the first fixture = the trajectory user's original text; the canonical deep case must hit — the regression anchor of the whole redesign); `agent-planning-turn-deadline-tests.ts` Cordis integration (converge/handoff dual exits, work-order fields, plugin message discipline, lifecycle); the E2E drags a synthetic deep message + relay past the hard threshold, asserting the handoff result, work order filed in the isolated root, final request text-only, non-empty final.

**Collection loop (2026-09-02 v2 completion, the background half of "come back in an hour")**:

- `ts/scripts/turn-handoff-collect.ts` (single `<ticketId>` / `--all`, the turn-handoff counterpart of async-collect) loads open work orders → spawns a headless planning session with `GOTRY_HANDOFF_CHILD=1` (`bin/gotry-inner.js`, overridable via `GOTRY_HANDOFF_PLANNER_BIN`; recursion guard: the child session's only exit is converge + long leash (300s/900s); the parent's numeric pins and work-order root are cleared in the child environment — foreground may hand off to background, background must produce a deliverable, it must not hand off again) → captures the final answer and settles.
- `settleTurnHandoffTicket` atomically writes `<id>.deliverable.md` + work-order status settled/failed (settledAt/deliverableFile/error). Terminal idempotence: an already-settled work order re-invocation returns the deliverable plus the `gotry_turn_handoff_terminal.v1` JSON (succeeded exit 0 / failed exit 2), zero recompute, zero re-spend; a failed deliverable is an honest failure note ("not completed, please re-initiate; no partial results to deliver").
- Revisit surface: the product path registers the read-only tool `gotry_turn_handoff_list` — when the user asks "is the plan ready", the model queries work-order status; settled attaches a deliverable excerpt (≤600 chars; full text in the same directory's .deliverable.md); fabricating nonexistent deliverables is forbidden.
- Scheduling: the collector is a one-shot command (drivable by cron/loopx tick/manual; `--all` scans all open). v1 honest boundary: the work order carries only the user's original text; the planning session is a fresh context (fresh cwd/DSH_HOME); the original session's already-read workspace/calendar conclusions do not migrate with the work order — the planner re-fetches as needed. E2E loop closure: §45's packaged-binary E2E runs the collector with the real binary as planner after the handoff assertion, asserting ticket open→settled and the deliverable containing the planner's final.

Re-review triggers: when routing misclassification becomes systemic ("what should have been answered in person got handed off" observable), first expand the Tier 0 vocabulary, then consider Tier 2 (structured state); when handoff work orders back up and need a real collector, start the loopx tick design; if the evaluation-side 60s is too tight, adjust the env pin first.

Explicitly rejected alternatives: any constant gate that kills the turn at the deadline (fixed or LLM-dynamically-generated — same shape, same trajectory failure mode); LLM routing (loses on consistency/reproducibility/cost three ways; if future signals prove insufficient, earn authorization via loopx RFC S4's L1 shadow→L2 advisory shadow-comparison path, not granted by default); handoff reusing the loop work-order format (async-collect false failure); thresholds handed over to the dsh host (violates the gotry/dsh layering).

## 9. Evolution (the single source of timeline = `roadmap.md`'s M0-M6; only principles and current state are kept here)

- **Lavish local review tools (#443, parent #438)**: the host explicitly enables five default-off tools through `lavishAxiPackageRoot`. They bind to the exact host session and workspace, serializing bounded untrusted feedback. Terminal records reject active commands; stop preserves owned cleanup and its failure result, and plugin unload closes registration. Existing itinerary generation, discovery and source reading feed the review. See the [Lavish contract](design/lavish-local.md#9-registration-into-the-product-tool-surface); the registered Lavish browser feedback chain is covered by #443 CLOSED + PR #456 merged acceptance, and native HTML preview proof is accepted on #448 with persistent regression in `ts/scripts/dsh-artifact-web-e2e.ts` (re-runnable: `GOTRY_ARTIFACT_WEB_E2E_OUT=<dir> npx tsx ts/scripts/dsh-artifact-web-e2e.ts`). This engineering capability does not establish M4/M5/M6 admission.

- **Itinerary HTML renderer + product generation entry (2026-09-12, internal slice of #442, parent #438)**: two landed pieces. ① `ts/src/itinerary-html.ts` — a pure bounded renderer: explicit itinerary (`trip_start`/`trip_end`/`stays`/`od_segments`) plus caller-selected facts in, one self-contained script-free HTML document out; plan plane and evidence plane never merge (a destination-level hotel inventory hit is rendered in a separate block and can never read as "this hotel has a room"; a stay only links facts with exactly matching destination and both dates); no overall verified badge; missing data stays missing; malformed input (wrong types, impossible calendar dates, bad enums, non-finite/out-of-range numbers, over-capacity arrays, oversized document) returns `ok:false` with structured errors instead of silently truncating the itinerary; no IO, no `Date.now`, no Markdown parsing, no date/night/budget arithmetic. ② `ts/capabilities/itinerary-artifact.ts` + registered tool `gotry_itinerary_render` — the single product generation path: `fact_ids` are resolved against the current `stateRoot` fact registry (`loadFactRegistry`; unknown/duplicate/over-limit ids rejected, malformed registered rows rejected by the renderer, caller-supplied fact objects never accepted), the document is written as exactly one **new** file at the session working directory top level with `O_CREAT|O_EXCL` (no overwrite, no symlink-target escape, `.git`/`node_modules` refused, crypto-random basename by default), invalid input writes zero bytes, requires an explicit absolute session working directory (missing/empty/relative cwd fails closed with no process-cwd fallback), and returns the final real path. Evidence = run-all §6c `itinerary-html-tests.ts` + `itinerary-artifact-tests.ts` over isolated `mkdtemp` stateRoot/cwd fixtures (registered-tool execute path). The generated document's browser coverage (real browser navigation, native details and Return-keyboard, narrow-screen readability, escaping) is closed by #442 CLOSED + PR #449 merged acceptance. Native HTML preview sandbox acceptance = #448 proof accepted with persistent regression in `ts/scripts/dsh-artifact-web-e2e.ts` (re-runnable: `GOTRY_ARTIFACT_WEB_E2E_OUT=<dir> npx tsx ts/scripts/dsh-artifact-web-e2e.ts`); the registered Lavish browser feedback chain = #443 CLOSED + PR #456 merged acceptance; no supplier evidence, no M3/M4 admission.

- **WriteGate mechanism layer: persisted trusted approvals + atomic outbox (2026-09-11, issue [#231](https://github.com/Danceiny/gotry/issues/231), M5-2 first slice)**: `ts/src/write-gate.ts` (write_gate.v1) landed the mechanism/contract layer of the production design (`design/write-gate-production-design.md` §3/§5, aligned with the `design/fact-writegate-seam.md` §2.2 two-transaction ordering): PreparedChallenge/ApprovalClaim/WriteEffectIntent persistence, transaction 1 = one-time receipt consumption + pending_writes transition + outbox append atomic in one SQLite transaction, transaction 2 = dispatcher atomic claim with immutable attempt/fencing persisted before any external call, storage-level non-bypassable constraints (composite FK, non-empty receipt CHECK, forward-only dispatch trigger, immutable attempt trigger), L4 revocation at issuance/consumption/dispatch, and pre-dispatch revalidation settling `rejected`. **Non-runtime activation: zero real supplier/transaction calls; the product runtime does not instantiate WriteGate before M5 Entry (#136); default paths unchanged; external exactly-once is not claimed.** Falsification: run-all §61 (95 assertions — model impersonation, prepared-challenge direct consumption, cross-intent/tenant replay, amount/terms/nonce drift, receipt/challenge expiry, three crash-injection points all-or-nothing, two-process claim race exactly-one-winner, raw-SQL physical red lines; red→green proven by removing the FK/forward-only trigger).

- **#299/#355 flight/train claim split and 12306 typed session (2026-09-10, parent #273)**: complete G/D/C/Z + 3–4 digit train numbers go into the separate `trains`; legitimate two-letter airlines (including `CZ8582`/`9C8781`) stay in flights; flight/rail verdicts are isolated by fact kind. FlyAI train and `gotry_session_search kind=train` both follow the tool → typed fact log → canonical anchor → `gotry_fact_gate` path; 12306 typed outcomes distinguish empty/malformed/transport; positive facts defer to host-captured invocation route/date/batch and exact response URL binding; the inline startTrainDate is only validated and preserved, with `captured_at_non_future_and_max_age_15m`, `canWebBuy=Y`, and a closed set of available-seat tokens; lists are not backfilled with fares. Missing evidence still fails closed as `rail_claim_unverified`; deterministic fixtures only — no claim of real supplier bookability or M4/M5/M6 admission; #299/#273 stay open.

Principle: **no stage skipping, no premature optimization of the next stage's concerns**; each stage's Entry/Exit/gate in roadmap. The old Stage 0-4, the master-outline Phases, and product M1-M3 have been merged and mapped onto M0-M6 (mapping table in roadmap).

- **M0 ✅ / M1 ✅ (bb880f3) / M2 ✅ (b0cfd97)**: M2 deliverable = §7-1 three-layer combination (skeleton + verification + anchors) + hbcli bridge + dsh end-to-end (DeepSeek native, persona + five tools) + one-command entry `./gotry`; the G1/S1/§7-1 three gates settled by founder instruction.
- **Issue #202/#242 map-plugin vendoring regression fix**: root/ts strict npm installs no longer resolve the external `dsh-map-tools` peer; `vendor/dsh-map-tools/` carries the upstream `0.5.1` MIT payload, whose vendored settings wiring uses the alpha.3-published `SettingsProvider.prototype.installSection(owner, ns, schema, entry, hooks)` method and a plain-string namespace, verified by proof. `bin/gotry-inner.js`, the CLI bootstrap, and `capabilities/doctor.ts` resolve the in-repo absolute entry with equal priority; packaging proofs cover unpack/import, the 32-file payload aggregate, the 7 `map_*` registrations, the settings watch/reload/dispose lifecycle, and the no-network inline coordinate path.
- **Issue #341 ground-transfer seam**: the public map-tool serves only as a provider boundary, no deep import of `OsrmClient`; route estimates resolve into deterministic minute facts at the capability layer before entering the existing solver input, so solver/evaluate performs no network. `ground_transfer` binds the arrival direction (A→B) and the return direction (B→A) separately — requested, cached, and degraded per direction, so a miss/error/stale/mismatch on one side only falls back that side to the static estimate and never borrows the other side's dynamic value; the candidate transfer holds `minutesOut`/`minutesRet` overrides consumed per direction by `evaluateChoice`. The first slice wires route minutes only into the static `taxi` transfer of named destinations; prices keep using the static pack; `bus`/`bus_plus_taxi`, live traffic, transit/rail, fares, address resolution, and broader transfer combinations are still out of scope, and the #364 slice does not change the #20/M5/M6 gates. The 2026-09-11 boundary-freeze suite pins the offline remainder closed (vocabulary closure, fallback completeness — self-contradictory route facts such as positive distance with zero duration now fall back explicitly as provider miss — the 900-second switch contract, cache boundedness, and evidence-annotation completeness); the gated remainder is real-data work, not engineering residue.
- **Current mainline = M3 evidence not converged; parallel line = founder-authorized M4 memory domain**: M3's engineering and distribution surfaces are ready; the real seed-user finalization-rate/NPS/POI-hallucination-rate evidence is still the Exit gap. M4 has advanced in parallel under founder authorization since 2026-08-26; the T1 and later memory slices, the Issue #20/#223 scorer, and the Issue #228 explicit lifecycle collector do not constitute M3 Exit proof; the real `observed_private` N≥5 repeat cohort + source-review attestation is still missing. M5 transactions and M6 B2B start only after their respective Entry gates are met; parallel implementation must not back-propagate into gate opening.
- **HotelByte Booking Copilot product-acceptance parallel line**: the candidate is GoTry under the single `booking.surface` contract (2026-09-05 #133 convergence, former v2 form promoted, v1 retired) serving as a BFF-only typed read-action planner inside the existing search/quote/Checkout workbench (boundary in §8.23). This line excludes `Book` and does not constitute M5 Entry; the four-surface real-inventory and "unbookable → re-search → new CheckAvail → original Checkout" evidence has not been obtained, see D-29.
- **M3 real-evidence parallel line (Issue #22)**: the v1 manifest, de-identified cohort/nightly schema, deterministic scorers, and fixture gatekeeping have entered the engineering surface; business pass accepts only the threshold-frozen `real_seed_cohort`, fixtures always fail. The real cohort is still empty, awaiting 50–200 de-identified samples; M3 Exit is not claimed.
- **Time-awareness optimization (2026-08-27, driven by external time evaluation)**: the time anchor layer (arithmetic in code, LLM reads cards, never computes) + slot extraction v1 (verbatim preserved) + 25-question evaluation set and scoring script landed — ADR-11 quality layer's first piece fulfilled (scheduled for M3, landed late); real model (deepseek-chat) 25/25. The slot→spec solve bridge is not done (D-10).
- **tsc debt zeroing + loopx RFC special (2026-08-27)**: `npx tsc --noEmit` 14 errors zeroed (D-11 discharged, 1bf9671); the same day, 13 loopx architecture RFCs were read through and mapped, producing `rfc/loopx-inspired-upgrades-rfc.md` — **founder accepted the same day**, four slices S1-S4 landed in order; the same instruction established **multi-user Agent as a Service** as the future direction (claim/CAS-style mechanisms moved into the RFC §6.5 long-term adoption surface). S1 tool-packet envelope (ADR-13) landed; S2/S3/S4 advanced in sequence (D-12).
- **Transactional state foundation landed (2026-08-28, ADR-15)**: industry durable-execution research converged into the five-piece set → `rfc/transactional-state-rfc.md` filed, founder accepted the same day; TS-0..TS-4 landed in one pass (ledger + five state-tool write paths + read-path fallback + state-cli operation surface + durable work-order crash recovery exactly-once (§28 subprocess exit 9 proof) + `gotry_async_terminal.v1` terminal contract + pending_writes saga, run-all §28/§29); TS-5 trigger-deferred = D-15. Same day **ADR-16 dual-form freeze**: tenant_id first-class field (schema v2), sync = event replication not state translation; §28 dual-form assertions.
- **Session data surface #21 first slice (2026-08-29)**: `session/benchmark.ts` froze the fixture scorer for required comparable fields (missing fields counted as errors, default 90% gate) and the dual-source contract (aligned by journey/segments/times/services; prices only diff-recorded, not equality-judged); `needs-attach`/`needs-login` are waiting-user no-spend; challenge and ReadGuard non-zero fail closed. Pure fixtures entered run-all §25; real sf-01..08 still await Chrome attach permission confirmation and handshake.
- **Extension distribution dual channel (2026-08-30, issue #21, ADR-21)**: founder instruction "artifact download and install also need to be a better user experience; GitHub can be a distribution channel". Chrome platform constraints **honestly surfaced upfront** — non-store cannot avoid the 3 clicks of "developer mode load unpacked"; GitHub can only improve download. The GitHub Releases download channel has landed (`gotry setup --extension-from=github` explicit opt-in, default stays bundled for offline determinism); Chrome Web Store approved and listed v0.1.0 on 2026-09-02 (one-click install + auto-update, now the recommended install method; materials and privacy policy in two `ops/` docs). Regression: run-all §43. Note: this entry once had two mutually contradictory copies in §1; the 2026-08-31 documentation refactor merged them taking "submitted, in review" as authoritative.
- **Session transport settled on the extension bridge (2026-08-30, #21 option C promoted to PRIMARY)**: founder field test "Chrome attach's per-connection permission box is fundamentally unusable" (chrome-devtools-mcp #825: a prompt on every connection, no persistent approval) → CDP demoted to explicit opt-in, extension bridge as primary transport — `extension/` (MV3 zero-build, fixed key = extension ID, SW long polling, MAIN-world passive sniffing, cookie-names name-only) + `extension-bridge.ts` (`node:http` loopback bridge, origin whitelist, zero new dependencies) + lane routing (extension default / cdp explicit / persistent for tests); login fast path avoids tabs; guards shaped per lane (extension = zero write behavior + hints whitelist, cdp = request-level abort); `needs-extension` → `waiting_extension` (waiting-* family no-spend); run-all §38 + bootstrap-tests; the real sf-01..08 gate lowered to "install the extension once".
- **Issue #67 static golden (2026-08-30)**: `sf-live-benchmark`'s vendor closed set extended to `manual|flyai|static`; `static` provides route/carrier from the OpenFlights pinned revision and estimated time/price bands from a manual manifest; requested/effective source, estimated fields, provenance, and fallback reason are written into evidence entry by entry. On static-source anomaly, warn on stderr then fall back to manual; it does not claim live schedules/prices/inventory. run-all §44 freezes CLI fail-closed, eight coverages, fallback, and provider-independent soft scoring.
- **Issue #335 offline summary repair**: the summarizer no longer picks manual by source preference, stitches queries across batches, or lumps all non-manual under FlyAI; the canonical producer filename is the legacy batch identity; the latest batch is selected by filename collection time and fails closed on corrupt/incomplete. run-all §44b covers temp evidence roots, eight queries per batch, old-manual vs new-static/error, missing/corrupt, and legacy unknown. Results remain deterministic/offline fixture evidence and do not change #272's live session/calibration gate.
- **Issue #67 login-state real run and bridge exit semantics (2026-08-30)**: two consecutive static official rounds both hit 8/8 with zero fallback; session hits fluctuated from 3/8 to 5/8; all scoreable hits (3+5 entries) scored 13/13=100%; non-hits must be explicitly disclosed and stay out of the soft-score denominator. The CLI-not-exiting issue triggered by the real-extension online scenario was fixed by parking the default bridge's timer/socket with `unref`; the `keepBridge` wizard track passed §40, and §38 gained a subprocess regression.
- **M4 Issue #20/#223/#238/#228 value-evidence slices (2026-08-29; 2026-09-08 scorer hardening and collector)**: the paired-cohort contract, active-planning wait-deduction caliber, experience-reflux, and preference/P4 red lines were frozen by one read-only scorer + synthetic fixtures and wired into run-all §34. #223/#238 added per-layer exact schemas, HMAC-SHA256 pseudonymous keys, N=5/median-reduction=0.5 threshold freeze (raw-ratio comparison, rounding only in the report), source-review attestation, and `reviewed_summary_digest_sha256` binding. #228/#248 added the explicit opt-in lifecycle collector: stateRoot/consent/HMAC required, dataset key verifier and source/wait freeze, full projection validation before write, write-all + atomic publish + leaf/parent-directory realpath isolation; exports only candidate/synthetic source_review. Synthetic data explicitly cannot close M4; observed-private lacking the manual-verification contract or with a digest mismatch can only serve as candidate. The next stage still awaits the real `observed_private` N≥5 repeat cohort; without samples, waiting/backoff/no-spend.
- **M4→M6 program task graph (2026-09-08, issue #225)**: `docs/design/milestone-delivery-plan.md` splits the already-in-main M4 scorer (#238), explicit opt-in planning lifecycle collector (#248), ledger tenant/fold/migration (#229/#237), state-cli (#243), Z3 lifecycle (#244), and map tools (#245), together with the still-TODO real N≥5 repeat cohort, M5 first supply chain `hotelbyte-cli` agreement verification + WriteGate, and M6 P6 review + pilot conditions, into owner/responsibility-file/dependency/E2E/falsification/exit-criteria; `docs/design/write-gate-production-design.md` serves only as the M5 proposal, covering request fingerprint/receipt/one-time confirmation/crash recovery/HotelByte unknown reconciliation/compensation/commission disclosure. This batch changes no runtime, unseals no transaction or B2B implementation, and changes no Exit.
- **HotelByte transaction bridge / unknown-reconciliation contract slice (2026-09-11, issue #232, M5 pre-entry)**: `ts/capabilities/hotelbyte-transaction.ts` lands the §7 reconciliation state machine as pure functions — book-execution classification (CLI exit0 is not success proof; timeout/kill/non-JSON/unattributable success/partial confirmation all classify unknown; explicit business failure classifies supplier_failed), fail-closed reconciliation seeding (attemptId/customerReferenceNo/intent idem key/request fingerprint mandatory, fact_id/approval-receipt-digest/outbox-ref traceability, probeDigest on every audit event), and query-orders reconciliation — miss inside the 180s Phase1 + up-to-10min Phase2 recovery window stays unknown with rebooking structurally forbidden; window expiry alone is never no-order proof and only escalates to manual; authoritative terminal negative evidence bound to the attempt (supplier cancel/fail, or an evidenced manual no-order confirmation) is the only path that allows a new intent; multi-order/reference-mismatch/post-terminal counter-evidence surface explicit conflict for manual review, never silent success or silent drop. Outbox/dispatch persistence remains #231's — this slice consumes only an `outboxIntentRef` placeholder ID. **Non-runtime-activation: zero real supplier calls** (no subprocess spawn, no network, no credential access; the issue #318 `GOTRY_HBCLI_LIVE` opt-in gate stays default-off); all behavior proven offline by `ts/scripts/hotelbyte-reconcile-tests.ts` (run-all §60). **Spawn-level fake-CLI full-chain E2E landed (issue #232 §5, PR pending link)**: `ts/scripts/hotelbyte-spawn-e2e-tests.ts` (run-all §66) drives quote(check-avail)→approval(L3 receipt digest)→book→unknown(enterUnknown)→query-orders(advanceReconciliation) through real `child_process` spawns of a local shell fixture (programmable modes: normal quote/book JSON, hang, non-JSON garbage, pending partial confirmation, exit0 unbound success, kill -9, explicit business failure; query miss/confirmed/auto-canceled/processing/multi/denied/crash/garbage) — within the suite a bare exit-code-only wiring (no reconcile layer) is proven wrong on the same real spawn outputs (timeout treated as a rebookable failure → double-booking risk; exit0 garbage/unbound/partial treated as success; in-window miss treated as no-order), and every scenario asserts the contract's terminal classification and reconciliation action: timeout→miss→late success converges reconciled_success without rebooking; timeout→miss→late auto-cancel converges reconciled_failed (the only new-intent path); pending→processing→window-expiry escalates to manual while staying unknown; multi-order surfaces explicit conflict; argv round-trip proves the customerReferenceNo/ratePkgId binding crosses the real process boundary. The only executable is a temp-dir fixture binary — zero real hbcli/network/credentials, all spawns bounded-timeout, `GOTRY_HBCLI_LIVE` default-off and not consulted.
- **sponsor plugin / same-kernel end-to-end reuse proof slice (2026-09-11, issue #235, M6 pre-entry)**: `ts/capabilities/sponsor-plugin.ts` lands the sponsor plugin contract face as pure functions over the unchanged kernel — traveler principal/sponsor/BFF principal three-subject separation (fail-closed construction; sponsor id conflating the traveler principal is rejected), authorization route closed set + mandatory session/order binding, covering the reuse walkthrough's three plugin slots (entry / inventory pool / disclosure configuration). The reuse proof is structural, not a slogan: the module only imports kernel functions (`booking_saga_fsm.v1` state machine + wish-pool condition scoring) with zero copy, `ts/scripts/sponsor-reuse-tests.ts` pins the kernel edge/rejection tables cell-by-cell from the consumer side (kernel drift = suite red), and one runner (`runBookingScenario`) drives both B2C and B2B forms with cell-identical saga edge sequences — the PR's kernel file set carries zero diff. Red lines structurally encoded: ranking runs only after kernel full-condition-hit (commission cannot redeem unmet traveler conditions; same-score tie-break only by sponsor declared order with mandatory bias disclosure; the commission value never enters the ordering key), the sponsor disclosure digest enters the confirmation fingerprint (disclosure change = fingerprint change), empty receipt is rejected in both forms (same WriteGate rule, no B2B exemption), and cancel/refund walks the same kernel edges with zero replay violations; A/B same-business-id scenarios stay partitioned by principal scope — events/fingerprints/approvals do not cross, and cross-scope approval replay fails fingerprint binding. Isolation: no `ts/src/**` file references the sponsor module (C-side default path untouched) and the B2C trace passes `sponsorLeakFindings` with zero findings. **Non-runtime-activation: default-off, zero real calls** (`SPONSOR_RUNTIME_ACTIVATION_DEFAULT=false`; no spawn/network/credentials; real activation stays behind #137 M6 Entry); all behavior proven offline by `ts/scripts/sponsor-reuse-tests.ts` (run-all §64). Real traveler outcomes and pilot signing remain #137 M6 Exit and cannot be substituted by fixtures.
- **M4→M6 public delivery ledger (2026-09-10, issue #270)**: issues record scope/base/execution route and gates; Draft PRs publish exact head/drift/local evidence/TODO; review binds to head; merge records method/merge SHA/destination; founder-authorized in-repo Claude lanes are exempt from external-bot T0/T1 veto but not from normal review. Debt owner/trigger in §10; local and fixture evidence does not replace the real admission of #20/#22/#136/#137.
- **Booking unavailable/changed recovery-chain contract slice (2026-09-11, issue #142 non-gated slice)**: `ts/src/booking-surface/recovery-chain.ts` lands the recovery chain's structured disposition as pure functions — detection classifies action receipts into the existing `ActionReceipt.status` closed set (confirmed/changed/unavailable/inconclusive/unrelated; incoherent receipts fail closed with `availability_receipt_incoherent`/`availability_observation_required`, and unconfirmable evidence can never claim the market is empty); controlled recovery records requery/recheck-version/alternative-candidate steps inside the planner three-call budget semantics with availability-policy budgets frozen-referenced; user communication is a structural red line — resolving onto a different hotel/offer requires prior explicit disclosure (silent swap refused), version shifts require an explicit old→new explanation, and recovery exhaustion may only degrade with a mandatory non-empty disclosure; audit events in the `booking.copilot.recovery.*` ledger namespace carry receipt digests and validate saga-style with resolved/degraded absorbing. Detection and legality remain owned by the availability reducer (ADR-23); this slice neither wires a runtime seam nor touches solving/evaluation layering. **Non-runtime-activation: zero real supplier calls**; all behavior proven offline by `ts/scripts/booking-recovery-chain-tests.ts` (run-all §65). The #142 gate items "four-surface real inventory" and "real Checkout/QueryOrders evidence" remain gated no-spend, waiting on real supplier/UAT.
- **#279 Ctrip flight malformed-response isolation fix (2026-09-10)**: the three-state parse contract is legitimate `flightItineraryList: []` → `miss`; non-empty list with at least one valid segment → `hit` with malformed siblings ignored; JSON/root/data/list with explicitly non-array `priceList`, or a non-empty list with no valid rows → `error`. The compatible API `parseBatchSearch(body): SessionFlightOption[]` never throws and returns `[]` on error; both extension/CDP searches map parser errors to the existing structured `error` after challenge detection, without options. `flight-malformed-tests.ts` proves null, non-array list/price, all-malformed, legitimate empty, hit, mixed, and challenge cases with an isolated extension poller declaring `capabilities: ['ctrip-flight']`; this is local parsing/orchestration evidence and does not satisfy #272 live interface calibration, real supplier evidence, or M4/M5/M6 admission.
- **#352 FlyAI malformed-response isolation fix (2026-09-10)**: an exactly empty `data.itemList: []` stays `miss`; any typed-validation failure on any item of a non-empty flight/train/hotel list returns a whole structured `error`, discarding valid siblings and generating no negative-inventory facts; only a fully valid list returns `hit`. `flyai-tests.ts` covers this boundary plus the existing 429/exit-failure semantics with production effect, isolated fake CLI, and fact sidecar; this is offline engineering evidence and does not satisfy real provider/UAT or issue closure.
- **#411 sf live benchmark challenge stop (2026-09-12)**: `sf-live-benchmark` stops on the first `challenged`/`challenge_stop`/`guard_violation` — no retry, no bypass, and later session/comparator calls are structurally impossible; the challenged verdict remains verbatim. `batch_complete` is true only when the terminal reason is `completed` and all eight queries ran. `sf-summary` fail-closes selected challenge/guard evidence, including a complete batch and legacy top-level `sessionVerdict`. Production cadence stays fixed at 35 seconds; the isolated offline harness mocks child timers. Regression: run-all §44c covers first-query, mid-batch, last-query challenge, and the intact eight-record batch with persisted JSON, CLI output, request counts, temporary roots, and zero network. This remains offline engineering evidence under #272/D-13; real login-state calibration stays there. Issue #420 entrypoint-alias child slice (2026-09-12): bounded fix to the CLI main guard only — `realpathSync` canonicalization closes the silent-skip-on-symlink defect; scoring/selection/source buckets/fail-closed semantics unchanged. Tracks #272; M4–M6 admission evidence unchanged.
- **Known-limitations clearance, first cut (2026-08-29, founder instruction "fix these known limitations")**:
  - **Z3 WASM lifecycle re-fix (#227, 2026-09-08)**: the 08-29 singleton+mutex still left two Node24 intermittent surfaces — `getZ3`'s cold-start Promise wrote the cache after await, allowing concurrent creation of multiple Contexts; `z3-solver@5.2.0`'s high-level `FinalizationRegistry` cleanup directly triggers low-level native `dec_ref`/`*_dec_ref`, bypassing `withZ3`. This round's fix: cache the Promise first, enable `enable_concurrent_dec_ref` fail-closed, locally wrap low-level cleanup and actual async native calls, queue cleanup only while a session/native check is active, and drain synchronously after idle; fatal WASM/heap errors poison first and reject subsequent solves. Counterexample coverage: global `FinalizationRegistry` identity unchanged, returned live Model/Ast remain usable across sessions, independent concurrent requests queue without false-positive nested detection, and the in-flight native barrier must be entered before settle is allowed. run-all §30/§30b/§30c.
  - **Thin-shell legacy** (`shell/` directory) physically deleted; dsh web confirmed as the only product surface.
  - **Live fare integration**: `ts/src/realtime-pricing.ts` — dated flight-chain segments overwrite spec prices via the FlyAI official read-only channel with exact flight-number matching, evidence chain `[live API: flyai@ts]` merged into skeleton_notes; miss/error/redacted price/no match all degrade back to the static pack, never throwing. `realtimeSolvePort` (env gate `GOTRY_REALTIME_PRICING`, default off) wired into replay-real — **the static pack went from sole source to explicit fallback**, run-all §31.
  - **i18n English-surface engineering layer**: `i18n.ts` message catalog — zh-CN default and byte-identical to golden, `GOTRY_LOCALE=en` switches to English, missing en keys fall back to zh; covers the solve deterministic surface (candidates/flight-chain answer_md, relaxation suggestions, exclusion reasons, wish reasons). run-all §32; tool cards and persona conversation surface await M4 calibration samples.
- **Contribution infrastructure (2026-08-29, open-source collaboration surface)**: GitHub Actions CI (node 22/24 matrix: typecheck + full-stack regression, `GOTRY_SESSION_LIVE=0`); `CONTRIBUTING.md` + issue/PR templates; lockfiles (root/ts both) and the dsh-runtime three manifests committed to git, resolved all repointed from the internal mirror to registry.npmjs.org (integrity verified per package); contribution flow switched to PR-based.
- **#271 Phase A process-incident observation (2026-09-09)**: the GoTry-owned `uncaughtExceptionMonitor` synchronously records uncaught/rejection by origin; the host still owns the fatal handler and exit policy; the incident writer does single-fd append+fsync+close, and any durability failure returns `false` without rethrowing. native Node24 ESM counterexamples cover monitor/no-monitor non-zero exits, existing host handler exit codes, writer fsync/close, and tool structured failure. child close/spawn error, SIGINT, descendant processes, and the upstream dsh supervisor remain uncovered #271 boundaries.
- **#271 outer dsh liveness closure (2026-09-10, GoTry-owned)**: `bin/gotry-inner.js` launches the dsh child in its own POSIX process group; child exit/error/close and parent SIGINT/SIGTERM all go through bounded TERM→KILL, waiting for the direct child and the whole group to drain before preserving the original exit/signal semantics; the non-benchmark path cleans up on exit first, preserving the benchmark zero-exit stdout drain/parser. Child incident writes still complete synchronously before exit. `ts/scripts/issue-271-liveness-tests.ts` uses Node24, fresh package-shaped HOME/DSH_HOME/stateRoot, and the real installed version `dsh@0.1.5-alpha.1` to cover the actual `bin/gotry-inner.js`'s inherited-pipe delayed-close, non-zero, normal zero exit, and TERM-resistant descendant, verifying parent/leader outcome, stderr, product incident lines, post-failure markers, and before/after descendant/group cleanup; the existing five focused cases remain. Only the GoTry outer owner is proven; the deeper contracts of dsh SDK direct transport and dsh's internal supervisor are not claimed discharged in this slice; upstream vendor/lock unchanged.
- **#467 booking-copilot embedded planner persona target (2026-09-13)**: `buildDshEmbeddedBookingPatch` patch target switches from the unregistered `agent-spine` row to the registered public `system-prompt` row, so the embedded booking personaPrefix and corrected decision envelope example reach the first outgoing DSH model request; the existing core proof now carries a maintained first-request assertion.
- **Existing Booking search invalidation (#468)**: for `search.run` and `search.patch`, the runtime validates the host's post-action workspace: a higher revision requires cleared hotel focus, loaded offers, shortlist, selection, and verification; an unchanged revision permits no content mutation. Missing optional fields compare safely. The maintained runtime proof covers `search.run` and valid `search.patch`, including rejection with the pending action retained. This is existing #468 runtime behaviour maintained as regression coverage, not new runtime behaviour or real UAT evidence; real Booking UAT remains open under #142.
- **#282 Booking planner correction (2026-09-09; superseded authority details in #3580)**: `dsh-planner.ts` never deletes non-empty occupancy rooms missing `adults`. Schema correction now belongs to the plugin/DSH loop and must end in a canonical call within the same run; event-authority failures do not cross-run retry. The three-provider-run cap remains only for no-tool/prose nudges, and provider failures propagate. Focused and real local-subprocess proofs are engineering evidence only — real inventory/UAT remains D-29.
- **Hotel date input gate (2026-09-09, issue #283, D-36)**: the discharged date gate is detailed in §1.2; this engineering evidence does not change M3/M4-M6 real admission or Booking inventory business effects.
- **Node 26 dist build compatibility gate (2026-09-09, issue #265)**: after Node 26 removed `stripTypeScriptTypes(...,{ mode:'transform' })`, the dist build switched to the root-declared exact TypeScript 5.9.3 `transpileModule` + explicit ESM emit; the focused proof verifies on Node 22/24/26 the source→dist exact file set, JS/Python/data asset bytes, absence of relative `.ts` specifiers / absence of CommonJS emit wrappers, and executes skeleton-check plus key-variable dynamic imports. The clean-archive release first installs the locked build dev tree, then strictly installs with the final runtime manifest/lock stripped of build-only entries, preventing npm optional-peer from pulling TypeScript back into the artifact. Node 22/24's original typecheck + full-stack CI stay independent. This engineering-quality evidence does not change the real admission graph of #20/#136/#137.
- **Onboarding wizard (2026-08-30, #21 · 2026-09-02 responsibility hand-back reset)**: the first version closed the loop `npx @danceiny/gotry setup wizard` (5-step orchestration + copy extension path + direct `chrome://extensions` + cross-platform GUI panel + health-watch probing + auto-replay once the extension is in place), reducing the user side to 3 clicks + 0 terminal commands. **Reversed after the store listing on 2026-09-02**: gotry had overstepped into managing the browser (`open`/`pbcopy`/`osascript`) and the dsh rendering layer (stdout text wall instead of verdict) — the LLM is dsh's business, the extension is the browser store's business, CLI bootstrap is `gotry setup`'s business; the three responsibilities were handed back. The wizard degraded to **offline health-watch waiting** (pure stdout, no spawn); `sessionFlightSearch`/`sessionLogin` return `verdict.installUrl` on `needs-extension` for the dsh UI to render a clickable link directly. Landed `ts/capabilities/session/wizard.ts` + `health-watch.ts` + `scripts/health-watch-cli.ts`; run-all §40.
- **Provider-aware price table v2 + price-drift monitoring (2026-08-30, issue #49, ADR-20)**: the frozen price table upgraded from `gotry_llm_price_table_v1` (DeepSeek V4 only) to `v2` (`providers.<id>.models.<model>` flattened + `family`/`price_strategy`/`source_url`/`aliases`); MiniMax M2/M2.1/M3 entered the table; `ts/scripts/price-drift-watch.ts` monitors four major providers (DeepSeek/MiniMax/OpenAI/Anthropic), **never auto-applying prices** (ADR-11 discipline). Same batch: CHANGELOG automation — `ts/scripts/build-changelog.ts` + `CHANGELOG.md`. run-all §41/§42.
- **dsh runtime tracking upstream (from 2026-08-29, issue #15)**: when upstream `dsh-v0.1.2-alpha.1` had not been published to npm, a full vendored tarball unblocked first; Round 5 locked the public root/package closure to npm `0.1.2-alpha.3` (source/package root-first resolution; source normal runs keep `ts/dsh-runtime/` state continuity; package/benchmark call directories isolated); the old vendor serves only as non-benchmark legacy resolution compatibility, not promised runnable. Legacy provenance and the upgrade procedure in `ts/dsh-runtime/vendor/README.md`.
- **OTA flatness + account authorization gate (2026-08-29 second batch, founder dictum "OTAs are all tools; don't distinguish main path/fallback path; using the user's account must be confirmed with the user")**: ① hotels wired into the FlyAI `search-hotel` official read-only channel (parse contract `FlyaiHotelOption`; redacted prices keep the priceRaw original value, numeric price constant 0 — preventing "¥7xx truncated to 7 masquerading as a real price"); `gotry_flyai_search` kind=flight|train|hotel three forms; ② OTA tool surface flattened — tool descriptions and persona (19) de-hierarchized (L4 evidence-chain per-source annotation unchanged); ③ account authorization gate v2: `session-consent.ts` hooks `tools/pre-execute`, first call per session per site prompts approval → remembered within the session, denial = revocation for this session (the first version's per-call prompting was rejected by founder field test); sessionAccess tri-state; no-approval-channel/headless fail-closed; authorization state stored in Weak<agent>, not cross-session; ④ login-state seam: `scripts/session-login.ts` (cdp attach → open login entry → human logs in → read-only poll ticket names); ⑤ test discipline: session-tests live sections SKIP by default, `GOTRY_SESSION_LIVE=1` explicitly enables — routine regression never auto-opens the user's browser window. run-all §24 + smoke §12-13.
- **Booking saga state-machine naming (2026-08-29 second batch, issue #17 adopted)**: after mechanism-by-mechanism reconciliation (shared state/checkpointing/duplicate-side-effect prevention/HITL already physically present in the ledger and durable work orders), the vocabulary layer `booking-saga.ts` + design doc + ADR-17 landed; no LangGraph orchestration introduced; compliance is always deterministic-edge; HITL approval = pending suspension + external-event resumption; run-all §36 physical reconciliation.
- **Artifact surface (2026-08-29, issue #25, two steps)**: ① in-dsh viewing — `gotry_artifacts_list/read` makes ledger work-order deliverables and working-directory md discoverable and readable (read card); ② mature panel — dsh-market research (2495 plugins) selected **dsh-better-sidebar** (★3083 / 189k weekly installs, #1 UI component; the self-built zero-dependency webui was withdrawn the same day for not meeting product-grade quality); `gotry setup` host-layer install (skippable via GOTRY_SETUP_SIDEBAR=0, idempotent/degrades to path ① on failure); dsh web sidebar workbench browses + renders workspace artifacts; the artifacts Tab (registerTab client-half) is the next stage.
- **Artifact view enters the M4 queue (2026-09-10, issue #285, on top of #25)**: list's dsh card upgraded to `SearchPathsResultView` (`card: 'search' shape: 'paths'`, truncated/total/paths complete; the client deliverables list binds directly to workspace files; the UI opens any path into read); read upgraded to `ReadResultView` + `presentCall.locations=[{path,line:1}]` (the editor view follows along at the call stage; fallback content[0] pins the source identity line, avoiding mistaking an old summary for new content). Read-scope whitelist = stateRoot root + dsh working directory (excluding node_modules/.git); extension whitelist = text types (md/txt/json/jsonl/csv/log/yaml/yml); cross-root / symlink escape / missing file / oversized file (>2MB) uniformly return ok:false + error + hint. This layer is read-only; the WriteGate red line is not involved; reuses dsh's existing `SearchPathsResultView`/`ReadResultView`/`FileLocation`, building no chat/file-manager replacement. Acceptance evidence = `scripts/artifacts-capability-tests.ts` 12 isolated fixture offline proofs + smoke §15/§15b dsh card contract assertions (`tsc --noEmit` + smoke all pass); the M4 UX engineering surface's (#258/#267 onboarding same-source) doctor-side hint now also points to gotry_artifacts_list. The "list paging / search substring filtering / direct-by-id lookup" todo exposed by this slice is closed by the #458 slice below; M4 still owes the cohort evidence + cross-session memory surfaces, not this slice.
- **Artifact HTML source preview (2026-09-12, issue #441, parent #438, on top of #25/#285)**: `listCwdMarkdown` becomes `listCwdArtifacts` — top-level working-directory discovery now covers `.md`/`.html`/`.htm` case-insensitively (`id` stays the filename, the title strips the matched extension, dotfiles plus `node_modules`/`.git` stay excluded, and the mtime-desc + per-source `slice(0, limit)` + merged `truncated` semantics are unchanged, so markdown compatibility does not regress), and the read extension whitelist gains `html`/`htm` → `lang: 'html'`. HTML is read as UTF-8 text under the same canonical-path / realpath / denied-segment / 2 MB guards as markdown: the read path does not parse markup, runs no script or inline event handler, fetches nothing and builds no iframe; the fingerprint remains sha256 of the full text and line numbers remain original. The list's open action stays host-native and is deliberately a different action: real-CUA review on `d69bc99` against the installed DSH `0.1.5-rc.1` found that opening a listed HTML entry is handed to the host document preview, which selects its HTML renderer by extension (sandboxed iframe, `allow-scripts`) and is not forced into source mode by `OpenFileOptions.line`; the client therefore labels that entry `Open HTML preview` (accessible label plus a visible HTML-preview badge) with a plain-language note that the page's scripts may run, does not pass guessed open options, and keeps the read card as the source-text surface. The read path's no-execution/no-fetch statement is scoped to the read/source card and is not asserted for the host's native preview. One real defect surfaced by the isolated list→read case: `list` returns canonical cwd paths while `read`'s candidate pre-check compared only the literal cwd/root, so on macOS (`/var` → `/private/var`) reading back the exact path returned by list failed; the pre-check now also accepts the canonical cwd/root forms, with the post-realpath canonical re-check unchanged as the boundary authority. The client contract additionally pins that hostile HTML renders as React text nodes (no `dangerouslySetInnerHTML`/`innerHTML`/`srcdoc`/`iframe` in the bundle). Acceptance evidence = `scripts/artifacts-capability-tests.ts` 18 isolated fixture proofs (new 13–18: discovery/title/order, `lang: html` + fingerprint, paging, malicious-HTML literals with zero execution, HTML size/traversal/symlink/hidden/denied-directory regressions, list→read round-trip from both cwd and state-root) + `artifact-client-contract-tests.ts` text-node pins. **This slice is source preview plus a correctly labelled native open, not Lavish integration.** The registered Lavish browser feedback chain is covered by #443 CLOSED + PR #456 merged acceptance; native HTML preview proof is accepted on #448 with persistent regression in `ts/scripts/dsh-artifact-web-e2e.ts` (re-runnable: `GOTRY_ARTIFACT_WEB_E2E_OUT=<dir> npx tsx ts/scripts/dsh-artifact-web-e2e.ts`).
- **Artifact list paging + literal metadata search (2026-09-12, issue #458, on top of #25/#285/#441)**: `listArtifacts` gains `offset` (zero-based nonnegative integer) and `search` (literal case-insensitive substring over `id`/`title`/filename; trim then empty = no filter; no regex/glob semantics, body content never searched) — the model is collect → canonical-path dedupe (ledger authority preserved when both sources point at the same file) → search filter → deterministic global sort (`updated` DESC + `(source, id, canonical path)` codepoint tie-break so equal-mtime pages stay stable across hosts/locales) → page slice. The previous per-source `slice(0, limit)` early-trim is removed: pageSize is applied once at the end so an older cwd artifact is reachable across pages instead of being silently dropped by a per-source cap. `limit` keeps the pre-#458 integer zero/negative clamp-on-1 contract (compatible), rejects strings/non-finite/non-integer predictably, and positive values cap at 50; `offset` defaults to 0 when undefined, rejects null/string/non-finite/non-integer/negative; `search` rejects non-string input rather than silently passing through as "no filter". `total` reflects the post-filter set (real filtered length, not a pre-claim); `nextOffset` is present only when `offset + returned < total`; an `offset` past the end returns an empty page with the known total + `truncated:false` + no `nextOffset`, and the model-visible summary text says "empty page and known total" rather than "no artifacts/no search matches" when `total > 0`. The registered tool layer reuses the same validation helpers as the capability layer (no duplicated validator), so a host schema rejection and a capability rejection both surface as `ok:false` with a field-named error/summary and a hint that names all three knobs (`limit`/`offset`/`search`). The read path is unchanged — root/extension/denied-directory/symlink boundaries, the read-by-id/path/window semantics, the HTML-source-preview rules, and the canonical-vs-literal pre-check fix all keep their #441 behaviour. Acceptance evidence = `scripts/artifacts-capability-tests.ts` 25 isolated fixture proofs (new 19–25 cover >50 cross-page enumeration with no omission/duplicate, equal-timestamp codepoint tie-break, mixed ledger/legacy/cwd dedupe across paging, literal search including case-insensitive / regex-literal / non-body / search+paging / metadata-via-title, invalid `offset`/`limit`/`search` type rejection with no silent coercion, compatible limit-zero/negative clamp-on-1, large-limit clamp to 50, plus the read boundary checks) + `scripts/dsh-artifact-e2e.ts` registered-plugin list→pagination/search→read→Host-presentation proof (>50 mixed-source fixtures, stable ties / dedupe / filtered totals / safe args, beyond-end summary wording).
- **Effect interpreter (2026-08-29, issue #16 adopted, ADR-18)**: "effect description + interpreter" landed at the L4 channel boundary — `ts/capabilities/effect.ts` (effect-value registry + production/mock dual interpreters) and `resilience.ts` (exponential backoff + circuit-breaker tri-state). Key conservative boundaries: ① resilience policy is decided per-effect explicitly — no policy row, no effect (default all-off = zero change to existing behavior); ② FlyAI Sentinel upstream "saying no" is never retried but counts toward the breaker (3 consecutive errors open it for 60s to protect quota); the SESSION channel is never retried and never breaker-counted (risk-control red line); free sources get 2 backoff retries; ③ browser interpretation = SESSION_* effects (existing CDP channel); the zero-Python red line means no visual CUA; ④ no channel auto-routing/price comparison (OTA flatness, founder's call; comparison belongs to the agent layer). The vertical slice wires 5 tools + the realtime-pricing default query entry; remaining channels migrate incrementally (D-23); run-all §37.
- **Bookable fact gate (2026-08-30, issue #46, ADR-19)**: a real-session artifact (a far-future 2027 itinerary) backfilled all-miss exact-date flights with "current schedule webpage + history" and marked them ✓/recommended — root cause = bookable facts had no single source, artifacts had no gate. Landed: `bookable-facts.ts` (pure-function layer) + `artifact-gate.ts` (artifact gate) + `fact-log.ts` (sidecar recording) + the 21st tool `gotry_fact_gate` + persona (20) red line + `data/airline-airports.json` mapping snapshot; the locked golden 2027 E2E (issue audit-value fixture) reproduces and catches every violation (run-all §39; smoke §16). Coverage gaps recorded as D-26.
- **npm-form custom endpoint fix (2026-08-30, issue #48, not released with a version)**: exposed by rc.15 pull-back testing — bin/gotry-inner.js's env mapping only mapped `LLM_API_KEY → DEEPSEEK_API_KEY`, base was unmapped, so the vendored dsh (llm-deepseek reads `DEEPSEEK_BASE_URL`) sent OpenAI-compatible endpoint keys to DeepSeek's official endpoint — an inevitable 401; the README's "any OpenAI-compatible" promise had not been matched by behavior; the same fix added `LLM_BASE_URL → DEEPSEEK_BASE_URL` (explicit DEEPSEEK_BASE_URL wins; the default official path is unchanged); both sides'拼接 semantics verified consistent (both `${base}/chat/completions`); README bilingual + .env.example + bin help synced. The known gap was closed by issue #77 (next entry).
- **LLM_MODEL wired into the dsh session surface (2026-08-30, issue #77, not released with a version)**: `LLM_MODEL` previously had zero consumers on the dsh session surface (exposed by #48's fix E2E: dsh model selection came only from llm-deepseek `DEFAULT_MODELS` or the user's ~/.dsh settings). Dual-track fix: ① bin/gotry-inner.js maps `LLM_MODEL` to `GOTRY_LLM_MODEL`, and the gotry-tools plugin (`capabilities/model-override.ts`) overrides provider/model in the `agent/request` waterfall post-next (in-memory, zero persistence, does not rewrite the user's ~/.dsh; dsh settings layer as schema defaults < composition config < user layer — after the web UI has picked a model, a composition patch alone cannot override; waterfall first-registered = outermost, post-next takes effect last, so explicit .env intent overrides every persistent layer; the override also clears the inherited reasoningEffort); ② the runtime cordis patch appends two by-id overrides (`agent-default-model` default model + `llm-deepseek` catalog single-entry replacement, without hardcoding upstream DEFAULT_MODELS, to prevent drift). Default-path protection: when `LLM_MODEL` is unset, neither track acts. E2E: `ts/scripts/model-override-e2e.ts` mock relay, four scenarios all green; smoke §17 unit regression. Side finding: the vendored in-repo form's Node compatibility window was broken, recorded as D-27 (discharged; archived in [`debt-archive.md`](debt-archive.md)).
- **Channel registry and health surface (2026-09-03, issue #106/#107/#108, ADR-25)**: the unified root cause of three tool-call issues = "at the moment of failure the model had no structured channel state or rerouting guidance in hand" (flyai 429 blind retries across turns / quota invisible / unconfigured calendar erroring mid-session). Landed: ① `channel-registry.ts` — single data source of channel×intent×quota class×evidence level; the persona routing card and `routing` advice field are generated; ② `channel-health.ts` — session transient state + JSONL persistent event surface; ③ doctor v2 = flyai latest rate-limit time + dsh-calendar tri-state (D-9 default not mounted; whether mounted is decided by the setup state surface `~/.gotry/calendar.json`, `npx @danceiny/gotry setup calendar` on/off — env controlling product behavior is forbidden, founder correction 2026-09-03); ④ smoke real-run hit a real 429, verifying the needs-setup→routing full chain. Full design in `design/tool-orchestration-design.md`; run-all §50. Typed-parameter contract migration remainder recorded as D-30 (discharged; archived in [`debt-archive.md`](debt-archive.md)).
- **Behavioral-contract cross-review feedback (2026-09-04, issue #121/#122)**: `evaluation/persona-bench/` same-question cross-review (Kimi 13 turns / Fliggy single turn, founder's call) exposed two interview/presentation gaps that entered the contract — ① (1) motivation-first extended with the "companion arrival chain": when a trip involves companions, must ask where they depart from / whether booked / whether they have their own time window, recorded via `gotry_companion_save`; arrival accounting and budget are accounted in separate chains (cross-review G4: both vendors turned "meeting my girlfriend" into a solo trip); ② new (22) arrival accounting must reach the user: for red-eye/early-morning departures, or segments with hard commitments on landing day, the local arrival time (including date offset), time difference, arrival energy, and previous-night lodging suggestion must be explicitly given (cross-review G7: Fliggy's "about 8 hours" contradicted its own "02:00-06:00+1" on the same page). No tool-surface change, no new tools.
- **Typed-parameter contract migration (2026-09-04, issue #112, D-30, five cuts to finish)**: ① `gotry_flyai_search` migrated to dsh typed ParameterSchemaSpec (seven flat fields, `parameterSchemaSpecToJsonSchema` projected model-visible, `validateArgs` host-authority validation; malformed parameters get a structured rejection at entry, ToolArgsError keeps the ADR-13 shape via guardToolExecute); ② `gotry_session_search`/`gotry_hotel_search`/`gotry_weather_check` (the three session intents share no common required field; per design §4③ the `interpretArgs` tolerance layer is retained); ③ seven model-high-frequency tools (session_login/doctor/artifacts_list/fact_gate/agent_reach/turn_handoff_list/artifacts_read); ④ `query:` blob zeroed (wish_pool/trip_log/flight_verify/anything/web/video/github; trip_log's evidence P0 red line enters the schema); ⑤ the last three payload forms (feasibility_check/motivation_save/companion_save; evidence-class P0 red lines enter nested schemas gated by host authority). **All 23/23 registered tools are now typed-contract**; the cut doctrine: tools with required fields get host-authority blob rejection, all-optional tools keep the tolerance layer. The ordinary-model canary has run (`ts/scripts/typed-contract-canary.ts`, MiniMax-M2.7 × projected JSON Schema × forced tool_choice, 10/10 first-try; secondary observation: without an anchor card the model guesses the year bare — corrected in real sessions by the existing time anchor card + past-date gate, not a D-30 defect); **D-30 fully discharged**.
- **Tool-description first-line generation + doctor host-plugin coverage (2026-09-04, issue #113, L1 remainder closure)**: ① `toolRoutingHeadline` (channel-registry) — retrieval tool descriptions uniformly prepend the registry-generated "serving intent × current channel order" card; seven retrieval tools wired via `routed()`; the model sees the same table when choosing a tool and after a failure; adding a registry row keeps descriptions automatically consistent. ② doctor completed the patch-distribution surface's host-plugin tri-state (`dsh-map-tools`/`dsh-tool-ask-user`; previously only calendar was covered): such plugins are silently dropped as a whole block when runtime resolution fails; doctor's candidate list and bin/gotry-inner.js's resolution logic use the same caliber. source/package is now delivered with the bundled MIT payload from `ts/dsh-runtime/vendor/dsh-map-tools/`; because its peer requirement `rc.1` conflicts with the alpha.3 closure, it is no longer an external npm dependency.
- **Startup one-shot doctor summary (2026-09-04, issue #114, L2)**: at `npx @danceiny/gotry web`/headless startup (in bin/gotry-inner.js before spawning dsh), a detached child process runs the read-only doctor in the background (`gotry-bootstrap.js doctor --summary`) — actionable items print one stderr line, all-ok stays silent with zero output; zero disk writes, detached+unref so it never blocks startup, always exit 0, benchmark surface exempt. "Visible at initialization" replaces "erroring mid-session"; bootstrap-tests §10 pins the behavior.
- **Interpreter migration wrap-up (2026-09-04, issue #115, D-23 closure)**: six channels entered the effect registry (`ANYTHING_SEARCH`/`WEB_READ`/`GITHUB_SEARCH`/`VIDEO_SUBTITLE`/`AGENT_REACH`/`SESSION_LOGIN`); **all 23 tools' external-dependency surface converged into effect_interpreter.v1** (backoff/breaker/declined surfaces unified; no policy-table row, no effect). Policy rows decided one by one: ANYTHING same as the HBCLI family (timeout retried once instantly); WEB_READ same as the free-public-source family; GITHUB/VIDEO timeout retried once, not-installed never retried; AGENT_REACH reflection-bridge passthrough never retried, never breaker-counted (a wrapper is not a router; retrying would amplify upstream quota consumption); SESSION_LOGIN same as the browser risk-control family. The tool surface stays flat as before; per-source evidence-chain annotation unchanged (effect-tests §12 four assertion groups).
- **Store-edition extension detection · adaptive copy (2026-09-04, issue #117, D-24 discharged)**: the needs-extension copy adapts to local channel placement state (`needsExtensionSummary`/`localExtensionInstalled`): local unpacked in place → keep the full dual-channel guidance; not in place (store-edition users / not installed) → automatically skip developer-mode/local-channel copy, pushing only the store one-click install. Bridge-failure summary uses the same function and semantics; doctor's extension item explicitly states "store-edition users can ignore this item"; the static constant `NEEDS_EXTENSION_HINT` retired into an adaptive function. extension-tests §38 three new assertions.
- **Fact gate coverage · hotels enter the gate + render primitives (2026-09-04, issue #118, D-26 closure)**: ① `HotelFact` (gotry_bookable_fact.v1 third form) — exact-date hotel search hit/miss recorded (flyai-hotel/session:ctrip-hotel two channels wired); discipline same-source with flights/trains: scoping not recorded, needs-setup/error transport failures never record negative facts, numeric prices not recorded (upstream redacted prices preserved verbatim; only the number of available properties is recorded). ② `gotry_fact_gate` hotel claims enter the gate — lines asserting bookability are back-traced to hotel facts by destination+date range; no fact → fail-closed (`unverifiable_hotel_claim`); an exact-date miss on record yet written as available = `not_in_source`. ③ **render primitives generate one-way**: `renderFlightFact`/`renderHotelFact` embed `<!-- fact:<fact_id> -->` anchors in every fact line — artifacts rendered via primitives carry provenance anchors by construction; the gate side back-traces anchors deterministically first (heuristics yield); hand-edited/forged anchors = `fact_anchor_unknown` direct violation. fact-gate-tests §10 eight assertions. Remainder: the policy-fact production end (live visa API) remains a D-26 external dependency.
- **Fact gate coverage · policy render-anchor closure + customs-declaration keyword (2026-09-10, issue #273, D-26 residual-closure slice)**: ① `renderPolicyFact` embeds a `<!-- fact:<fact_id> -->` anchor at line end — the policy render primitive is same-source typed-anchor with flights/trains/hotels; artifacts generated via primitives carry provenance anchors; the gate side back-traces anchors deterministically; hand-edited/forged anchors = `fact_anchor_unknown`; the heuristic `POLICY_WORD` covers only handwritten/historical artifacts. ② anchored-line content fingerprint verifies only the anchored policy line's `as of YYYY-MM-DD` against the fact's `as_of`; rewriting or deleting the date while keeping fact_id = `fact_anchor_unknown` — fail-closed same-source with unknown anchors; no claim of sealing the full statement text or all fact drift. ③ `POLICY_WORD` added "customs declaration" (same nature as "entry declaration" but missed by the original regex) — a handwritten policy line missing as_of → `policy_without_as_of` direct violation. fact-gate-tests §11 eight assertions (64 pass, red→green). Residual: full-text fingerprinting of policy statements is outside this slice; re-review when M5 WriteGate wires booking-class write tools (belongs to #136/#231); 12306 typed session integration is covered by the #355 slice described in §1/§9, still with deterministic-fixture boundaries only.
- **Fact gate coverage · policy anchor full-field content fingerprint (2026-09-10, issue [#359](https://github.com/Danceiny/gotry/issues/359), parent [#273](https://github.com/Danceiny/gotry/issues/273), D-26 residual-closure slice)**: `renderPolicyFact` and the registered gate share a single authoritative constructor; **when a policy line contains a `<!-- fact:` anchor**, it must strictly equal the canonical full line + a single `<!-- fact:<id> -->` (subject/statement/source/fetched_at/query_id/as_of); any change → `fact_anchor_unknown`, same-source with unknown anchors; multiple anchors, malformed anchors, non-canonical non-empty prefix/suffix text, or the borrowing form "borrowing a legitimate anchor to write the opposite policy" all fail closed. Policy lines **without** `<!-- fact:` still go through the unanchored/free-text heuristic, outside this slice (handed to parent #273 / D-26). `opts.itinerary.trip_start` bridges to `opts.tripStart`, and like `review_by` is a legitimate review-reminder form. Parent #273 stays open; this slice is a further closure of the D-26 residual; deterministic synthetic / isolated evidence — no claim of live policy, supplier, or M5/M6 admission.
- **Fact gate coverage · flight/hotel anchor field fingerprints (2026-09-10, issue [#363](https://github.com/Danceiny/gotry/issues/363), parent [#273](https://github.com/Danceiny/gotry/issues/273), D-26 residual-closure slice)**: the render primitives (`renderFlightFact`/`renderHotelFact`) and the registered gate share the same authoritative facts; the heuristic **re-extracts the flight_no token from the rendered text of a flight line** requiring strict == `fact.flight_no` (uppercase normalized); **a hotel line must strictly contain** `fact.destination` + `fact.check_in` + `fact.check_out` (exact substrings); any inequality → `fact_anchor_unknown` fail-closed, same-source with unknown anchors. **Field fingerprints do not replace full-line strict comparison** (root contract item 2): the comparability of flight_no/carrier/times/route/price, train canonical, policy canonical, §4b price-compatible forms (total budget / baggage fee / grand total / explicit fare / thousands separators / price-on-request / ¥7xx / approximate price), and the classification and ordering of `price_contradicted` vs `unverified_price_claim` are not swallowed by the new fingerprints and keep the existing judgment primitives. **Borrowed/unknown/duplicate anchors** remain handled by the existing `fact_anchor_unknown` path; field fingerprints only close the root counterexample of "keeping anchor/registry/fact_id while rewriting the visible flight_no/destination/date range" fail-closed. The `gotry_fact_gate` registered gate execution surface adds no new paths, keeping `loadFactRegistry` + `gateArtifact`. The fact log goes through the real append-only JSONL (`appendFacts`); an isolated temp `stateRoot` loads registry definitions → the real `gotry_fact_gate.execute()` is captured; no new execution surface; fact root counterexamples = forged `UO999` / forged destination / forged date etc. Residual: reverse extraction remains a regex heuristic; price tolerance, FX/multi-currency policy, and M5 WriteGate are reviewed per existing debt; parent #273 residual debt retained; the exchange-rate/multi-currency residual is tracked under [#344](https://github.com/Danceiny/gotry/issues/344) (CNY-only settlement today; the first real non-CNY supplier quote, user budget, or destination demand triggers implementation per [`docs/data-sources.md`](data-sources.md) §2); no claim of live supplier, live 12306, live policy, or M5/M6 admission.

- **Fact gate coverage · flight/train inline price contradiction (2026-09-10, issue [#300](https://github.com/Danceiny/gotry/issues/300), parent [#273](https://github.com/Danceiny/gotry/issues/273))**: after exact-date back-trace succeeds, the flight/train shared gate primitive compares only within canonical fare fields or explicit fare-label fields against the complete `¥NNN`/`CNY NNN` comparable CNY hard price of the corresponding flight/train fact; amounts separated off with non-fare text labels do not participate; bare/unattributed or malformed fare money produces `unverified_price_claim` and blocks. Numeric mismatch produces `price_contradicted` and blocks; source fact missing price, unsupported hard currency, or no anchor with price outside the heuristic 120-character window produces `unverified_price_claim` and blocks; non-numeric/from-price/approximate-price values are not compared, no conversion guessing, hotel `priceRaw` does not participate. Price tolerance and FX/multi-currency policy still await owner/founder decision — the exchange-rate/multi-currency residual is tracked under [#344](https://github.com/Danceiny/gotry/issues/344) (CNY-only settlement today; the first real non-CNY supplier quote, user budget, or destination demand triggers implementation per [`docs/data-sources.md`](data-sources.md) §2); does not close all D-26 residuals.
- **Fact gate coverage · non-keyword lodging categories + policy-word finite union (2026-09-10, issues [#301](https://github.com/Danceiny/gotry/issues/301)/[#302](https://github.com/Danceiny/gotry/issues/302), parent #273, D-26 residual closure)**: the handwritten/historical artifact heuristic vocabulary expansion: the lodging class gained 5 non-keyword categories (boutique hotels keep using the existing `hotel` token); bookability assertions under a lodging heading (ATX 1–6) without lodging tokens are also promoted to hotel claims; a same-or-higher-level non-lodging heading exits the context, deeper headings inherit; the policy class gained a 13-word finite union; missing as_of fails closed on read. Latin tokens are case-insensitive but word-bounded (REVUS ≠ EVUS); narrow-vocabulary boundary — no NLP/synonyms/arbitrary policy-vocabulary expansion. fact-gate-tests §12/§13 coverage; `run-all §39` description synced. Deterministic read-side fixture coverage; no claim of live policy correctness, supplier readiness, or M5/M6 admission.
- **Metrics panel first slice (2026-09-05, issue #138, ADR-11 quality-layer engineering surface)**: `ts/scripts/build-metrics-report.ts` read-only aggregates existing on-disk sidecars (no new data sources) — fact gate verdict distribution and blocked rate / channel-health down·cooldown (30-day window) / incident surface (7-day window) / bridge latency percentiles and over-budget counts (>500ms = re-review trigger anchor) / ledger and doctor report existence → single markdown; bad lines skipped per same-sidecar discipline; empty root well-formed; collect has zero writes throughout; the three evaluation layers' run results remain carried by run-all-tests.sh/CI; v1 does not rerun evaluations. Continuous observation/visualization surfaces left to later slices (metrics-report-tests §1-6 all green, run-all §51)
- **External-event seam segments (issue #82)**: segment 1 (2026-09-07 local producer) landed `ts/scripts/channel-probe.ts` probe tick per seam design §6① — read-only probing (hbcli whoami/open-meteo/opensky; session family skipped, flyai not probed by default to save quota); anomaly writes `down`, recovery writes `'ok'` (latest-wins supersedes down; persisted consumers judge health by `state !== 'down'`); channel-health gained `ChannelEventState` (`'ok'` persistent-surface only); doctor readers and the metrics channel surface follow the persisted-health caliber; probe-result→event is a pure-function anchor (channel-probe-tests 6/6, run-all §52, which also pins the strict-timestamp opt-in). Same day segment 2: wish pool consumption — `conditions.channels` optional condition + a named channel being down at recall refutes the recall conditions (wish-channel-gate-tests 5/5, run-all §53; absent health surface = behavior identical to before). Segment 3 (#436, routing propagation): the six non-hit tool results read the persisted latest event per channel at the tool-result boundary under the strict timestamp caliber, project the pure down set, and union it into `routingAdvice`, so a persisted local-probe `down` excludes the channel without any verdict call; missing/unparseable/future/retention-expired rows are dropped before latest-wins so they cannot hide an earlier valid down; only `down` adds a persisted exclusion (`cooldown` stays a pacing state; `'ok'` recovery or expiry only ceases the exclusion), and session down always wins. The default reader caliber (doctor/wish pool/probe), the 30-day retention, and the static `renderRoutingCard` are unchanged. The approved #432 slice (2026-09-12) is deliberately narrower than real remote callbacks: `ts/capabilities/external-event.ts` implements a pure/default-off W2A `w2a/0.1` core-envelope projection with exact tuple admission, 64 KiB bound, required canonical fields, no clock-age policy, no NLP grammar validation, and stripped opaque/free-text fields; `ts/scripts/external-event-tests.ts` covers root counterexamples, hostile payload stripping, cross-product tuple attacks, and a Node permission subprocess that installs a fetch spy before dynamic import, proves the module does not call fetch, and denies file-write plus child-process side effects. It does not register a listener, authenticate a sender, consume events, or write health/wish/ledger state. Real sensor/auth/consumer integration remains #82.
- **persona domain-boundary surface rule (2026-09-07, issue #192 rc18 user field fix)**: in an rc18 real session the model passed `gotry_motivation_save` to the skill loader as a host skill (dsh skill names' legal domain `^[a-z0-9]+(-[a-z0-9]+)*$`; underscores always throw invalid skill name hard errors) — persona (16) added the surface rule: gotry capabilities are always tool calls (gotry_ prefix), never enter the skill loader; on invalid/unknown skill invocation, switch back to a tool call; the contract stays 22 clauses ((16) internal clarification, no new entries); persona-surface-guard-tests 3/3 pin the regression (run-all §54)
- **Clarification-card examples pass the anchor card (2026-09-08, issue #2 rc18 recurrence field fix)**: founder screenshot proof — a clarification card offered the **already past** 2026 Spring Festival/Qingming/May Day as candidate examples for "when I want to spend these 5 IRW days"; the original (8) fix covered only the recommendation chain (time anchor layer + in-year exclusion, e2e §11); the ask_user_question card's example enumeration is model-free-generated and bypassed the anchor card. persona (8) internal clarification added one sentence: candidate time-slot examples (festivals/weekends/months) listed to the user in clarification cards/interviews/options likewise only take dates after today; past festivals do not enter example enumerations; the contract stays 22 clauses (no new entries); persona-surface-guard-tests 4/4 pin the regression (run-all §54). The anchor card itself was already correct (from a 2026 vantage, Spring Festival auto-resolves to 2027-02-06); the defect was purely in card generation not consuming the anchor.
- **Sub-task waiting discipline (2026-09-08, issue #194 track B)**: rc19 npm real session (founder screenshot + session zip proof) — a continuable sub-agent's spawn receipt gives only a durable id (uuid); the model, following jobs-discipline hints, treated it as a job id and called `job_output`; `jobs-local expect()` threw a bare `unknown job` isError straight at the user. Root cause on both sides: on the registration side, dsh-tool-subagent's continuable branch does not enter the jobs registry (only one-shot background registers `subagent-N`); on the query side, unknown ids had no recoverable guidance. Track-B fix: persona gained (23) sub-task waiting discipline — a sub-agent receipt id is not a job id; calling job_output/job_kill on sub-agents is forbidden; completion notices arrive automatically; additional input uses send_message; the contract went 22→23 clauses. Track A (upstream deepseek-harness Discussions report; English draft prepared in the #194 decision-package comment) involves external publication and awaits founder confirmation. persona-surface-guard-tests 5/5 pin the regression (run-all §54).
- **#194 track-A GoTry mitigation (2026-09-10)**: the typed `tools/pre-execute` direct-child guard recognizes continuable durable ids before the dsh jobs registry and returns completion notice/`list_agents`/`send_message` recovery guidance; non-owner, one-shot, and misses keep the native jobs error. The upstream dsh unknown-id general recoverable contract stays open; vendor unchanged, issue not closed.
- **Behavioral-contract slim-down (2026-09-11, M4 persona calibration line; founder directive "the profile prompt is too long and its narrow-domain wording is uninspiring")**: `cordis.gotry-patch.yml` behavioral contract 23→13 clauses (folded text 4095→2637 chars, −36%) — deduplication with zero information loss: former (15) channel setup → `gotry_agent_reach` description (already present verbatim), former (19) session authorization/login detail → `gotry_session_search`/`gotry_session_login` descriptions (already present verbatim), former (20) bookable-fact detail (protected connections / FD·VZ airport mapping / policy wording) → `gotry_fact_gate` description (already present verbatim); former (2)(9)(13)(14) folded into the time/fact/retrieval clauses. The persona keeps only the cross-scenario behavioral contract; regression anchor = persona-surface-guard-tests 6/6 re-anchored (two new anchors: the four dynamic-variable injections and "never claim the tool budget is exhausted"; the rc18 surface-rule / #2 clarification-card / #194 sub-task incident anchors are all preserved). run-all §54; ships with the next npm version.

- **Install-chain three fixes (2026-09-08, rc.19 `npx doctor --fix` field test, three issues closed)**: ① sidebar install false-negative — pnpm ≥10.5 does not run dependency build scripts by default; under strict mode (pnpm 11) `ERR_PNPM_IGNORED_BUILDS` exits 1 even though all 167 packages are fully on disk; `setupSidebar` changed to **judge success by on-disk state** (same caliber as doctor checks; the profile package.json is the only source of truth), attaching `npm_config_strict_dep_builds=false` single-shot downgrade for dsh plugin calls + approve-builds guidance for the skipped node-pty build — no longer false-reporting "install failed" off the dsh→pnpm two-layer-relayed exit code. ② map-tools npm layout genuinely missing — the 2026-09-04 leftover "dependency-channel decision" landed as **bundled vendor distribution** (`ts/dsh-runtime/vendor/dsh-map-tools/` in files[]; the inner resolution chain prefers vendor; the dependency form was vetoed upstream: its peerDependencies require `dsh-settings/dsh-tools >=0.1.2-rc.1` while the runtime pins `0.1.2-alpha.3` (semver alpha<rc); npm strict peer resolution ERESOLVEs, and optionalDependencies are not exempt either — a hard dependency would break the npx main install path). ③ ask-user false-negative — under the npm/npx hoisted layout, dependencies land in a hoisted sibling position outside the package; both sides of doctor's existsSync hardcoded candidates all missed; switched to inner's same-caliber createRequire resolution chain (dsh context first → package-root context → static candidates). Incidentally discharged the committed merge-conflict markers in bin/gotry-inner.js's help text. doctor-tests §5b/5c + bootstrap-tests §11 (run-all §7c/§15b) pin the regression.
- **CI two-layer fix (2026-09-08, #208/#202; main all-red since #197, the rc.19 line was released带病)**: ① the `Install ts dependencies` red root cause = after the runner's npm upgrade, bare `npm ci` force-validates peers; #208 first restored the pipeline with `--legacy-peer-deps` and removed the redundant dsh-map-tools dependency; #202 then pinned all 14 recursive DSH peers exactly at `0.1.2-alpha.3`; CI and CONTRIBUTING returned to bare `npm ci`; the lock's resolved entries all point to registry.npmjs.org. ② the typecheck five-red streak (turn-deadline's session events) root cause = the type surface's hidden dependency on a peer accidentally materializing: `session/event`/`session/disposed` are declared in dsh-session's cordis Events augmentation; under pnpm's isolated layout the root side's same-name package augmentation merged into a different cordis instance — explicit `import type` + `dsh-session@0.1.2-alpha.3` into ts dependencies; the complete alpha.3 closure prevents rc.1 version mixing.
- **Ledger tenant scope implementation fix (2026-09-08, issue #224)**: an independent audit reproduced: after tenant-a/tenant-b wrote wishes, raw events all landed in `local`, `readEvents` cross-saw each other, rebuilding the target tenant wiped it, and the `wish.updated` fold cross-read same ids across tenants. The fix: `insertEvent` writes the current tenant; `readEvents`/fold/rebuild carry the tenant condition; legacy/v1 migration folds only into `local`; new assertions for non-local owner, same id/idem_key, interleaved updates, cross-process reopen, A-rebuild not affecting B/local, duplicate rebuild, and booking saga audit owner (run-all §28/§36). Historical non-local events miswritten as `local` cannot be auto-repaired without evidence; boundary in §8.16.
- **state-cli tenant parameters and un-isolated command boundaries (2026-09-08, issue #226/#241)**: the ledger CLI's `rootOf`/`tenantOf`/business positional misalignment converged into one centralized parse; `--state-root`/`--tenant`/`--limit` are order-equivalent; unknown/duplicate/missing-value/illegal numerics (including `.5`/`+.5`) are rejected before touching the state root. `tick`/`export`/`whatif` are explicitly local-only: non-local can no longer invoke the default local async settlement, overwrite the shared legacy filename, or pass a whole-database snapshot off as a tenant export; `--tenant` is still only a ledger scope, not authentication or authorization. #241/#243 landed in main and closed.
- **gotry-backend unified service skeleton (2026-09-10, founder dictum "only one gotry-wrapped service is allowed")**: `ts/src/backend/kernel.ts` kernel (one HTTP service surface + module-exact routing table; auth/health are module-self-hosted; the kernel only dispatches/404s/isolates exceptions) + two peer modules — booking-copilot (booking surface capability; `server.ts` extracts `bookingCopilotTrafficHandler` for verbatim reuse across both independent-deployment and module-mount paths; server proof all green) and session-search (`POST /v1/session/search|GET /v1/session/status|POST /v1/session/login/open`, the hotel-be aggregation workbench M0's session retrieval surface; ticket cookies name-level, values never pass through). Entry `ts/src/gotry-backend.ts` + `bin/gotry-backend.js`; port `GOTRY_BACKEND_PORT > GOTRY_BOOKING_COPILOT_PORT > 3082` (routing contract unchanged, hotel-be BFF zero changes). gotry-a2a's positioning = this service's A2A protocol port (migration belongs to the deployment-convergence batch); kernel-tests 8/8 pin the regression.
- **Dida supplier-portal session adapter + bridge capability routing (2026-09-09, hotel-be portal integration migration line)**: ① `adapters/dida-portal.ts` wires into the session-search extension/CDP dual lanes (tool surface `gotry_session_search kind=dida`) — entry=`portal.dida.com/hotel/find`, networkHints=portal-webapi `HotelPriceAPI/SearchRealTime|SearchMonitor` (2026-07 hotel-be packet-capture caliber), parsing = SearchRealTime envelope flattened per plan (`RoomTypeList`∪`RoomList` merged, `referenceNo` kept for the server-side booking chain), ticket name `CN_M_DidaTravel` name-level; login is completed by a human on Dida's official site via the hotel-be portal autofill springboard; credentials never pass through gotry. ② `extension-bridge.ts`'s `/jobs` polling-body `capabilities` upgraded from "reported then discarded" to **routing basis** — with multiple Chrome versions / multiple extension versions on one machine, an old extension would claim a new site's cookie-names job and poison-answer with `ok:true names:[]` (passing not-logged-in off as a census conclusion; live E2E proof); site jobs are now dispatched only to pollers declaring that site; a polling body missing capabilities is treated as match-all for backward compatibility (extension-tests §capability-routing pins the regression). ③ live E2E driver `scripts/session-dida-live-e2e.ts` + SW/cookie diagnostic probes (`dida-sw-probe.ts`/`dida-cookie-probe.ts`, read-only cookie names) landed in the same batch; §38 anti-drift assertions synced with the manifest (`*.dida.com` host_permissions + dual-world content_scripts). **The store-edition extension needs one update submission** (host_permissions change); cadence belongs to the founder. Known defects in §10 D-37. The #308 forward fix additionally locks query-first site selection, flight default, and train not bypassing the authorization gate.
- **web startup interactive onboarding (2026-09-09, issue #258/#267, M4 UX proof; #267 = post-merge hardening after #266 merged)**: explicit optional-capability configuration before `npx @danceiny/gotry web` startup (evaluated once per qualifying launch, asked at most once, no cross-launch persisted confirmation — no "already asked" marker is written), reusing `doctor --fix`'s idempotent installers (setupHbcli/setupReach/setupSidebar, no second set built). Only on interactive TTY + auto-installable gaps does it ask once; y → install and give tri-state results (installed/needs-user-action/unavailable, each with a concrete reason); n → continue to web immediately. When there are no auto-installable items but there are user-action/unavailable gaps (e.g. win32 platform does not support auto-install), it renders the classified plan with concrete reasons without prompting or installing, marked `reported` so inner suppresses the duplicate detached doctor summary. CI/benchmark/non-TTY/all-healthy/`GOTRY_SETUP_SKIP=1`/`GOTRY_ONBOARDING_SKIP=1`/`--no-onboarding` all zero-prompt zero-install and still start web; never installs in postinstall or background tasks. `installerEnabled` accepts injected env; classifyDoctorGap/buildOnboardingPlan/runOnboarding never depend on ambient `GOTRY_SETUP_*` throughout. **#267 hardening**: web-onboarding child calls changed to awaited POSIX process-group `spawn` (JS-serviceable SIGINT/SIGTERM; the signal path cleans up result+patch directories); the bootstrap installer `run()` synchronously enters the bounded process-group lifecycle; outer grace explicitly covers the installer's TERM+SIGKILL budget. The result channel changed to 0700 `mkdtemp` + `0600/wx` exclusive writes; landed `bin/gotry-{inner,bootstrap}.js` `orchestrateWebLaunch`/`runOnboarding`/`renderClassifiedPlan`; bootstrap-tests §12/§20/§21/§22/§23 deterministic unit tests — §21 uses a temp install-package fixture + fake dsh + fixture-local TTY preload to cross the **real** inner→bootstrap onboarding→dsh-web process boundary (observing the prompt exactly once); §21c prompt-wait SIGTERM cleanup; §21f accepted-install parent SIGTERM clears a stubborn installer; §21g accepted-install onboarding timeout clears the installer and continues to web. **M4 UX quality line; does not count toward #20's real `observed_private` cohort Exit** (D-34 discharged).
- **DSH runtime closure migration 0.1.5-alpha.1 (2026-09-09, issue #268)**: root/ts dual manifests + npm/pnpm dual locks migrated exactly from `0.1.2-alpha.3` (216-package closure) to `0.1.5-alpha.1` (230-package closure: 15 new sentinels + removed `dsh-tool-subagent-report`). All 230 `@deepseek-ai/dsh*` packages are pinned to exact versions — rejecting `^0.1.5-alpha.1`, which would match `0.1.5-alpha.2` semver prerelease drift; CI `npm ci --strict-peer-deps` / pnpm `--strict-peer-dependencies` are explicitly strict, not relying on local config. run-all §23a-§23e five deterministic proofs (subprocess-local: only public API mounts Cordis/provider + real active parent-process spawn of a non-detached child + public terminate/waitForExit process-group signal terminating the whole group → descendant PIDs gone + real spawnTerminal cross-platform verification of PTY output 'pty-line' and exitCode=0 + under Darwin prebuilds additionally asserting node-pty spawn-helper 0755 mode — on non-Darwin platforms the helper is inapplicable; they report the platform-pty fact without faking a stat on a nonexistent helper; session V3: isolated temp-directory file bytes — V2 codec encode → JSONL write → catalog read classified migration-required → V3 recovery → independent V3 successor writes → validation:current fully decodes → post-migration source bytes compared unchanged; http-proxy: real fetch loopback SSE=200 + poisoned hit=0 + finally await disposer + await both servers' close + global routing restored directly; target-closure: root 230 npm/pnpm/importer + ts three-layer 230 lock/installed; createRequire resolution rejects nested/mixed versions). Setup behavior unchanged; historical 0.1.2-alpha.3 evidence is retained in §9/roadmap/stage1/release-notes old entries, not batch-replaced. This item has not yet been tagged or npm-published; these deterministic proofs do not constitute M5/M6 admission.
- **DSH alpha.1 public-contract compatibility note (2026-09-09, issue #290)**: fixes target public-contract differences on top of the 230-package closure. ① system-prompt Config: `dsh-system-prompt` exposes/reads `personaPrefix` and `personaSuffix`; the legacy `persona:` key is not projected; `bin/gotry-inner.js` `projectBenchmarkPatch` and `projectBenchmarkSystemPrompt`, the `system-prompt` block of `cordis.gotry-patch.yml`, and the booking-copilot embedded-planner projection in `ts/src/booking-surface/dsh-planner.ts` all rewrite `personaPrefix: >-` (the embedded booking persona text); e2e and proof unit tests assert the captured benchmark-tool planner request carries each stable sentence exactly once (the title request is outside another contract). ② `SubprocessHandle` public contract: the target `0.1.5-alpha.1` SubprocessHandle does not expose `pid`, only `collected`/`done`/`terminate?`/`waitForExit`; the bridge call handler classifies in phases — `timeout` first → rejected start/provider `done` path goes to `spawn_failed` → resolved nonzero exitCode and post-start `collected.readFrom` failures go to `runner_failed`; the bridge.ts interface drops the old `pid` sentinel; test fakeHandle synchronously drops the `pid: -1` field, the description changes to "public done rejection is classified as spawn/provider failure", and a new counterexample "collected output reading failure after a resolved done is runner_failed, not spawn_failed" is added. Secret/tool isolation and #286 budget/room/date planner behavior preserved; no claim of release/publication, M5/M6 entry, Windows execution, or real supplier/HotelByte admission.
- **doctor self-repair (2026-09-10, issue #284, M4 UX engineering surface; M4 does not count toward Exit)**: `gotry_doctor`'s explicit `action: 'repair'` selects only auto-repairable gaps by stable item id (agent-reach / hbcli-missing / sidebar); browser store, credentials/API keys, profile, package reinstall, and Node upgrade remain user-action/unavailable. Production execution lazily loads `bin/gotry-bootstrap.js` and calls the existing `buildOnboardingPlan`/`runOnboardingFix`, thus inheriting the idempotence, timeout, and process-cleanup boundaries of `setupHbcli`/`setupReach`/`setupSidebar`. The scope-keyed approval gate reuses approvals within the same agent+same scope, re-asks on different scopes; rejected/cancelled is remembered for the session with zero execution; unavailable is not cached. The tool result fully returns diagnosis → selected/skipped plan → approval → repairs → recheck; an installer exiting successfully while recheck still fails yields `failed` with a `nextAction`; the sidebar report writes the recheck state. `doctor-tests` §7–§13 use isolated stateRoot, fixture installers, and actual tool registration/approval/real bootstrap orchestration across the tool boundary, without touching real installs or shared state. The default `diagnose` path stays read-only compatible. (D-38 discharged)
- **Issue #2 future-year planning window (2026-09-10; relative years 2026-09-11)**: named-year future intent — four-digit, or relative (今年/明年/后年/年内) resolved against this round's anchor — derives this round's `referenceDate` in `time-anchor.ts`; the registered tool `gotry_feasibility_check` applies a host-clock future floor by default to any dated candidate, and an explicit `planning.intent=future` request year is additionally bounded to year-end. Elapsed years do not auto-roll; expected rejections return a structured validation result without entering incident; fully dateless input keeps the old feasibility computation; historical/backtest requires explicit `historical` mode. loop continues to apply the window only to explicitly stated planning years (four-digit or the relative forms above); negated past recommendations and multi-year ambiguity are not granted the historical bypass. `time-eval-tests.ts` §6 and `smoke.ts`'s registered execute fixture verify the planning loop and the actual registered execute respectively; the reference date comes from injection/host clock; evidence is isolated fixtures, constituting no supplier or real-business admission.
- **Canonical tool-call argument boundary (PR #327 history; superseded by #3580 on 2026-09-12)**: the embedded planner now accepts exactly one canonical JSON object via one `JSON.parse`. The former identical-duplicate recovery scanner has been removed; repeated objects, nested/stringified envelopes, incomplete or garbage-wrapped input, non-object sequences, and coercions are rejected. A schema-shape-invalid call may be superseded only by a later canonical call after a typed `INVALID_ARGS` result in the same DSH run. Public-path and real local-subprocess proofs are engineering evidence, not provider reliability, HotelByte UAT, or M3/M4 business admission.

- **Safe dispatch logging (#329, 2026-09-10)**: the synchronous HTTP 409 turn-dispatch catch uses a closed reason vocabulary, distinguishing only complete fixed tokens; unknown values and suffixed tokens uniformly become `UNCLASSIFIED`; the stderr structured line contains only the existing typed `code` and `reason`; the HTTP typed response/status is unchanged. Public HTTP request and subprocess stderr byte proofs are deterministic offline evidence; they do not replace real provider, HotelByte UAT, or M3/M4/M5/M6 admission.

- **DSH runtime closure upgrade 0.1.5-rc.1 (2026-09-11)**: root/ts dual manifests + npm/pnpm dual locks upgraded from exact `0.1.5-alpha.1` (230-package closure) to exact `0.1.5-rc.1` (232-package closure: added `dsh-chunked-list` / `dsh-client-ui-sidebar-documentpreview` / `dsh-tool-present`; removed `dsh-client-ui-sidebar-textpreview`). `REQUIRED_BENCHMARK_DSH_VERSION` and `REQUIRED_DSH_RUNTIME_PACKAGE_COUNT` synced; publish preverify still fails closed on missing pins, mixed versions, or ranges. GitHub `dsh-v0.1.5-rc.2` remains deferred until the full npm family is publish-complete. No claim of GoTry npm release/tag in this slice.

- **Issue #343 timezone evolution**: flight-pack v2 generates UTC instants from explicit IANA zone and local date; the deterministic model computes durations from UTC instants and projects the home-zone work window from known instants; unknown zones and DST gap/overlap are rejected at the boundary. The dsh/mock adapter keeps the pack `homeZone`; profile provides only schedule; explicit vacation removes the work-window restriction; numeric v1 stays compatible; this offline deterministic contract does not represent live schedules/prices/availability/inventory.
- **Issue #338 persistent default origin (2026-09-10)**: the write patch accepts `homeCity` and optional `homeCityEvidence` (a single non-empty entry may be omitted; multiple require explicit exact binding); the event projection stores `homeCityPreference { value, evidence, updated_at }`; `{{motivation_brief}}` reads back only a complete typed preference as soft context; the current turn's explicit origin wins. Typed missing/malformed, evidence not in pool, or illegal timestamps all ask for an explicit origin as missing. `resolveDefaultOrigin` carries only a pure precedence contract; by default it does not hard-filter or change deterministic candidates/recommendation.
- **Issue #233 cancel/refund independent outcomes + commission disclosure contract layer (2026-09-11, M5-4 pre-entry)**: the cancel/refund/commission wording authority of write-gate design §7/§8/§10 and the fact-writegate seam lands as a pure contract module (`ts/src/booking-surface/cancel-refund-commission.ts`, run-all §62) — cancellation and refund are two outcome objects with disjoint status closed sets (a merged success state is unrepresentable; cancel-confirmed + refund-failed/pending asymmetry is expressible); "cancelled" wording requires an explicit supplier cancellation receipt while a bare submission stays "cancellation requested"; a `refunded` conclusion must bind supplier/wallet authoritative evidence and `cancel.serviceFee` can never enter the customer refund amount; commission/sponsorship disclosure (who pays / to whom / amount basis) is mandatory at the confirmation boundary, `unknown` does not default to none, and its canonical-JSON SHA-256 digest binds into the request-fingerprint field `commission_disclosure` — a caliber change is a digest mismatch that invalidates the old receipt; a token gate keeps ledger tokens and user copy consistent (compensated alone never refunds; refund pending never arrives in copy). **Contract only, default-off: zero supplier/payment call surface, zero ledger/schema change, not wired into any runtime path; the #231 → #232 → #233 implementation order and M5 Entry/Exit are unchanged — this is fixture-level engineering evidence, not real-transaction admission.**


### External benchmark generalization (Rounds 1–12, Discussion #78, official scores still empty)

> The full engineering contract lives in `evaluation/benchmark-environment-bridge.md`; this section keeps only per-round fact notes. Shared conclusion: official scores are all null — no uplift or external-benchmark-closure claims.

- **Round 1 (ChinaTravel canary feedback)**: the frozen grounding-v3 canary passed its first case to terminal state; the second case hit a 300s timeout on repeated planner tool calls — no valid 5-query aggregate. GoTry's convergence boundary sank into `tools/execute` (16 soft prompts / 18-call cap / structured refusal on the 19th + next-step text-only). The Round 1 treatment-attested installed GoTry actually ran; exact DeepSeek scored 0 for environment-unavailable/schema-invalid, GLM timed out at 300s. run-all §45 covers the Cordis integration and the packaged-binary offline E2E; CI repackages the current SHA and replays the same chain in an isolated pnpm consumer. **Follow-up (ADR-24 v2)**: real traces showed any "kill the turn at deadline" shape reproduces the same failure; the final shape = routing + wall-clock dual exit (§8.24), with the handoff-collection loop completed the same day.
- **Round 2 (environment bridge)**: default-off, owner-local config, and the host-enforced isolation contract landed (fixed argv/allowlist, minimal runner environment, recursive no-oracle key rejection, per-tool `allowed_output_keys` positive-key contract, cold-start + native definition-only, quarantine through disposal). The only frozen treatment reached terminal state but counts as diagnostic-only: provider preflight PASS, planner exit 0 / 30.826s, runner exit 3, scores all null — the agent only narrated wanting to run the CLI, produced no structured bridge call or tagged JSON, and the evaluator never ran ([evidence](https://github.com/Danceiny/gotry/discussions/78#discussioncomment-18215707)).
- **Round 3 (agent conformance)**: added a provider-neutral conformance layer (CLI/shell/Python operations in the prompt map to the single native bridge as `query.action=call`; a single tagged JSON object terminal is accepted only after a paired result succeeds; at most one fixed correction pass, format corrections no longer dispatch the bridge; the parent captures stdout with bounds and re-verifies). The new frozen treatment passed provider preflight and the runner dispatched exactly once, but planner/runner exited 1, the parent received 0 terminal bytes, the evaluator never engaged, scores null — the gap was located at benchmark child/plugin startup.
- **Round 4 (startup composition isolation)**: after config validation and before any optional host plugin resolution, the CLI projects top-level inserts into the single `gotry-tools`; insert-block/plugin-name/config-path anchor uniqueness is checked and any drift fails closed before relay; the default-off composition is unchanged. Treatment (SHA `5ebddb2`, `0e93eae` after rewrite) primary preflight=pass, planner/runner both 30.968s exit 1, released 0 bytes, scores null; the product gate used Node v24.20.0 while the treatment used v26.3.0 — diagnostic-only. GitHub Node 22/24 §48 also exposed a 30s lifecycle hang in source default-off.
- **Round 5 (headless lifecycle containment + runtime resolution)**: removed timer/keepalive preload, pinned the DSH runtime closure to `0.1.2-alpha.3`, preserved `ts/dsh-runtime/gotry-state/` state continuity, isolated the benchmark/package invocation directories, and added a benchmark-only structured diagnostics pipe (allowlisted redacted reason codes) while stdout stayed fail-closed. The frozen treatment (SHA `752e54c`, `7d6f8a9` after rewrite) stopped after 140.715s with `child_nonzero_exit`, 0 terminal bytes, scores null. Runtime invariant: the DSH name sets in package.json, package-lock, and the root pnpm importer must all be the same 216 exact direct dependencies; publish preverify fails closed on missing pins/mixed versions/range declarations; the actual DSH version is verified before benchmark spawn; the Node floor is unified at 22.15.
- **Round 6 (structured terminal diagnostics)**: the benchmark child's final `turn/end.reason` maps to a closed redacted coarse-grained diagnostic enum; per-session single-writer arbitration prioritizes bridge/conformance-specific causes; only a read-only structured `kind`, allowlisted `code`, and a bounded HTTP `status` are exposed — never message/stderr/paths/prompts/credentials. Treatment (SHA `c61600b`, `ea678f8` after rewrite; ChinaTravel `..._00001`, `deepseek-v4-flash`) stably reported `child_runtime_error` after 49.546s with 0 terminal bytes, scores null, and zero leakage/credential/endpoint scan hits — attribution ambiguity eliminated, no uplift produced.
- **Round 7 (minimal kernel)**: benchmark opt-in now keeps only the tool budget, model override, the single bridge, and tool isolation/conformance; product prompt variables/process guards/consent/ordinary tools are not installed. The CLI makes a stable task-agnostic persona projection of the system prompt; the root patch contains exactly one canonical `- insert:` and one `- id: system-prompt`, and any variant fails closed. The frozen treatment (SHA `edb9392896625adbb48abae4a2ecf968dbfc0349`; UID `e20241028160248698752`, easy, `deepseek-v4-flash`) passed preflight, then the runner exited 1 after 80.463s with terminal 0 bytes/invalid and scores null; whitelisted attribution: `child_bridge_runner_failed`. The next round targets the generic bridge tool schema and a recoverable domain-error contract.
- **Round 8 (generic flat bridge action + protocol failure inventory)**: the `gotry_benchmark_environment` parameter surface went from a query blob to three typed fields (`action` enum tools/call/errors required + `tool` + `arguments` additionalProperties:true); the closed `BRIDGE_ERROR_CONTRACT` vocabulary of bridge protocol/infrastructure failures (10 codes × recoverable/remedy) is fetchable via `action=errors`; the conformance contract surface got the same flat treatment. Stacked merges went in via #148→#151 (feat line). **Round 9 (governance-face budget and output caps, #100/#102)**: the owner-local governance environment, rebuilt pinned to registry revisions (ChinaTravel `b071db25` + sandbox en data + the official `agent_env` adapter), pinned down two real infrastructure root causes in live testing — ① when `LLM_MODEL` explicitly overrides to a relay model, dsh sends `max_tokens` with the unknown-model 256K budget; MiniMax (output cap 196608) rejects with 2013 — one of the real root causes of Rounds 2-7's `child_nonzero_exit`/0 terminal bytes: the `bin/gotry-inner.js` llm-deepseek directory entry now supports injecting `maxTokens` metadata via `LLM_MAX_TOKENS` (the dsh-llm-deepseek `defaultMaxTokens` official channel); ② the governance face's pinned 60/120s budgets are too tight for the official task shape (single query 300s): `GOTRY_BENCHMARK_SOFT_MS/HARD_MS` are now set explicitly per run, defaults unchanged, and illegal values/inverted ranges throw at startup (pinned, reproducible semantics unchanged). After the fix the treatment survived the full chain for the first time (22 requests / ~20 bridge calls / the 65536 cap in effect, MiniMax-M2.7 genuinely exploring the ChinaTravel tool surface), with the remainder converging inside budget (diagnostic finding: current reasoning models emit a `<think>` prefix even when explicitly forbidden, so a strict terminal gate structurally fails on them — terminal acceptance now strips paired reasoning blocks before validation, the strictness itself unchanged, locked by bridge-tests assertions); this does not constitute external benchmark closure.
- **Round 10 (per-tool typed result contract hardening; real `glm-5.3-flash` treatment, main `c843fae`)**: on top of Round 8's single flat `tools|call|errors` protocol, the owner-local v3 descriptor's closed/bounded `input_schema` is projected same-source into a per-tool `call` branch and reused pre-execution; `output_keys` must be non-empty and `domain_outcomes` is limited to a finite exact tuple. The adapter only accepts exact `gotry_benchmark_tool_result_v1`: every primitive leaf of a concrete result must sit under a declared key, a domain outcome must exit 0, and a non-zero exit remains an infrastructure failure; conformance takes the latest bridge response as the tagged-terminal time lower bound, rejects post-domain failures, and lets a later concrete result recover. The real treatment exposed a provider/model visibility failure: facing the top-level `oneOf` wire schema the model never formed a visible, usable tool call and emitted 57 empty `{}` calls; no countable score. This diagnostic changes no provider, scorer, evaluator, or default product path; frozen treatments and scores can only come from subsequent independent evidence.
- **Round 11 (flat model-facing bridge wire)**: only the model-visible benchmark bridge wire was narrowed: the request is a flat object, `action` is the `tools|call|errors` enum, `tool` is a tool-name enum derived from the frozen descriptor, `arguments` a generic object. Execution still validates against the selected descriptor's exact frozen `input_schema`; descriptor, adapter result/failure contract, provider/scorer/evaluator, the default product path, and the scoring basis are all unchanged. This round provides no score/uplift evidence.
- **Round 12 (exact terminal schema projection, #215)**: the Round 11 frozen treatment exposed a new bottleneck — GoTry only said "output one JSON object"; the model added its own root key and wrote itinerary rows as direct activities, and the official scorer rejected all 22 per convention without running. The structure half closed out (#217 merged): bridge config v4 carries a data-value-free closed `body_schema` (structural-keyword whitelist; enum/const/example/default and combinators are rejected outright); the same structural contract is deterministically projected into the system prompt and the single terminal correction; accepted terminal bodies are validated node-by-node fail-closed — multiple root keys / missing day·activities / wrong types / extra nested keys are rejected as-is with zero autofix; v3-and-older configs fail closed. Source tests + source/packaged E2E cover the legal ChinaTravel-like hierarchy, the five rejection classes, and single-source projection; a frozen rerun (same case/model/budget, only legal terminals entering the pinned official scorer) is the remaining segment — not run at this SHA, no treatment or uplift claimed. Engineering contract: the Round 12 section of `evaluation/benchmark-environment-bridge.md`.

## 10. Debt Register (engine-detail work may only originate here)

> Debt may only be born in this register — it must not live solely in code comments (§11 M-exit checklist item 3).
> **§10.1 = debts still open** (work to pick up starts here); paid-off debts move to the [`debt-archive.md`](debt-archive.md) archive and are not kept in this document.

The #279 Ctrip flight malformed-response gate paid off this round stays in [`debt-archive.md`](debt-archive.md); full behavior and evidence boundary in §9; the existing D-36 hotel-date gate and D-37 Dida/CfT cookie debt numbers are unchanged.

Issue #2's date lower bound and named-year window are now enforced deterministically by the time anchor and the planner/registered-tool entries: candidates with dates default to a future floor, an explicit future year adds an upper bound, and violations are rejected as structured results; fully dateless and explicit historical semantics are unchanged. No parallel time engine or Python runtime is added, so this is not re-registered as open debt.

The former duplicate tool-call argument recovery from PR #327 was removed by #3580. The current one-object parse boundary plus same-run typed `INVALID_ARGS` correction path adds no adapter-recovery debt; its public and local-subprocess proofs still do not substitute for real provider, HotelByte UAT, or milestone admission evidence.

Issue #329's dispatch-log convergence is security-boundary hardening and adds no open debt; the public HTTP/subprocess stderr proofs only prove the deterministic offline contract, not real provider, HotelByte UAT, or milestone admission evidence.

Issue #343's timezone handling is a model-layer deterministic boundary and adds no open debt: `ts/src/tz-resolver.ts` parses v2 IANA zones and local dates, `ts/src/model.ts` computes durations from UTC instants, and the adapter keeps the v2 pack `homeZone` while merging only profile schedule; explicit vacation removes the work-window restriction and numeric v1 stays compatible. The contract does not represent live schedules/prices/availability/inventory.

Issue #338 current shape: the write patch accepts `homeCity` and optional `homeCityEvidence` (a single non-empty entry may be omitted; multiple require explicit exact binding), persisted as `homeCityPreference { value, evidence, updated_at }`; `{{motivation_brief}}` provides only a soft default from a complete typed preference, the current turn's explicit origin wins, and missing/malformed or unbound evidence returns missing. `resolveDefaultOrigin` carries only a pure precedence contract; by default it does not hard-filter or change deterministic candidates/recommendations.

Issue #432 current shape is an inert contract slice, not an activation of the world2agent bridge: the adapter admits only caller-enabled, exact-tuple `w2a/0.1` core envelopes and returns untrusted metadata with opaque/free-text fields stripped. It adds no open runtime debt by itself because there is no listener, auth, consumer, network, state, process, or product switch; actual sensor transport/authentication and health/wish/ledger consumption remain under #82.

Issue #438 current shape (2026-09-12): the itinerary HTML slice landed as an internal slice — a pure bounded renderer plus a registered generation entry that writes exactly one new, non-overwriting file in the session working directory from registry-selected facts (see §1/§9). It adds no open debt row of its own. The #443 wiring slice (see §1/§9 above) adds the registered product path to the existing #450 adapter — five dsh tools bound to a host session id + absolute cwd, with bounded feedback, sticky terminals and owned reap; the real registered-browser feedback E2E and final-gate verification are covered by #443 CLOSED + PR #456 merged acceptance. Native HTML preview acceptance (opening a listed HTML entry in the host's preview) lives on #448 with proof accepted and persistent regression in `ts/scripts/dsh-artifact-web-e2e.ts` (re-runnable: `GOTRY_ARTIFACT_WEB_E2E_OUT=<dir> npx tsx ts/scripts/dsh-artifact-web-e2e.ts`). The bounded renderer + registered generation + browser coverage (navigation, native details/Return-keyboard, narrow-screen readability, escaping) are closed by #442 CLOSED + PR #449 merged acceptance; the generated document is not supplier evidence and not M3 / M4 / M5 admission.

Issue #436 is paid off and migrated to the [paid-off debt archive](debt-archive.md): the six non-hit tool results read the persisted health surface at the tool-result boundary (`readLatestChannelEvents` under the strict timestamp caliber → pure `persistedDownChannels` projection → `routingAdvice`'s explicit down set). Only `down` adds a persisted exclusion; `'ok'` recovery and expiry merely cease a persisted exclusion under the existing routing rules, `cooldown` stays a pacing state, and none of them clear a session `down`. Rows with missing, unparseable, future, or retention-expired timestamps are dropped before latest-wins so they cannot hide an earlier valid down. The default reader caliber (doctor / wish pool / probe), the 30-day retention, and the static persona routing card are unchanged. #432 stays inert and real #82 sensor activation stays gated.

### 10.1 Open (working face)

> **#254 current boundary (2026-09-11)**: the read-only `repair-plan` slice plus the authorized `repair-apply` / `repair-rollback` execution face are in-tree (`ledger-repair-plan.ts` / `ledger-repair-apply.ts`, run-all §29b/§29c). Apply requires the triple gate (`--mapping` + matching `planDigest` + `--i-authorize-apply`), forces a checksummed backup before writes, CAS-updates `tenant_id` while preserving `seq`, rebuilds affected tenants, and is idempotent via applied stamps. Owner checklist and receipt schema: [`ops/ledger-tenant-repair.md`](ops/ledger-tenant-repair.md). Closing #254 still requires a separate founder-authorized real (or confirmed-unnecessary) repair receipt — fixture green ≠ real repair; not M5/M6 gate evidence.

| Debt | Status / redemption timing |
|---|---|
| Future SDK direct-connect descendant cleanup trigger | **Trigger-gated, not current product runtime**: SDK direct transport (`dsh-sdk-client`) creates no process group, so runtime descendants may survive a killed SDK-owned leader. The current GoTry CLI launcher has its own tested process-group cleanup (the closed #271 boundary); a future direct-connect product path must not silently inherit CLI guarantees. Activation requires (a) a concrete proposal to put the dsh SDK direct-connect form into a product runtime, followed by (b) the recorded architecture/founder decision on upstream ownership. Until both arrive, this row is a watch/trigger tracker — it does not authorize a vendor fork, production activation, dependency upgrade, or an upstream change. Preserves the scoped #271 acceptance; does not change real M4/M5/M6 admission. (public tracking = [#422](https://github.com/Danceiny/gotry/issues/422)) |
| D-37 CfT/Chromium build's cookies API cannot see HttpOnly cookies | **Empirically confirmed 2026-09-09** (dida live E2E SW probe: the same browser's CDP sees `CN_M_DidaTravel`, while the extension's `chrome.cookies.getAll` filtered in all forms returns only the non-HttpOnly `dida-locale`; branded Chrome 144 works, verified on the ctrip lane). Impact: on CfT, login quick-checks misreport `needs-login`; search/sniffing is unaffected (page requests carry the browser's own cookies; the extension only forwards response text). The live E2E uses the `allowAnonymous` self-check state plus a driver-preconditioned GUI login to skip the gate. Redemption: an upstream Chromium fix, or a validated branded-Chrome + store-extension combination; accelerated if a second adapter depends on an HttpOnly ticket-name gate. Anchor `scripts/dida-sw-probe.ts` (public tracking = [#272](https://github.com/Danceiny/gotry/issues/272)) |
| D-13 session adapter maintenance face (RFC user-session-data-rfc) | See "D-13 session adapter maintenance face" below (public tracking = [#272](https://github.com/Danceiny/gotry/issues/272)) |
| D-15 ledger trigger-based backends (ADR-15 TS-5) | Litestream cloud backup / cr-sqlite multi-writer replication / RFC(loopx) §6.5 claim-fence-receipt multi-user implementation — starts only when a trigger appears: a second real user / multi-machine deployment / an AaaS initiative (public tracking = [#275](https://github.com/Danceiny/gotry/issues/275)) |
| D-18 M3 Exit real-cohort evidence gap | See "D-18 M3 Exit real-cohort evidence gap" below (public tracking = [#22](https://github.com/Danceiny/gotry/issues/22)) |
| D-19 M4 real repeat-cohort gap | **The evidence contract has landed and was hardened on 2026-09-08 via #223/#228**: Issue #20's fixture scorer pins the paired/active-planning/reflux/provenance/P4 basis; synthetic fixtures never count toward Exit; thresholds frozen at N≥5 and median reduction ≥0.5 (raw ratios compared, rounded only in reports); layer-by-layer exact schema and HMAC-SHA256 pseudonym keys fail closed; observed-private additionally requires the `memory_value_source_review.v1` manual source-review attestation contract with a `reviewed_summary_digest_sha256` matching the current summary. The #228 collector provides only explicit-consent, isolated-stateRoot first-visit/return lifecycle collection and candidate/synthetic de-identified export; it never elevates to source_review. Redemption = a private `observed_private` cohort at N≥5 with the source-review attestation contract bound to the summary digest and producing a de-identified summary; with no real samples, missing manual review, or a digest mismatch, the state stays candidate/waiting/backoff/no-spend — the schema is never widened to fake progress. (public tracking = [#20](https://github.com/Danceiny/gotry/issues/20)) |
| D-22 pending_writes empty receipt lacks a physical CHECK (known boundary of booking_saga_fsm.v1) | The vocabulary-layer audit chain already covers it (`sagaTraceViolations` reports a violation on an empty receipt, run-all §36); **redemption timing = the M5 Entry decision**: pending_writes gains a `receipt non-empty CHECK` in a schema upgrade plus a named seam vocabulary freeze (`design/booking-saga-fsm.md` §4); the write path is untouched before M5 Entry (M5 Entry is governed by [#136](https://github.com/Danceiny/gotry/issues/136); successor = [#231](https://github.com/Danceiny/gotry/issues/231)). **Outbox-side progress (2026-09-11, #231 first slice)**: the dispatchable surface (`write_effect_intents`) now carries storage-level non-bypassable constraints — non-empty receipt CHECK, composite FK to `approval_claims`, claim-field CHECKs, forward-only dispatch trigger, immutable attempt trigger — proven by raw-SQL negatives in run-all §61; the `pending_writes` CHECK and seam-vocabulary freeze themselves remain the M5 Entry decision and are not claimed closed here |
| D-26 fact-gate coverage gap (ADR-19) | **Partially closed**: hotels, policies, flight/train prices, and the non-keyword lodging/limited policy vocabulary have entered the gate via the slices listed above; **#299/#355** completed flight/train claim triage and `gotry_session_search kind=train` typed rail facts: the parser distinguishes empty/nonempty/malformed/transport, mixed malformed rows fail closed, positive facts are bound to the host-captured invocation route/date/batch and the exact response URL, in-row startTrainDate is validated and kept, collection time must be non-future and within 15 minutes, `canWebBuy=Y` and the closed seat-availability token set are kept, lists never backfill fares, and only genuine emptiness generates negative facts; **#359** extended anchored policy-row content fingerprints from "check as_of only" to all fields plus a whole-row structural guard (single anchor, strict whole-row match, non-empty pre/post context, duplicate/malformed anchors fail closed), bridged `itinerary.trip_start` to `opts.tripStart`, and kept both legitimate forms of `review_by` and `tripStart`-derived reminders; **#363** completed field fingerprints for anchored flight/hotel rows — flight rows re-extract the `flight_no` token requiring strict equality with the fact, hotel rows must strictly contain `destination + check_in + check_out`, any mismatch yields `fact_anchor_unknown`, and this **does not replace strict whole-row comparison** (root contract item 2); §4b price-compatible forms/trains/policy canonicalization reuse the existing decision primitives; along `loadFactRegistry` + `gateArtifact`, no new execution surface; fact-root counterexamples = forged UO999/forged destination/forged date. Existing unknown anchors still fail closed; reverse extraction is still a regex heuristic; price tolerance/exchange-rate/multi-currency policy (price tolerance stays with the existing R2 founder decision and is **not** transferred to #344; exchange-rate/multi-currency is tracked under [#344](https://github.com/Danceiny/gotry/issues/344) — CNY-only settlement today; the first real non-CNY supplier quote, user budget, or destination demand triggers implementation per [`docs/data-sources.md`](data-sources.md) §2) and M5 WriteGate remain subject to the existing debt for re-review. This entry records only isolated deterministic fixture and local gate evidence — it does not represent live 12306/supplier inventory or prices, nor M4/M5/M6 admission or full closure of D-26 (public tracking = [#381](https://github.com/Danceiny/gotry/issues/381)/[#299](https://github.com/Danceiny/gotry/issues/299)/[#355](https://github.com/Danceiny/gotry/issues/355)/[#359](https://github.com/Danceiny/gotry/issues/359)/[#363](https://github.com/Danceiny/gotry/issues/363)) |
| D-28 external-benchmark-driven agent generalization evidence gap | See "D-28 external-benchmark-driven agent generalization evidence gap" below (public tracking = [#203](https://github.com/Danceiny/gotry/issues/203)) |
| D-29 Booking Copilot real-inventory product acceptance | The typed read-action/BFF/task ledger and reproducible Linux artifacts only prove engineering boundaries; #282/#3580 additionally pin same-run typed schema correction and the bounded prose-only nudge budget; #467/#468 add the embedded planner persona target and the host-driven workspace validation (for `search.run` and `search.patch` the runtime validates the host's post-action workspace — a higher revision requires cleared hotel focus, loaded offers, shortlist, selection, and verification; an unchanged revision permits no content mutation; missing optional fields compare safely). None of this proves anything about supplier inventory, unbookable recovery, or Checkout/order-state business effects. **Open**: after freezing exact SHAs of the three repos, run real inventory across the tenant/customer/storefront/payment-link four surfaces; at least one unavailable/changed quote must recover via re-search, a new CheckAvail, and restore of the original Checkout; Book remains authorized only by Checkout, closed out with QueryOrders/cleanup evidence (public tracking = [#142](https://github.com/Danceiny/gotry/issues/142)) |
| D-33 M4→M6 program evidence collection and gate closure gap | Issue [#225](https://github.com/Danceiny/gotry/issues/225) has landed the task graph in `docs/design/milestone-delivery-plan.md`; the M4 scorer (#238), explicit opt-in collector (#248), tenant ledger/CLI, and the Z3/map stability foundations are all in main, and #227/#241/#242 are closed. The real remaining gaps: ① the M3 50–200-person cohort and the M4 `observed_private` N≥5 repeat cohort/reflux baseline; ② protocol/Buyer/routing/reconciliation/UAT-signing or internal authorization evidence for M5's first supply chain `hotelbyte-cli`; ③ P6 founder approval and a real pilot contract; ④ successors execute within each issue's admission scope: [#136](https://github.com/Danceiny/gotry/issues/136)/[#137](https://github.com/Danceiny/gotry/issues/137) — explicitly authorized design, read-only investigation, fixtures, and failing-before work may proceed before Entry, while transaction runtime, supplier writes, and real B2B paths require their respective Entries. The M5 proposal is at `docs/design/write-gate-production-design.md`; transaction/B2B implementation stays sealed until then. (governance tracking = [#270](https://github.com/Danceiny/gotry/issues/270); real gates = [#20](https://github.com/Danceiny/gotry/issues/20)/[#22](https://github.com/Danceiny/gotry/issues/22)/[#136](https://github.com/Danceiny/gotry/issues/136)/[#137](https://github.com/Danceiny/gotry/issues/137); successors = [#231](https://github.com/Danceiny/gotry/issues/231)/[#232](https://github.com/Danceiny/gotry/issues/232)/[#233](https://github.com/Danceiny/gotry/issues/233)/[#234](https://github.com/Danceiny/gotry/issues/234)/[#235](https://github.com/Danceiny/gotry/issues/235)) |
| D-39 ground-transfer data boundary | The offline boundary is closed and mechanically pinned (2026-09-11 boundary-freeze suite, run-all §3b): explicit-coordinate driving routes through the public `map_driving_route`, separately bound arrival (A→B) and return (B→A) directions of the named static `taxi` transfer with per-direction isolated cache/fallback, `minutesOut`/`minutesRet` override consumption, static price preservation, mode/position vocabulary closure (only `mode=driving` at `position=destination`; the seam's transit/walking/bicycling routes are refused, never disguised), a complete per-direction fallback taxonomy (provider error/miss — including self-contradictory route facts such as positive distance with zero duration — stale re-query failure, mode/position mismatch, invalid coordinates, binding mismatch), the static↔dynamic switch contract (fixed 900-second TTL boundary, identity-minutes provenance switch, bounded per-direction cache), and evidence-annotation completeness (static price label on every path, `trafficStatus: not-live-route-estimate`, host-observation `asOf`). The remainder is gated real-data work, deliberately no-spend until separately authorized: live traffic, transit/rail, fares, address resolution, and broader transfer combinations. The slice does not change the #20/M5/M6 gates; the follow-up boundary is tracked at [#429](https://github.com/Danceiny/gotry/issues/429). The closed #341 first-slice acceptance (explicit-coordinate driving with the named static `taxi` transfer, per-direction cache/fallback, vocabulary closure) stays historical; every wider path admits independently with its own named product use case, data source, and access boundary, and a public read-only route lookup is not itself gated by M5 transaction Entry while any write path retains the existing M5/WriteGate admission. |


**D-13 session adapter maintenance face (RFC user-session-data-rfc)**

- **#335 offline aggregation subtask**: the sf-01..08 summary now selects a single coherent batch by defensible batch identity and batch collection time; producer-filename batches keep each query's independent `started_at`, source/fallback/provenance are preserved, and the newest corrupt/incomplete batch never falls back to an older success. This subtask closes only the offline aggregation defect — not #272's real browser/session or packaged connected/degraded evidence.
- **#411 challenge-stop subtask (2026-09-12)**: the sf-01..08 live benchmark batch stops at the first `challenged`/`guard_violation` (RFC §3.5), preserves the structured `challenge_stop`/`no_spend_stop` semantics, and writes an auditable batch (`stop_reason` + attempted/not_attempted lists); `batch_complete` is false for challenge/guard termination even when all eight records exist. `sf-summary` marks selected challenge/guard evidence fail-closed via `challenge_stop_detected`, including legacy top-level `sessionVerdict`. Production cadence remains 35 seconds; only the isolated harness mocks child timers. Offline engineering evidence only (run-all §44c); real browser/session calibration remains #272's.
- **#308 forward fix (2026-09-10)**: the authorization gate and `gotry_session_search` execute share `interpretArgs`' query-first kind selection; the default is flight, unknown/malformed fails closed, and train remains constrained by off/ask/allow and per-station rejection/cancellation. Standard evidence: session-tests §I + smoke §13.

- **Partial payoff 2026-08-30**: the action cache + golden-standard inputs + the #21 field fixture scorer/dual-source shape gate have landed; the transport layer settled on the extension bridge (§38 anti-drift tests lock Node constants and extension code together); Issue #67 gained a `--golden=static` offline comparator (OpenFlights fixed route/carrier + manual schedules/price bands, requested/effective/provenance/fallback auditable, §44), but it is not a live-availability source and does not reduce the Ctrip batchSearch-revamp risk; real sf-01..08 session evidence still depends on the user's extension connection, degrading explicitly per the existing channel when a site breaks.
- **Partial payoff 2026-09-03 (12306 first-party calibration)**: the rail-12306 adapter no longer waits for "calibration after the first real session" — the telecode table was checked entry by entry against the official `station_name.js` full station table (32-city starter set → 129 cities; this once caught the Nanning NIZ→NNZ wrong code), and the seat-bucket indexes and station-name mapping come from the official frontend `queryLeftTicket_end_js.js` cN(result,map) conversion function (the old public folklore indexes disagree with the official current mapping — corrected before the first query); the `data/stations-12306-verify.json` snapshot + session-tests section L anti-drift assertions lock it in. Ctrip-side interface calibration still awaits a real session.

**D-18 M3 Exit real-cohort evidence gap**

**In progress (Issue #22)**: the public face already has a frozen manifest, a strict de-identification schema, a deterministic scorer, and synthetic-fixture guarding; the nightly real-LLM evidence producer has entered the engineering face (sealed prompt set/price table, zero-write waiting without credentials, budget gate) — the mechanical preconditions for acceptance ⑥ "nightly rerunnable" are in place, with real nightly records awaiting a credentialed environment run. Private real samples have not yet entered `ts/gotry-state/evidence/m3/`. The business bar may be met only when a 50–200-person real cohort simultaneously reaches completion rate ≥40%, NPS ≥40, POI hallucination rate <1%, and in-window rerunnable nightly real-LLM evidence; M3 Exit stays open without samples. Anchors: `product-metrics.ts`; `nightly-evidence.ts`; run-all §33/§35.

**D-28 external-benchmark-driven agent generalization evidence gap**

- **Current state**: ChinaTravel grounding-v3's frozen 5-query canary has produced only 1 scoreable terminal state; the second case hit a 300s timeout in repeated planner tool calls — there is still no valid 5-query aggregate. Round 1–12 per-round facts in §9 "External benchmark generalization"; Round 10's `glm-5.3-flash` treatment was diagnosed on main `c843fae` as a top-level `oneOf` visibility failure (57 empty `{}` calls, no countable score); Round 11 only adjusted the model-facing flat wire; Round 12 closed the structure half (config v4 closed body schema); there is still no attributable official score/uplift.
- **Sub-debt: Phase 0→Phase 1 adapter admission (open)**: the Phase 0 foundation contains contracts/registries/validators, unmatched diagnostic fixtures, and a deterministic cadence planner (no scheduling/spend/scoring/baseline/uplift validity); every adapter, external runner, baseline, and matched production-evidence path still needs its own approved plan/PR and must pass the license/evaluator/source-fence controls of [`evaluation-foundation.md`](evaluation/evaluation-foundation.md).
- **Redemption order**: source/installed startup+conformance contracts and the full regression passing → a newly frozen case reaching `exit=0` under the no-oracle boundary with a non-empty terminal, planner<300s, and the evaluator executing → the original manifest 5/5 terminals with schema/forbidden/seven-metric completeness → extending to other public benchmarks per the registry and attributing holistically after 3–5 independent optimization PRs; no score-improvement or external-closure claims before matched evidence.

## 11. Freshness Mechanism (sync discipline between docs and reality)

**State-face list** (only these 6 places in the whole repo record "current state"; every other document cedes status): ① this document §1 current shape; ② this document §9 evolution; ③ this document §10 debt register; ④ `roadmap.md` current position; ⑤ `README.md` current shape; ⑥ the `design/stage1-top-down-design.md` status header.

**Same-commit sync rule**: any commit that changes the system's current shape/state/debt must synchronize all state faces in the same commit — `bb880f3` (M1 exit) touched only §1 and the ADR table, and four state faces lagged one commit cycle; this section exists because of that.

**M-exit freshness checklist** (reconciliation items for a milestone-exit commit, results attached to the commit message):
1. All 6 state faces synchronized (or explicitly ceded, naming the cession target);
2. Every ADR in the full table checked against its "retirement/re-review condition"; triggered ones get an issue opened or a status change on the spot;
3. Debt-register closures and additions — debt may only be born in this register, never solely in code comments; closed items move to the [`debt-archive.md`](debt-archive.md) archive;
4. Count-type statements (ADR counts, test counts) become references, not numbers — numbers rot;
5. Acceptance evidence is rerunnable: fixture/script commands go into the commit message.

**Re-review cadence**: driven by events, not calendars — M-exit reviews the full table; triggered retirement conditions (solving >500ms, 20 differential runs with no divergence, bridge latency >500ms) trigger a review any time.

## 12. Document Map

Organizational rules (directory taxonomy/naming/header blocks/lifecycle) and the master index: [`README.md`](README.md). The table below is grouped by directory:

| Document | Concern |
|---|---|
| This document | Technical: system/modules/model/loops/data overview/ADR/evolution/debt |
| `roadmap.md` | **Sole source of the timeline**: M0–M6 milestone three-line view and legacy-model consolidation |
| `gotry-master-outline.md` | Program: work breakdown/reuse matrix/decision gates (master outline) |
| `gotry-product-design.md` | Product: main loop/transparency mechanisms/full cost/shared experience |
| `data-sources.md` | **Sole data-source authority**: domain matrix/four-layer architecture/Google Place pipeline/evidence-chain contract/TREK reference |
| `tech-strategy.md` | Tech selection & half-year iteration route (M2–M4): selection matrix/evaluation system/division of labor/continuous optimization loop/decision register |
| `tokens.md` | **Sole token authority**: npm three paths (web session/granular bypass/OIDC) + agent-reach 8-channel acquisition table + unified .env storage |
| `user-guide.md` | End-user getting-started guide (dsh-form usage) |
| `tools.md` | **Tool reference**: 23 registered tools grouped with per-tool contracts/degradation behavior + channel routing (the registry only suggests, never dispatches) + web onboarding (#258/#267) and operator scripts |
| `release-notes.md` | Release records (archived per version, newest first) |
| `decisions-needed.md` | Decision list awaiting the founder |
| [`debt-archive.md`](debt-archive.md) | Paid-off debt archive (append-only evidence; open debts and the working face live only in §10.1 of this document) |
| `design/memory-design.md` | **Memory domain design**: six-layer C-end redesign (M1–M6 status mapping/P1–P4 phased increments/iron rules and acceptance), the formal document of M4's "six-layer framework redesign" deliverable |
| `design/memory-lifecycle-collector.md` | **M4 planning lifecycle collector usage contract** (#228/#248): explicit opt-in CLI, isolated stateRoot, HMAC/consent/source/wait freezes, JSONL+manifest atomic persistence, and the #223/#238 scorer export chain; produces candidate/synthetic only, never replaces the real cohort |
| `design/milestone-delivery-plan.md` | **M4→M6 living task graph (issue #225)**: splits by real dependency — M4 #223/#238/#228/#248/#20, the ledger/state-cli/Z3/map foundations already in main (#229/#237/#243/#244/#245), M5 #136 HotelByte first supply chain + WriteGate (#231/#232/#233), M6 #234/#235/P6/pilot and open-source quality gates — each with responsibility face/responsible file/E2E/falsification/exit criteria; also tracks the quality/follow-up lines #254/#255 (#257 was closed via PR #264) (non-blocking for M5/M6 Entry) |
| `design/write-gate-production-design.md` | **M5 WriteGate production proposal (issue #225/#136)**: HotelByte versions/artifacts, trusted receipt issuance/consumption authority, approval_claims persistence, local outbox intent (no external exactly-once claim), query miss stays unknown, supplier unknown/manual reconcile/cancel-vs-compensation/L4 revoke/commission disclosure; design only — transactions stay sealed |
| `design/effect-interpreter.md` | **Effect interpreter design (issue #16 adopted, ADR-18)**: effect_interpreter.v1 vocabulary (effect values/EffectOutcome/trace) + channel resilience strategy table (backoff/circuit-breaker/cadence, line by line) + production/mock dual interpreters + the decision record on why visual CUA and automatic multi-channel routing are not done + the D-23 migration face |
| `design/booking-saga-fsm.md` | **Booking saga state machine design (issue #17 adopted, ADR-17)**: booking_saga_fsm.v1 alphabet/edge table/rejection closed set + three edge-type vocabularies (deterministic/gate/external-event) + the suspend-resume shape of HITL approval + M5 unsealing increments and the decision record on not introducing an orchestration framework |
| `design/tool-orchestration-design.md` | **Tool orchestration & channel health face design (proposal, 2026-09-03)**: closes issues #106/#107/#108 — channel registry (single source of data truth) + channel health face (doctor's persistent face + session transients) + DP orchestration = health-state-driven dynamic suggestions; answers both propositions of "sustained tool-calling performance under ordinary LLMs" and "open-ecosystem extensibility"; decision points = decisions-needed D-7/D-8/D-9 |
| `design/adapter-authoring-guide.md` | **Session adapter author handbook** (D-13): four-step method (probe → first-party golden standard → dual-source shape gate → drift lock)/integration checklist/discipline red lines/Ctrip real-session calibration checklist |
| `design/external-event-seam.md` | **External event seam design** (#82-compatible direction/D-31): events as a new producer for the health face and wish pool; trust grading and landing sequence (trigger-driven, consuming existing seams without building a new runtime) |
| `design/hotelbyte-skills-design.md` | hotelbyte-skills architecture (knowledge enters the repo / execution stays in gotry, issue #5) |
| `design/stage1-top-down-design.md` | Stage 1 detailed design and implementation order (its status header = §11 state face ⑥) |
| `rfc/transactional-state-rfc.md` | **RFC (accepted 2026-08-28, ADR-15)**: transactional state foundation — industry durable-execution research converging on the five-piece set + GoTry's landing architecture + the TS-0..TS-5 execution plan and decision records (D1–D5) |
| `rfc/user-session-data-rfc.md` | **RFC (approved 2026-08-28)**: user session data plane — official channel first + user-session backfill, four-phase landing (P0–P4) and decision gates (G7/G8/G9 settled) |
| `rfc/loopx-inspired-upgrades-rfc.md` | **RFC (accepted 2026-08-27)**: mapping upgrades from loopx's 13 architecture RFCs — four seams (S1 tool packet discipline/S2 memory utility sidecar/S3 wish outreach 0..1 discipline/S4 WriteGate L0–L4 vocabulary) |
| `research/kimi-postmortem.md` | Counter-example textbook and ground-truth extraction |
| `research/maka-research.md` | Apache Maka (Incubating) research → point-by-point comparison with ADR-15's transactional state foundation (durable-execution mechanics/adoptable surface; research draft for founder decision) |
| `research/deerflow-research.md` | DeerFlow research → gotry optimization goals T1–T4 (issue #10) |
| `research/dsh-plugins-shortlist.md` | dsh community plugin selection (awesome-dsh-plugin full survey, issue #9) |
| `research/enterprise-travel-reference-study.md` | **An enterprise travel-agent system, eight-dimension reference study (2026-09-03, source anonymized)**: dual-track execution/write closure → M5 design input (ADR-17/S4 seam); compliance-closure decorators/domain skills → M6 input; production-grade state → reference when D-15 triggers; dual-model/context compression → not adopted (ADR-24 already covers it); the differentiated keep-list and the "no pursuit of code-volume parity" decision |
| [`evaluation/evaluation-foundation.md`](evaluation/evaluation-foundation.md) | Evaluation Phase 0 contracts, registry ownership, aggregate admission, and non-uplift boundary |
| `evaluation/benchmark-environment-bridge.md` | External benchmark bridge (Phase 1 seam, default-off) + the per-round engineering ledger |
| `evaluation/e2e-prompts.md` | dsh end-to-end real-LLM verification records (§1–§11: wrapper/clarification cards/background checks etc.) |
| `evaluation/persona-bench/` | Product-persona comparison: archived answers to the same real itinerary prompt/scorecards/persona feedback |
| `milestones/demo-plan-2026-07-17.md` `milestones/demo-reconciliation.md` | Demo deliverables and reconciliation |
| `milestones/m2-capability-gap.md` `milestones/m2-flight-data-options.md` | M2-era capability gaps and flight data selection (historical memos) |
| `milestones/m3-web-gap.md` | M3 web-form gaps (G-1..G-4 directions) |
| `milestones/m4-calibration-questions.md` | M4 calibration question set |
| `milestones/m6-b2b-reuse-walkthrough.md` | M6 P6 B2B reuse walkthrough minutes (draft): traveler principal/sponsor/BFF principal tokenization, measured reuse-rate basis, tenant adversarial cases, disclosure-plugin proposal; pending founder review |
| `milestones/s1-walkthrough.md` `milestones/g1-market-memo.md` | Stage 1 walkthrough and the G1 launch-market memo (historical memos) |
| `ops/extension-webstore-submission.md` · `ops/extension-privacy.md` · `ops/external-pr-workflow.md` · `ops/ledger-tenant-repair.md` | Chrome Web Store submission materials and privacy policy (ADR-21 channel B); maintainer-side workflow for external PRs (including automation bots); #254 ledger tenant repair owner-gate checklist |
| `assets/` | archify-generated system architecture diagrams (tool artifacts, no in-repo consumers) |
| `superpowers/` | superpowers workflow plans/specs (evaluation plan, tool-managed) |
