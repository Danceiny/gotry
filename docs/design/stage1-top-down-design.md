[English](stage1-top-down-design.md) | [简体中文](stage1-top-down-design.zh-CN.md)

# Stage 1 Top-Level Design: Top-Down (Contracts → Loop → Wiring Intelligence to Reality)

> **Status snapshot**:
 - Issue #343 current status: flight-pack v2 parses UTC instants using an explicit IANA zone and local date, rejects unknown zones and DST gap/overlap, and computes durations from the UTC instant; the dsh/mock adapter keeps the pack `homeZone`, the profile only provides schedule, explicit vacation removes the work-window restriction, and numeric v1 stays compatible. This deterministic contract does not represent live schedules/prices/availability/inventory; see `../data-sources.md` for the boundary.
 - From 2026-09-10, the #341 ground-transfer first slice adds an adapter in front of `gotry_feasibility_check` that takes explicit coordinates `mode=driving`: the registered public `map_driving_route` produces route facts with `asOf`/freshness/cache/provenance, covering only the minutes of named static `taxi` destination transfers; `bus`/`bus_plus_taxi` keep static values, and static prices remain `[静态包:估算]`; provider/coordinate/mode/binding failures all fall back to static explicitly. `asOf` is the host observation/query time; live traffic and transit/rail/fare are outside this slice, and the existing #20/M5/M6 gates are unchanged.
 - From 2026-09-10, Issue #2 future planning with a named year derives the current turn's reference date in `time-anchor.ts`; the registered tool `gotry_feasibility_check` applies a host-clock future lower bound by default to dated candidates even when planning is omitted, and applies a year-end upper bound when a future year is explicit; expected rejections return a structured validation result instead of entering an incident. Fully dateless candidates keep the old feasibility computation, expired years do not roll over, and history/backtesting requires the explicit `historical` mode; the loop applies the window only to explicitly named-year plans, and negative past recommendations plus multi-year ambiguity are not misjudged as historical. `time-eval-tests.ts` §6 and the `smoke.ts` registered execute fixture verify with an injected clock; this is isolated engineering evidence only and does not replace supplier or real business admission.
 - From 2026-09-10, artifact views enter the M4 queue (issue #285): the Host persists `presentationMeta`; the public `./client` adapter registers `tool.call.toolview` keyed cards for `gotry_artifacts_list/read` via `window.__ModuleLoader__.load`, showing clickable paths, line numbers, source identity, and content version per runtime `block`, with workspace/sidebar as an additional preview surface. The read-scope allowlist = stateRoot root + session working directory (excluding node_modules/.git); the extension allowlist = text types; cross-root / symlink escape / missing file / oversized file (>2MB) uniformly return ok:false + error + hint. This layer is read-only; the WriteGate red line is not involved. Acceptance evidence = `scripts/artifacts-capability-tests.ts` 12-item isolated fixture proof + `scripts/dsh-artifact-web-e2e.ts` fresh-profile Web list→select/open→read→edit→updated-read proof + smoke §15/§15b.
- From 2026-09-10, PR #327 revisions tighten duplicate tool-call argument recovery in the embedded planner: ordinary single objects still go through `JSON.parse`; recovery happens only for at least two complete top-level objects separated by whitespace only and deep-structure equal; prefix/trailing garbage/truncation/conflict/non-object sequences/a single invalid object fail closed, and string braces and escapes are handled correctly by boundary scanning. The public runPort fixture is deterministic local evidence only and does not constitute real provider reliability, HotelByte UAT, the M3/M4 cohort, or M5/M6 admission.
 - From 2026-09-10, #329 tightens logging of synchronous Booking HTTP 409 dispatch rejections: stderr contains only the typed `code` and the exact closed `reason`; unknown/suffixed values are unified to `UNCLASSIFIED`. The public HTTP + subprocess stderr proof is deterministic offline evidence only and does not constitute real provider reliability, HotelByte UAT, the M3/M4 cohort, or M5/M6 admission.
 - From 2026-09-10, Issue #338 motivation writes accept `homeCity` with optional `homeCityEvidence` (a single non-empty evidence may be omitted; multiple entries require explicit exact binding), persisted as `homeCityPreference { value, evidence, updated_at }`; `{{motivation_brief}}` reads back only the complete typed preference as a soft default, an explicit origin in the current turn takes precedence, and a missing/malformed one or evidence outside the pool requires an explicit departure city.
- From 2026-09-10, #270 unifies the public flow per `../ops/external-pr-workflow.md` §0: public issue kickoff → Draft PR → exact-head review → merge/destination receipt, and points the `../architecture.md` §10.1 active debt at the public tracker/triggers; the founder-authorized in-repo Claude lane is not subject to the external bot T0/T1 veto but still goes through normal review. Local/fixture proofs do not change the real gates of #20/#22/#136/#137.
- From 2026-09-10, #194 A-track GoTry-owned mitigation: a typed `tools/pre-execute` direct-child guard recognizes continuable durable ids ahead of the dsh jobs registry and returns recovery guidance via completion notice/`list_agents`/`send_message`; non-owner/one-shot/no-match cases keep the native jobs error. The upstream dsh unknown-id generic contract remains open; the local guard does not touch the vendor and does not close the issue; the focused proof is `issue-194-job-id-guard-tests.ts`.
- From 2026-09-10, #284 `gotry_doctor action=repair` forms the chain diagnose → item-scoped plan → session-scope approval → the existing bootstrap idempotent installer → actual re-check; rejection/cancel/no approval channel means zero execution, user-action/unavailable do not overstep authority, and the sidebar report writes the re-check state. Isolated fixture tool E2E counts as M4 UX engineering evidence and does not count toward the #20 real repeat cohort or M5/M6 gates.
- From 2026-09-10, #279 Ctrip (携程) flight batchSearch parsing/search boundaries distinguish a legitimate empty `miss`, a valid `hit`, and a malformed `error`; the extension/CDP two lanes keep challenge priority. Isolated fixtures prove local parsing/orchestration only and do not satisfy #272 live interface calibration, real supplier evidence, or M4/M5/M6 admission.
- From 2026-09-10, #352 FlyAI `data.itemList` flight/train/hotel boundaries distinguish an exact empty-list `miss`, a complete legitimate list `hit`, and an overall `error` when any item is malformed; the latter drops valid siblings and does not enter negative-inventory facts. `flyai-tests.ts` is offline proof of an isolated fake CLI/production effect/fact sidecar only and does not constitute real provider/UAT or issue closure.
- From 2026-09-10, #335 the offline sf-summary subtask selects a single batch by canonical producer filename, processes newer/older evidence by batch collection time, and keeps per-record `started_at` plus requested/effective/fallback/provenance; it fails closed when the newest batch lacks a query or contains corrupt JSON, and legacy records without batch identity are recorded as unknown/ineligible. The run-all §44b synthetic CLI proof does not satisfy #272 live session/calibration or the packaged connected/degraded gate.
- From 2026-09-09, #283 the hotel date gate reuses the shared `parseAbsoluteDate` real calendar validation and, at the hotel consumption boundary, rejects missing dates, overflow, and wrong ordering; failures do not dispatch and return `input_required`. Authoritative description in `docs/architecture.md` §1.2/§10 D-36 and issue #283; this isolated engineering evidence does not constitute real supplier admission.
- From 2026-09-09, #290 public contract compatibility: persona projects through `personaPrefix` / `personaSuffix` (legacy `persona:` does not project); bridge handlers get structural classification (`timed_out` / `spawn_failed` / `runner_failed`); see `docs/architecture.md` §9. No claims of release/publication, M5/M6 entry, Windows execution, or real supplier/HotelByte admission.
- From 2026-09-10, #271 outer liveness closure: the GoTry launcher puts the dsh child into its own POSIX process group; exit/error/close (including delayed close with inherited stdio) and parent-only SIGINT/SIGTERM go through bounded TERM→KILL, preserving the original termination semantics after confirming the direct child/group is cleared, and the existing incident observer/writer remains the only record surface; benchmark zero-exit continues through the original stdout drain/parser. The maintenance proof for the actually installed dsh `0.1.5-alpha.1` on Node24 uses the real package-shaped `bin/gotry-inner.js` to cover nonzero exit, normal zero exit, and a TERM-resistant descendant, while keeping child crash, rejected promise, spawn ENOENT, and SIGINT/SIGTERM, and checks parent/leader outcome, stderr, product incident, post-failure marker, and before/after descendant/group cleanup. dsh SDK direct transport, the internal supervisor, and the real deployment contract remain open TODOs; this slice does not change vendor/lock, does not install a daemon, and does not raise any real milestone gate.
- From 2026-09-09, #282 Booking planner correction: the embedded read-action planner keeps non-empty occupancy rooms and childAges, and a missing `adults` still goes through schema validation; correction prompts count against the same provider budget of at most three, a valid correction returns along the same authority path, and provider failures pass through. The focused proof uses an injected runPort; real supplier inventory/UAT remains open.
- From 2026-09-09, Node 26 dist build compatibility (issue #265, M4 open-source/release quality line): the support floor stays `>=22.15.0`; the root build generates ESM using exact TypeScript 5.9.3 only, CI keeps typecheck + full-stack regression on Node 22/24, and additional Node 22/24/26 focused proofs verify exact source→dist, JS/Python/data assets, no relative `.ts` specifiers/no CommonJS emit wrapper, skeleton-check, and key dynamic imports. This item does not count toward the #20 real repeat cohort and does not satisfy #136 supply agreement/internal authorization or #137 P6 approval and the real pilot; all three gates remain open.
- From 2026-09-09, interactive onboarding at web startup (issue #258/#267, M4 UX engineering surface, D-34 cleared; #267 = post-merge hardening after #266 was merged): optional capability configuration before `npx @danceiny/gotry web` starts (evaluated once per qualifying startup, asked at most once, no persistent confirmation across startups), reusing the `doctor --fix` idempotent installer; it asks once only for interactive TTY + auto-type gaps (three-state outcome); with no auto but reportable gaps (e.g., win32 without an automatic install surface), it renders a categorized plan + concrete reasons without prompting or installing, and `reported` suppresses duplicate detached summaries; CI/non-TTY/all-healthy/opt-out keep zero prompts and zero installs while still starting web; `installerEnabled` is injected via env, and the pure-function chain never depends on ambient state. #267 hardening: the web-onboarding subprocess call becomes an awaited POSIX process-group `spawn` (SIGINT/SIGTERM are serviceable; the signal path clears the result+patch directories), and the bootstrap installer `run()` also has a bounded process-group lifecycle; outer grace covers the installer TERM+SIGKILL budget. The result channel is a 0700 `mkdtemp` + `0600/wx`; bootstrap-tests §21 crosses the real inner→bootstrap onboarding process boundary, and §21c/§21f/§21g cover prompt-wait, accepted-install parent signal, and accepted-install timeout cleanup. M4 UX quality line; does not count toward the #20 real `observed_private` cohort Exit.
- From 2026-09-08, dsh-map-tools runtime regression fix (issue #242): #239 mistakenly used the nonexistent alpha.3 exports `installSettingsSection/settingsNamespace`, breaking plugin imports in the actually installed package; this restores plain namespace strings and the `ctx.inject(['settings'], scope => scope.settings.installSection(...))` wiring, with packaged proof pinning the 7 `map_*` tools, settings watch/reload/dispose, and the offline inline coordinate path.
- From 2026-09-08, ADR-16 tenant-scoped fix (issue #224): ledger `insertEvent` writes the current tenant, `readEvents`/fold/rebuild all carry tenant conditions, and legacy/v1 migrates into `local` only; cross-tenant same id/idem_key, interleaved updates, A rebuild not affecting B/local, and cross-process reopen are pinned by ledger-tests/booking-saga-tests. Historical non-local events miswritten as `local` are not auto-guessed into repair.
- From 2026-09-10, #254 read-only repair planning: copy db/`-wal`/`-shm` to a temp directory and inventory there, reconcile source bytes before/after copy and at read end, and fail closed on any change; a move can be planned only with an explicit evidence mapping. Un-checkpointed WAL and v1 zero directory writes are both covered by isolated tests.
- From 2026-09-11, #254 authorized apply/rollback: `repair-apply` requires `--mapping` + matching `planDigest` + `--i-authorize-apply`, writes a checksummed backup first, CAS-updates `tenant_id` (keeps `seq`), rebuilds affected tenants, and supports `repair-rollback` plus receipt schema; run-all §29c is isolated-fixture evidence only. A founder-authorized real repair receipt (or confirmation that no move is needed) remains the issue close condition and does not constitute an M5/M6 gate.
- From 2026-09-08, state-cli tenant argument boundary (issue #226/#241): the ledger CLI centrally parses cmd/positional/`--state-root`/`--tenant`/`--limit`; unknown/duplicate/missing-value/invalid numeric arguments (including `.5`/`+.5`) fail closed before any state-root side effect; #241/#243 are already in main and closed; `tick`/`export`/`whatif` are explicitly local-only, and `whatif` is a whole-ledger admin snapshot, not a tenant export.
- From 2026-09-08, M4→M6 program task graph (issue #225): the M4 scorer/explicit-consent collector, tenant ledger/CLI, and the Z3 and map stability foundations are in main, with #227/#241/#242 closed; the real M3/M4 cohort, #136 supply agreement/internal authorization, and #137 P6 approval plus the real pilot remain TODO. `milestone-delivery-plan.md` continues to define ownership/E2E/falsification/exit criteria; `write-gate-production-design.md` remains a proposal only and does not unseal transactions or B2B implementation.
- From 2026-09-06, Round 9 governance surface (issue #100/#102): LLM_MAX_TOKENS injects maxTokens into the llm-deepseek directory (fixing the real relay model 2013) + GOTRY_BENCHMARK_SOFT_MS/HARD_MS budgets configurable per run (defaults 60/120s unchanged); the treatment survived the full chain for the first time, and the remainder was convergence within budget.
- From 2026-09-08, Round 10 merges Round 8's flat `tools|call|errors` discovery/recovery surface with the owner-local per-tool v3 descriptor: per-tool exact input schemas constrain both the model and the spawn, and the result/domain/failure envelope plus the latest-response terminal fence fail closed; provider/scorer/evaluator/default product path unchanged, and freezing a treatment and its score still await independent evidence.
- From 2026-09-08, the Round 10 real `glm-5.3-flash` treatment (main `c843fae`) diagnosed a provider/model visibility failure: the top-level `oneOf` wire schema caused 57 empty `{}` calls with no countable score; Round 11 only flattens the model-facing bridge wire into `action=tools|call|errors`, a descriptor-derived `tool` enum, and a generic object `arguments`, while execution still exact-validates the frozen descriptor `input_schema`.
- From 2026-09-05, metrics panel first slice (issue #138): `build-metrics-report.ts` read-only aggregates existing sidecars (fact gate verdict distribution and blocked rate / channel health / incident surface / bridge latency >500ms re-review anchor / ledger and doctor report existence) into a single markdown; engineering surface only, not M3 Exit evidence (belongs to #22).
- From 2026-09-05, policy fact production side v1 (issue #141, D-26): a VISA_POLICY_FETCH effect registry row, the tier-C China Consular Service Network country guide tree, polite fetch → PolicyFact ledger entry; the founder decided on the tier-C route, with Timatic/Sherpa° deferred.
- From 2026-09-04, fact gate coverage (issue #118, D-26 closure): the third HotelFact form (exact-date hotel search lands in the ledger, with probing/transport-failure/masked-price discipline identical to flight/train) + hotel claims entering gotry_fact_gate + one-way generation of render primitives (renderFlightFact/renderHotelFact embed fact anchors, with gate-side anchors preferring deterministic backtracking); the policy production side (real-time visa API) remains a D-26 external dependency.
- From 2026-09-10, fact gate coverage policy render anchor closure (issue #273, D-26 residual closure slice): renderPolicyFact lines embed fact anchors (same-source typed anchors as flight/train/hotel), gate-side anchors backtrack deterministically, and hand-edited/forged anchors = fact_anchor_unknown; the anchor-line as_of content fingerprint checks only the "as-of date" (rewriting or deleting the date while keeping a same-source fact_id fails closed), with no claim of sealing the full statement text or all fact drift; POLICY_WORD adds 「海关申报」 (customs declaration; same nature as 「入境申报」 entry declaration but missed by the original regex), and handwritten policy lines missing as_of → policy_without_as_of. fact-gate-tests §11, eight assertions (64 pass).
- From 2026-09-10, fact gate coverage policy anchor whole-field content fingerprint (issue #359, parent #273, D-26 residual closure): rendering and the registered gate share a single authoritative constructor; **when a policy line contains a `<!-- fact:` anchor**, it must strictly equal the canonical whole line + a single `<!-- fact:<id> -->` (subject/statement/source/fetched_at/query_id/as_of); any change → fact_anchor_unknown, same source as unknown anchors; multiple anchors, malformed anchors, and non-canonical non-empty leading/trailing text all fail closed. Policy lines **not containing** `<!-- fact:` still go through the unanchored/free-text heuristic and are out of scope for this slice (handed to parent #273 / D-26). `opts.itinerary.trip_start` bridges to `opts.tripStart`, a legitimate re-review reminder form alongside `review_by`. Deterministic synthetic / isolated evidence; no claims of live policy, supplier, or M5/M6 admission; parent #273 residual debt retained.
- From 2026-09-10, fact gate coverage flight/hotel anchor field fingerprints (issue #363, parent #273, D-26 residual closure): the render primitives (`renderFlightFact`/`renderHotelFact`) and the registered gate share the same authoritative fact — a flight line re-extracts the `flight_no` token and requires strict == with the fact (uppercase-normalized), and a hotel line must strictly contain `destination + check_in + check_out` (exact substring); any mismatch → `fact_anchor_unknown` fail-closed, same source as unknown anchors. **Field fingerprints do not replace strict whole-line comparison** (root contract item 2): the comparability of flight_no/carrier/times/route/price, train canonicalization, policy canonicalization, the §4b price-compatible forms (total budget/baggage fee/total/explicit fare/thousands separators/price on request/¥7xx/approximate prices), and the `price_contradicted` vs `unverified_price_claim` classification and ordering are not swallowed by the new fingerprints and keep the existing verdict primitives. Borrowed/unknown/duplicate anchors remain handled by the existing `fact_anchor_unknown` path; the field fingerprints only close the root counterexample of "keeping anchor/registry/fact_id while rewriting a visible flight_no/destination/travel dates", which now fails closed. The `gotry_fact_gate` registered gate execution surface adds no new paths and keeps the existing `loadFactRegistry` + `gateArtifact`; the fact log uses a real append-only JSONL (`appendFacts`), loading registry definitions into an isolated temp `stateRoot` → capturing the real `gotry_fact_gate.execute()`. Deterministic synthetic / isolated evidence; no claims of live supplier, live 12306, live policy, or M5/M6 admission; parent #273 residual debt retained.
- From 2026-09-10, fact gate coverage flight/train inline price contradiction (issue [#300](https://github.com/Danceiny/gotry/issues/300), parent [#273](https://github.com/Danceiny/gotry/issues/273)): after exact-date backtracking succeeds, the shared flight/train gate primitive checks consistency only within canonical fare fields or explicit fare-label fields, against the fully comparable CNY hard prices `¥NNN`/`CNY NNN` of the corresponding flight/train fact; amounts carrying non-fare text labels after separation do not participate, and bare/unattributed or malformed fare money = `unverified_price_claim` blocked. Numeric mismatch = `price_contradicted` blocked; source fact missing a price, unsupported currency, or no anchor with the price outside the heuristic 120-character window = `unverified_price_claim` blocked; non-numeric/from-price/approximate prices are not compared, exchange rates are not guessed, and hotel `priceRaw` does not participate; price tolerance and exchange-rate/multi-currency policy still await owner/founder decision, and not all D-26 residuals are closed.
- From 2026-09-10, fact gate coverage · non-keyword lodging categories + finite policy-word union (issues [#301](https://github.com/Danceiny/gotry/issues/301)/[#302](https://github.com/Danceiny/gotry/issues/302), parent #273, D-26 residual): the handwritten/historical read-side claim heuristic expands vocabulary: 5 non-keyword lodging categories plus no-token stayability assertions under a lodging heading are promoted to hotel claims, with a heading-depth state machine for activation/exit/inheritance (ATX 1–6); the policy side takes a finite union of 13 words, and direct reads missing as_of fail closed; Latin words are case-insensitive but bounded by word boundaries. Covered by fact-gate-tests §12/§13. Deterministic fixture coverage; no claims of real-time policy correctness, supplier readiness, or M5/M6 admission.
- From 2026-09-10, #299/#273/#355 fact gate triage and typed rail ingestion: full G/D/C/Z + 3–4-digit train numbers enter a separate `trains` claim set, so `CZ8582`/`9C8781` are not mistakenly dropped by first letter; the flight verdict looks only at flight facts, the rail verdict only at train exact-date/route/code/bookable facts, and missing evidence outputs `rail_claim_unverified`. The 12306 session plugs into the typed registry via `gotry_session_search kind=train`: the parser distinguishes recognized empty/nonempty, malformed, and transport-runtime errors; positive facts rely on the host-captured invocation date/binding and the exact response URL; inline startTrainDate is only validated and kept, collection time non-future within 15 minutes, `canWebBuy=Y`, and a closed set of available-seat tokens; the listing has no fares, and no static-schedule substitute is used. Deterministic fixture evidence only; no claims of real supplier bookability or issue closure.
- From 2026-09-04, legacy vendored dsh fallback removed (issue #120, D-27 cleared): dsh resolution recognizes only the root manifest/dependency closure and fails closed with an error pointing to reinstall when not found; DshRuntime.source converges to 'root'.
- From 2026-09-04, needs-extension copy adaptation (issue #117, D-24 cleared): developer-mode/local-channel guidance is skipped automatically based on the local channel placement — store-build users see only the store one-click install and "already installed = good to go"; the bridge failure summary and the doctor extension item adapt in sync.
- From 2026-09-09, the session search surface gains a dida supplier portal adapter (`gotry_session_search kind=dida`, SESSION_DIDA_SEARCH effect registry row) — the interpreter flat discipline is unchanged, with no new policy-table form; see `../architecture.md` §9. This design original (Stage 0-4 solving/orchestration) is unaffected; status cedes back to architecture §9.
- From 2026-09-10, #308 forward fix solidifies session kind selection: gate and execute share the `interpretArgs` query-first semantics, defaulting to flight, and unknown/malformed fails closed; the public train query surface remains constrained by sessionAccess off/ask/allow and per-site rejection/cancellation. Regression entries are session-tests §I + smoke §13.
- From 2026-09-04, interpreter migration wrap-up (issue #115, D-23): the six channels anything/web/github/video/agent_reach/session_login enter the effect registry, and the external dependency surface of all 23 tools converges on effect_interpreter.v1 (no policy-table row, no effect); the tool surface stays flat as before, and per-source evidence chain labeling is unchanged.
- From 2026-09-04, one-shot doctor summary at startup (issue #114): at web/headless startup a detached subprocess runs a read-only checkup in the background, and pending items get one line on stderr (all-ok silent / zero disk writes / non-blocking / benchmark exempt) — initialization visibility replaces running into errors mid-session.
- From 2026-09-04, the first line of tool descriptions is generated from the channel registry (issue #113): the seven retrieval tool descriptions gain a leading "service intent × channel order" card (same table as the routing field at failure sites), and doctor completes patching the host plugins dsh-map-tools/dsh-tool-ask-user in both states; map-tools is currently delivered in-package from `ts/dsh-runtime/vendor/dsh-map-tools/` as an MIT payload, and the external npm dependency was removed due to rc peer and alpha.3 closure conflicts.
- From 2026-09-04, typed parameter contract migration (issue #112, D-30, wrapped up in five cuts): all 23 registered tools' parameter-surface blobs → dsh typed ParameterSchemaSpec flat/structured fields with model-visible per-field JSON Schema; the master cut rule = tools with required fields get host-authority rejection of malformed parameters (ToolFailure shape unchanged; evidence-class P0 red lines enter the schema under host-authority gates), and all-optional tools keep the interpretArgs tolerance layer; migration locks are smoke §1/§6/§12/channel-registry-tests §8; the plain-model canary has run (10/10 on the first pass, 2026-09-04), and D-30 is fully cleared.
- From 2026-09-04, behavior contract at 22 items (cross-benchmark feedback, issue #121/#122): (1) motivation-first extends the companion arrival-chain interview (meetup/convergence types must be asked, answers land in `gotry_companion_save`, with per-chain accounting) + new (22) the arrival account must be covered (red-eye/land-then-consume flight segments explicitly feed the arrival account); provenance and scorecards in `docs/evaluation/persona-bench/`.
- From 2026-09-08, clarification-card examples pass through the anchor card (issue #2 rc18 recurrence): (8) internal clarification — candidate time-slot examples (holidays/weekends/months) listed to the user in clarification cards/interviews/options likewise only take dates after today, and past holidays never enter example enumerations; the contract stays at 22 items, persona-surface-guard-tests 4/4 pin the regression (run-all §54).
- From 2026-09-08, behavior contract 22→23 items (new (23) subtask waiting discipline, issue #194 B-track): a continuable subagent receipt id is not a job id; calling job_output/job_kill on subagents is forbidden; completion notices are delivered automatically and follow-up input uses send_message; persona-surface-guard-tests 5/5.
- From 2026-09-03, retrieval-channel orchestration converges to the channel registry + health surface (ADR-25, issue #106/#107/#108): the persona routing card `{{channel_routing_card}}` and the `routing` suggestions inside tool results are generated by `channel-registry.ts`; statements in this document about the persona (19) tool enumeration defer to that card; dsh-calendar is not mounted by default (D-9).
- S2 mock slice ✅ / S3 solver mount ✅ / S4 real LLM ✅ (MiniMax-M2, `bb880f3`) / S5 orchestration+persistence ✅ (see `../architecture.md` §9, the only current-state source); this document keeps the design original for traceability. **M1/M2 have exited; the M3 engineering surface is ready but real seed-user evidence is not closed, so M3 Exit remains open; M4 proceeds in parallel under founder authorization, and the #20/#223 scorer and #228 lifecycle collector are engineering evidence surfaces that do not constitute M3 Exit proof; M5/M6 remain bound by their respective Entry gates.
- HotelByte Booking Copilot is a separate parallel product-acceptance line: GoTry provides the collaboration surface through a BFF-only typed read-action planner under the single `booking.surface` contract (converged 2026-09-05 in #133; the former v2 form was promoted to canonical, v1 retired) — a durable projection of six lifecycle stages and seven phase literals (`terminal`/`error` are two terminal outcome kinds); production standalone accepts only BFF-bound `user.turn`/receipt continuations by default, and the full principal + binding seam is what opens `user.turn.ingress`. Deployment candidates bind exact SHA/schema/Linux Node 24+ABI provenance, and the authenticated health endpoint echoes the actual process identity and ingress mode; it does not expose `Book` and does not unseal M5. Real inventory for tenant/customer/storefront/payment-link and the unavailable/changed recovery chain still await UAT, so it stays Draft and unmergeable.
- External benchmark feedback from Discussion #78 has entered the S5 orchestration boundary: the 16th real tool dispatch per turn injects soft convergence, the 18th is the last body, and from the 19th within the same step comes structured rejection; the next native request after that batch's `step/end` suppresses inherited tool schemas. run-all §45 pins real runtime behavior with Cordis contracts + npm-mode dist/dsh headless offline E2E, and CI replays through an isolated pnpm consumer entry on the current SHA tarball. Round 1's exact DeepSeek scored environment unavailable/schema-invalid 0, and GLM hit a 300s timeout; Round 2's single frozen treatment got only as far as diagnostic-only. Round 3 focused on CLI→native invocation and tagged-JSON terminal conformance, but with the new frozen treatment the planner/runner exited 1 after the runner's single dispatch, with a 0-byte terminal state and the evaluator never entering. The Round 4 treatment (SHA `5ebddb2(rewritten as 0e93eae)`) had primary preflight=pass, but both planner/runner ended at 30.968s with exit 1, released 0 bytes, and the evaluator never entered; the product gate runs Node v24.20.0 while the treatment used v26.3.0, so diagnostic-only, no uplift. GitHub Node 22/24 §48 additionally exposed a source default-off 30s lifecycle hang. Round 5 is limited to removing the timer/keepalive preload, setting the locked DSH runtime closure to alpha.3, keeping `ts/dsh-runtime/gotry-state/` state continuity for normal source runs, isolating benchmark/package into the invoking directory, and adding a benchmark-only structured diagnostics pipe (allowlisted redacted reason codes) while stdout stays fail-closed; its frozen treatment (code SHA `752e54c(rewritten as 7d6f8a9)`) stopped after 140.715 seconds with `child_nonzero_exit`, 0 terminal bytes, and evaluator/official scores null — diagnostic-only; later pure lock-consistency commits do not rewrite that UID attribution and do not claim offline E2E as benchmark improvement.
- Phase 1 environment bridge current state: for the default-off owner-local v3 descriptor the model-visible wire is flat `action=tools|call|errors`; the `tool` enumeration derives from the descriptor, `arguments` is a generic object, `tools` does discovery, and `errors` returns a closed bridge protocol/infrastructure recovery inventory. Execution still exact-validates the frozen descriptor `input_schema`, and the responsibility boundary between the model surface and the pre-spawn validator is unchanged. The adapter accepts only exact `gotry_benchmark_tool_result_v1`: concrete results are bound by the non-empty `output_keys` positive-key contract, finite exact `domain_outcomes` enter the model history with exit 0, and nonzero exits remain infrastructure failures; the bridge does not auto-retry. Conformance takes the latest bridge response as the tagged-terminal ordering lower bound, and older terminal states cannot shadow later domain/failure. cold-start, headless one-shot, native definition-only, host-enforced writes/network isolation, and the default-off product boundary are unchanged; there is currently no attributable official score/uplift.
- Round 5 runtime resolution: both the source checkout and the clean package resolve the same DSH `0.1.5-alpha.1` closure of 230 exact direct dependencies (2026-09-09 issue #268 migrated exactly from the `0.1.2-alpha.3` 216-package closure: 15 new sentinels + removal of `dsh-tool-subagent-report`; all 230 `@deepseek-ai/dsh*` packages pin exact versions, rejecting the semver prerelease drift where `^0.1.5-alpha.1` would match `0.1.5-alpha.2`; CI `npm ci --strict-peer-deps` / pnpm `--strict-peer-dependencies` are explicitly strict; run-all §23a-§23e gives five deterministic proofs. Settings behavior unchanged; no tag or npm version published yet; these deterministic proofs do not constitute M5/M6 admission); the DSH name sets in the manifest, package-lock, and root pnpm importer must all equal 230, and publish preverify fails closed on missing pins, mixed versions, or range declarations. For normal source runs the dsh cwd stays `ts/dsh-runtime/`, state continues to land in `ts/dsh-runtime/gotry-state/`, and benchmark opt-in and the npm package isolate into the invoking directory; the legacy vendored alpha.1 remains only a non-benchmark resolution compatibility, with no runnability promise. The benchmark rejects actual DSH version drift before spawn; the Node floor is unified at 22.15, and older versions are stably rejected before DSH import/spawn.
- Round 6 terminal diagnostics: benchmark conformance observes only the final `turn/end`, mapping to a coarse enum by allowlisted code/limited status; all free text and raw stderr stay out of the control plane. The per-session arbiter writes only once at the final terminal state, bridge/conformance-specific reasons take priority, and a successfully recovered model retry yields zero failure output. The ChinaTravel frozen treatment of code SHA `c61600b(rewritten as ea678f8)` (`..._00001`, `deepseek-v4-flash`) stably output `child_runtime_error` after 49.546 seconds, still with 0 terminal bytes and evaluator/official scores null; leakage and local credential/endpoint scans were both 0. This slice does not change retry, prompt, tool, or scoring logic and produces no uplift.
- Round 7 minimal kernel: the benchmark opt-in of code SHA `edb9392896625adbb48abae4a2ecf968dbfc0349` keeps only the turn deadline (not installed on the product path by default; benchmark opt-in installs a wall-clock gate), model override, the single bridge, and isolation/conformance; product prompt variables/process guards/consent/ordinary tools are all not installed, and the default path is unchanged. The system prompt projects into a stable task-agnostic persona, and the root patch accepts only one canonical `insert` and one `system-prompt`; other root items/variants fail closed. The ChinaTravel frozen treatment UID `e20241028160248698752` (`easy`, `deepseek-v4-flash`) passed preflight and did not roll back, but after 80.463s the runner exited 1 with terminal 0 bytes/invalid; the evaluator never entered, official was null, and it was unscoreable; allowlisted attribution is `child_bridge_runner_failed`, producing no uplift or external benchmark closure. Round 8's generic bridge actions/recovery inventory took over this problem.
- **From 2026-08-27, the time semantics of loops ① and ③ are supplied by the `ts/src/time-anchor.ts` anchor card (ADR-12), and the hardcoded "2026 calendar" wording in ① of this document defers to the anchor layer.
- From 2026-08-28, S5 work-order persistence upgrades to the ledger durable form (ADR-15: workflow_runs/steps authoritative + json/md views); work-order file wording in this document defers to `state-ledger.ts`; `gotry_async_terminal.v1` maps 4/4 to `succeeded`/ledger `settled`/exit 0 and non-4/4 to `failed`/ledger `failed`/exit 2, and terminal-state replay keeps the same result and exit code with zero recomputation. From 2026-08-29, the solving runtime converges on `z3-shared.ts` (single WASM instance + session-level mutex, run-all §30 concurrency regression gate) —
- The Z3 WASM race known limitation was cleared in the first round. 2026-09-08 #227 re-fix: the new root cause of intermittent heap corruption in the Node24 full-stack §30 was a cold-initialization Promise cache race + `z3-solver@5.2.0` high-level `FinalizationRegistry` native cleanup bypassing `withZ3`; this round adds a low-level cleanup local queue + actual native check barrier + fatal poison, with deterministic guards entering run-all §30b and multi-process re-entry §30c. Same 2026-08-29 batch: the dated segment in ④ is overwritten with real-time prices via `realtime-pricing.ts` (env gate default off, evidence `[实时API:flyai@ts]`), run-all §31. Same batch: the i18n catalog seam (`i18n.ts`, `GOTRY_LOCALE` defaults to zh-CN, golden standard byte-for-byte unchanged), making the solving deterministic surface available in English, run-all §32.
- Same-day second batch: OTA tool surface flattened (`gotry_flyai_search` kind=hotel plugs into Fliggy (飞猪) `search-hotel`; OTA tool descriptions and persona (19) drop the "three-level routing/main chain" hierarchy) + account session tool authorization gate (`tools/pre-execute` → dsh native approval card; requested on the first call per session, remembered within the session, and rejection revokes it for the session; the `sessionAccess: ask|allow|off` master switch can be turned off anytime, smoke §12-13/session-tests §I) + login productization (the 18th tool `gotry_session_login`: on needs-login the agent calls it directly, pops the login page in the user's Chrome, and waits for them to finish logging in on the Ctrip official site, no terminal needed;
- gotry reads only ticket cookie names, with values passing through as zero values; the login guide page not mounting ReadGuard is the only exemption surface), session-tests §J).
- The M3 Issue #22 evidence manifest, de-identification schema, and scorer have entered the engineering surface; the real 50–200-person cohort has not entered the private evidence surface, and M3 Exit remains open. 2026-08-29 same-day second batch: the nightly real-LLM evidence producer `ts/scripts/nightly-evidence.ts` (sealed prompt set + sealed price table with conservative peak conversion; without credentials: waiting/backoff/no-spend and zero writes; budget gate exits 3 on overrun; run-all §35) is in place, closing the mechanical precondition of acceptance ⑥; real nightly records await a credentialed environment run;
- The session data surface #21 already has field fixture scorer/dual-source contracts and the waiting-attach no-spend deterministic gate; real-browser acceptance status cedes to `../rfc/user-session-data-rfc.md`; on 2026-08-30 the transport layer settled on the extension bridge (MV3 one-time install replacing per-connection CDP popups, `needs-extension`/`waiting_extension` no-spend, real sf-01..08 gate = install the extension once, run-all §38).
- Same 2026-08-30 batch, onboarding UX loop closure (issue #21 P3.6, later withdrawn on 2026-09-02 after the store listing, with §3.3 responsibility handed back): the `npx @danceiny/gotry setup wizard` single command + cross-platform GUI panels (macOS osascript / Linux zenity / Windows msg / headless terminal) + background health-watch auto-replaying the same query_id within ≤120s; the first user-side version got down to 3 clicks + 0 terminal commands + zero rerun after install (after withdrawal, the dsh UI verdict.installUrl took over rendering, bringing users further down to 1 click: "Add to Chrome" in the Chrome Web Store);
- `ts/capabilities/session/{wizard,health-watch}.ts` + `scripts/health-watch-cli.ts` + the bootstrap `wizard` subcommand; run-all §40 onboarding-tests 9/9 + bootstrap-tests 7/7 wizard section.
- Same 2026-08-30 batch, issue #46 (P0 factuality): bookable facts converge on the single data source `gotry_bookable_fact.v1` (exact-date tool results hit/miss land in the ledger; miss forbids backfill); delivering an artifact containing bookable facts must pass the 21st tool `gotry_fact_gate` first (ADR-19, run-all §39, smoke §16), and the tool-inventory section of this document cedes status to `../architecture.md` §3. Same 2026-08-30 batch, issue #21 distribution channels (ADR-21): extension artifact distribution has two channels —
- The GitHub Releases download channel has landed (`--extension-from=github` explicit opt-in, SHA256+key pinning + fallback to bundled on failure, run-all §43); the Chrome Web Store listing is live (2026-09-02 v0.1.0, one-click install + auto-update = the recommended install surface; the store re-signs the key ⇒ the store-build extension ID is independent, and the bridge Origin allowlist trusts both channels identically, `docs/ops/extension-webstore-submission.md`; D-25 cleared).
- Same 2026-08-30 batch, issue #49 (price table v2 + long-term price drift mechanism, ADR-20): the sealed price table upgrades from `gotry_llm_price_table_v1` to `v2` (provider-aware: DeepSeek tiered_peak_offpeak + MiniMax flat_no_offpeak; MiniMax M2/M2.1/M3 enter the table, with M3 taking the >512k tokens tier as the peak ceiling to uphold ADR-11's "peak only-high-not-low");
- Price drift monitoring `ts/scripts/price-drift-watch.ts` + `tests` (covering the four mainstream providers DeepSeek/MiniMax/OpenAI/Anthropic; by default compares offline against a baseline fixture and outputs a PR-ready Markdown diff with the four directions model/field/from/to/direction; `--fetch` pulls official pages + writes the fixture the first time, and **prices are never auto-applied** — price changes go through PR + human review); run-all §41 contract verification 8/8.
- Same 2026-08-30 batch, issue #77 (P2 configuration surface): the `.env` trio's `LLM_MODEL` is wired into the dsh session surface — the bin maps `GOTRY_LLM_MODEL` + the gotry-tools plugin `agent/request` waterfall in-memory override (dsh settings user layer ~/.dsh takes precedence over the composition layer, which a patch alone cannot override; overrides are zero-persistence and never rewrite user settings) + runtime cordis patch by-id double override (`agent-default-model` default model + `llm-deepseek` directory);
- Without `LLM_MODEL` set, zero behavior change, and the .env.example default line becomes a comment; the E2E `ts/scripts/model-override-e2e.ts` four scenarios (mock relay + isolated DSH_HOME + current tarball clean installed-package bin) are all green, no longer faking package mode by moving source dependencies; smoke §17.
> **M4 Issue #20/#223/#228**:
- The paired-cohort/active-planning/experience-reflux synthetic fixture scorer has entered run-all §34; #223 is hardened with layer-by-layer exact schemas, HMAC-SHA256 pseudonymized keys, N=5/median reduction=0.5 frozen thresholds (raw ratio comparison, rounding only in reports), and observed-private source-review attestation + the `reviewed_summary_digest_sha256` binding contract. The #228 collector records first-return flow, external wait, reflux, and preference only under explicit stateRoot/consent/HMAC, exports candidate/synthetic scorer inputs, and does not fabricate manual attestation. They prove only the evidence contract and the collection path; the real `observed_private` N≥5 repeat cohort + human verification contract remains an Exit precondition, and M3 Exit must not be back-inferred. From 2026-08-29, the toolchain availability semantics of this document's input cases defer to `capabilities/` and install-time bootstrapping (Issue #24: weather geocoding dual-source fallback / flyai past-date pre-validation with miss/error as separate statements / hbcli official bootstrap + static-package filtering degradation by destination).
- From 2026-08-29, work-order deliverables and working-directory artifacts are viewable directly inside dsh (`gotry_artifacts_list/read`), with host sidebar workbench rendering (dsh-better-sidebar, installed via `gotry setup`). Same 2026-08-29 batch, issue #16 adopted: external channels converge on the effect interpretation layer `effect_interpreter.v1` (ADR-18, `capabilities/effect.ts`+`resilience.ts`) —
- Exponential backoff retry/circuit breaker/mock interpreters execute uniformly per the per-effect policy table (all off by default, zero behavior change); flyai/hotel/session/weather/flight_verify and realtime-pricing query entrances already go through `interpretEffect`, with the remaining channels migrating incrementally (D-23), run-all §37; the tool surface stays flat as before, per-source evidence chain labeling is unchanged, and the tool-inventory section of this document cedes status to `../architecture.md` §3 and `effect-interpreter.md`.
> **Issue #67 session benchmark status**: `sf-live-benchmark --golden=static` uses a fixed OpenFlights revision for route/carrier and a hand-maintained manifest for estimated schedules/price bands; evidence explicitly records requested/effective source, provenance, estimated fields, and fallback reason. Static source failure warns on stderr and falls back to manual, never masquerading as live schedules/fares/inventory; the real session side still depends on the user's Chrome extension. The offline contract belongs to run-all §44.
> **Issue #67 real-run boundary and bridge lifecycle**: over two consecutive rounds of static official on a logged-in Chrome, 8/8 hit and fallback 0 both times; session scored 3/8 and 5/8 hits respectively, and all scoreable hits (3+5 entries) were 13/13=100%, with every non-hit an explicit miss. This proves soft scoring ≥90% and auditable sourcing; it does not prove 8/8 bookability. The default bridge now calls `unref` on idle parked timers/sockets, and the wizard `keepBridge` is unchanged; §38 24/24, §40 9/9.

> Evaluation Phase 0 foundation boundary: contracts/registry/validators/unmatched diagnostic fixtures/test-only aggregate admission plus a deterministic PR/nightly/weekly/milestone cadence policy/planner. It returns admission, `pass^k`, budgets, calibration, failure-registry, and cross-benchmark synthesis obligations only; it has no scheduler, external launch, spend, score, Agent-round, or uplift effect. No Python runtime dependency, baseline, or matched production evidence is included.
> Founder directive (third correction): implement top-down; do not polish details bottom-up.
> This document is the **single authoritative design** for Stage 1; all implementation work derives from here. The leaves (solvers/engines) are in place; what is missing is the trunk.
> The key architectural judgment comes first: **architectural validation of the conversation loop does not need DEEPSEEK_API_KEY — go first with a mock LLM; the API key only unlocks intelligence quality and does not block the architecture.** The earlier "Stage 1 is fully blocked on the key" was a misjudgment.

## 1. Top-level black box: system behavior of one session

Input (the user's first message, a real case):
> Landing in Shenzhen at 22:40 on Friday 7.17; on the morning of 7.18, going to Hong Kong to open a bank account & sign an insurance contract; ... departing Shenzhen in the early hours of Monday 8.10, to arrive in Dubai before work on Monday. Please plan and recommend flights and hotels for my trip.

The system must complete this in one turn (what Kimi botched over 13 turns):

```
user message ──► ① calendar/fact assertion (2026 calendar, weekday mapping computed once, persisted into state forever)
          ──► ② interview completion (ask for whatever is missing: work hours? booked resources? companions? budget tier?)
          ──► ③ JourneySpec extraction (natural language → unified model, the LLM's translation duty)
          ──► ④ solving (unified engine, deterministic duty: anchors/work windows/full cost/wish pool)
          ──► ⑤ rendering (transparent cards + full-cost table + multiple-choice gates, LLM explanation + templates)
          ──► ⑥ when complex: async ("come back in an hour", loopx tick)
```

② is incremental follow-up rather than starting over; ③④⑤ are re-entrant each turn (when the user changes one answer, only the affected segments re-run). **State persists; the human is not a system component.**

## 2. First-level decomposition: component contracts

### 2.1 Session state TripState (the top-level data contract; every component reads and writes around it)

```ts
TripState = {
  calendar: { year: 2026, assertedWeekdays: {...} }        // ① output; assert once, use for life
  profile: { workWindow?, companions?, budgetTier?, ... }  // ② output (Kimi retrospective: these two used to appear last)
  spec?: JourneySpec                                       // ③ output (unified model, already exists)
  solve?: SolveResult                                      // ④ output (already exists: verdicts/exclusions/red_flags)
  gates: Gate[]                                            // ⑤ pending questions (multiple choice)
  wishes: WishEntry[]                                      // "the next trip"
}
```

### 2.2 Tool surface (L2 contract; registered as dsh plugins, 3 exist, 2 to add)

| Tool | Responsibility | Status |
|---|---|---|
| `gotry_interview_next(TripState) → Question[]` | Deterministic (missing-field driven, not LLM improvisation) | **To define** |
| `gotry_spec_extract(conversation history) → JourneySpec` | LLM (translation) | **To define** (inside the dsh runtime) |
| `gotry_solve(JourneySpec) → SolveResult` | Deterministic (implemented: unified) | ✅ |
| `gotry_render(SolveResult) → cards/tables/gates` | Templates + LLM polish | Partial (answer_md exists) |
| `gotry_wish_pool_add` / `gotry_motivation_save` | Deterministic | ✅ (plugins exist; `motivation_save` accepts `homeCity` + optional/explicit exact `homeCityEvidence`, persisting `homeCityPreference { value, evidence, updated_at }`; `resolveDefaultOrigin` serves only as the precedence contract — issue #338) |

The iron law of responsibility is unchanged: the LLM only does question phrasing for ②, translation for ③, and explanation for ⑤; **verdicts and arithmetic are always deterministic components**.

### 2.3 Conversation loop (L2 orchestration contract)

```
loop:
  msg ← user
  state ← TripState.load(session)
  ①if new facts conflict with calendar/profile → point it out and confirm (never silently reshuffle)
  ②qs = interview_next(state); if qs non-empty and msg unanswered → follow up (incremental)
  ③spec = spec_extract(history + state)     // LLM
  ④state.solve = solve(spec)                // deterministic
  ⑤reply = render(state.solve) + gates      // LLM+templates
  TripState.save(state); → reply
```

## 3. Top-down implementation order (each step has independent acceptance; leaves move last)

| Step | What | Acceptance | Depends on |
|---|---|---|---|
| S1 | **Contract freeze**: TripState and the 5 tools' schemas (TS types + JSON Schema) land in `ts/src/contracts.ts` | Contract walkthrough passes (one founder review) | None |
| S2 | **Mock vertical slice**: mock-LLM (deterministic script: reads a script and replays the user-side input of the Kimi conversation) + real tool surface → the §2.3 loop runs end to end | Replay with your original opening line: the system proactively asks for the work window and the booked hotel, the calendar is asserted once, and it produces the plan and gates — **zero API key throughout** | S1 |
| S3 | Solver mount (wire the finished unified into the loop as the implementation of gotry_solve) | Replay output is equivalent to the current demo plan document | S2 |
| S4 | Real LLM hookup (dsh runtime + DEEPSEEK_API_KEY) | Same opening line, real conversation quality ≥ mock replay (the acceptance criterion from the Kimi retrospective) | S2+key |
| S5 | Async mode made real (loopx tick drives "in an hour") | The four no-disappointment criteria hold in real conversations | S4 |

**This order demotes "waiting for the key" from an architecture blocker to a quality variable of S4: S1-S3 can all be done now.**

## 4. New ADRs

- **ADR-8 (mock-LLM first)**: architectural validation of the conversation loop uses a deterministic scripted LLM and does not depend on a real model; intelligence quality and architectural correctness are decoupled. Retirement condition: after S4 completes, the mock is kept as a regression fixture.
- **ADR-9 (interview determinism)**: `interview_next` is driven by missing fields (a configurable question bank); the LLM only polishes question wording — the root cause of Kimi's "never interviews" disease is improvisation, and deterministic driving is the cure.

## 5. Relation to debt and milestones

- D-3 (LLM not in the loop) decomposes into: S1-S3 (architecture, actionable) + S4 (intelligence, waiting for the key) — the "architecture half" of the debt no longer blocks.
- D-4 (UI) stays after Stage 1; this design's L1 is "conversation as the interface", with gates presented as in-message multiple choice.
- The existing unified solver/data packs/plugins = the leaves of this design, zero rework.
